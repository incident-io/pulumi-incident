bin/
.make/

# tfgen writes a Pulumi home here, including credentials.json.
.pulumi

# Language SDK build output. The generated SDK *sources* are committed — that is
# what lets consumers install pre-built packages — but their build artifacts and
# per-language toolchain droppings are not.
**/bin/
**/obj/
**/dist/
**/nuget/
**/node_modules/
**/version.txt
sdk/python/venv/

# Stack files from acceptance tests.
Pulumi.*.yaml

go.work
go.work.sum
*.tar.gz

.DS_Store
