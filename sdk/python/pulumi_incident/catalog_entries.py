# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = ['CatalogEntriesArgs', 'CatalogEntries']

@pulumi.input_type
class CatalogEntriesArgs:
    def __init__(__self__, *,
                 catalog_entries_id: pulumi.Input[_builtins.str],
                 entries: pulumi.Input[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]],
                 managed_attributes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None):
        """
        The set of arguments for constructing a CatalogEntries resource.

        :param pulumi.Input[_builtins.str] catalog_entries_id: ID of this catalog type
        :param pulumi.Input[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]] entries: Map of external ID to entry in the catalog.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] managed_attributes: The set of attributes that are managed by this resource. By default, all attributes are managed by this resource.
        """
        pulumi.set(__self__, "catalog_entries_id", catalog_entries_id)
        pulumi.set(__self__, "entries", entries)
        if managed_attributes is not None:
            pulumi.set(__self__, "managed_attributes", managed_attributes)

    @_builtins.property
    @pulumi.getter(name="catalogEntriesId")
    def catalog_entries_id(self) -> pulumi.Input[_builtins.str]:
        """
        ID of this catalog type
        """
        return pulumi.get(self, "catalog_entries_id")

    @catalog_entries_id.setter
    def catalog_entries_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "catalog_entries_id", value)

    @_builtins.property
    @pulumi.getter
    def entries(self) -> pulumi.Input[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]]:
        """
        Map of external ID to entry in the catalog.
        """
        return pulumi.get(self, "entries")

    @entries.setter
    def entries(self, value: pulumi.Input[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]]):
        pulumi.set(self, "entries", value)

    @_builtins.property
    @pulumi.getter(name="managedAttributes")
    def managed_attributes(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        The set of attributes that are managed by this resource. By default, all attributes are managed by this resource.
        """
        return pulumi.get(self, "managed_attributes")

    @managed_attributes.setter
    def managed_attributes(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "managed_attributes", value)


@pulumi.input_type
class _CatalogEntriesState:
    def __init__(__self__, *,
                 catalog_entries_id: pulumi.Input[Optional[_builtins.str]] = None,
                 entries: pulumi.Input[Optional[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]]] = None,
                 managed_attributes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None):
        """
        Input properties used for looking up and filtering CatalogEntries resources.

        :param pulumi.Input[_builtins.str] catalog_entries_id: ID of this catalog type
        :param pulumi.Input[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]] entries: Map of external ID to entry in the catalog.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] managed_attributes: The set of attributes that are managed by this resource. By default, all attributes are managed by this resource.
        """
        if catalog_entries_id is not None:
            pulumi.set(__self__, "catalog_entries_id", catalog_entries_id)
        if entries is not None:
            pulumi.set(__self__, "entries", entries)
        if managed_attributes is not None:
            pulumi.set(__self__, "managed_attributes", managed_attributes)

    @_builtins.property
    @pulumi.getter(name="catalogEntriesId")
    def catalog_entries_id(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        ID of this catalog type
        """
        return pulumi.get(self, "catalog_entries_id")

    @catalog_entries_id.setter
    def catalog_entries_id(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "catalog_entries_id", value)

    @_builtins.property
    @pulumi.getter
    def entries(self) -> pulumi.Input[Optional[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]]]:
        """
        Map of external ID to entry in the catalog.
        """
        return pulumi.get(self, "entries")

    @entries.setter
    def entries(self, value: pulumi.Input[Optional[Mapping[str, pulumi.Input['CatalogEntriesEntriesArgs']]]]):
        pulumi.set(self, "entries", value)

    @_builtins.property
    @pulumi.getter(name="managedAttributes")
    def managed_attributes(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        The set of attributes that are managed by this resource. By default, all attributes are managed by this resource.
        """
        return pulumi.get(self, "managed_attributes")

    @managed_attributes.setter
    def managed_attributes(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "managed_attributes", value)


@pulumi.type_token("incident:index/catalogEntries:CatalogEntries")
class CatalogEntries(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 catalog_entries_id: pulumi.Input[Optional[_builtins.str]] = None,
                 entries: pulumi.Input[Optional[Mapping[str, pulumi.Input[Union['CatalogEntriesEntriesArgs', 'CatalogEntriesEntriesArgsDict']]]]] = None,
                 managed_attributes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        """
        This resource manages all entries for a given catalog type and should be used when
        loading many (>100) catalog entries to ensure fast and reliable plans.

        Please note that this resource is authoritative, in that it will delete _all_ entries from
        the catalog type that it doesn't manage, even those created outside of Terraform.

        If you have a catalog source such as Backstage or some custom catalog you'd like to sync
        into incident.io, this is the recommended way of achieving that.

        ## External IDs

        As this resource loads content from an existing catalog source into the incident.io
        catalog, it requires that each entry is given a stable identifier that can uniquely
        identify it in the upstream system.

        We call this the 'external ID' and it might be something like:

        - The ID of the entry in a custom catalog, often the primary key of the entry
        - Any stable human identifier (often called a slug) that uniquely reference the entry

        This external ID is what we use as a map key for the entries attribute, and how we map
        changes to one entry to an update to that same entry when the upstream changes.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident
        import pulumi_std as std

        def try_(*fns):
            for fn in fns:
                try:
                    result = fn()
                    return result
                except:
                    continue
            return None


        ################################################################################
        # Simple Example
        ################################################################################
        simple_example = incident.CatalogType("simple_example",
            name="Simple Example",
            description="A simple example catalog type",
            source_repo_url="https://example.com/myrepo")
        example_url = incident.CatalogTypeAttribute("example_url",
            catalog_type_id=simple_example.id,
            name="url",
            type="String")
        simple_example_catalog_entries = incident.CatalogEntries("simple_example",
            catalog_entries_id=simple_example.id,
            entries={
                "foo": {
                    "name": "Foo",
                    "attribute_values": example_url.id.apply(lambda id: {
                        id: {
                            "value": "https://example.com/foo",
                        },
                    }),
                },
                "bar": {
                    "name": "Bar",
                    "attribute_values": example_url.id.apply(lambda id: {
                        id: {
                            "value": "https://example.com/bar",
                        },
                    }),
                },
            })
        catalog = {str(entry["uid"]): entry for entry in std.jsondecode(input=std.file(input="catalog.json")["result"])["result"]}
        # Define the catalog type, creating attributes that map to the values in the
        # catalog data source.
        service = incident.CatalogType("service",
            name="Service",
            description="All services that we run at Example Org")
        service_owner = incident.CatalogTypeAttribute("service_owner",
            catalog_type_id=service.id,
            name="Owner",
            type="String")
        service_description = incident.CatalogTypeAttribute("service_description",
            catalog_type_id=service.id,
            name="Description",
            type="String")
        service_tags = incident.CatalogTypeAttribute("service_tags",
            catalog_type_id=service.id,
            name="Tags",
            type="String",
            array=True)
        ################################################################################
        # Provision the entries
        ################################################################################
        # This is where we create all the entries. If we have any entries in Service
        # that are not defined in this resource, we will delete them.
        services = incident.CatalogEntries("services",
            catalog_entries_id=service.id,
            entries={external_id: {
                "name": entry["metadata"]["name"],
                "aliases": [entry["metadata"]["name"]],
                "attribute_values": pulumi.Output.all(
                    serviceOwnerId=service_owner.id,
                    try_=try_(
                        lambda: entry["spec"]["owner"],
                        lambda: None
                    ),
                    serviceDescriptionId=service_description.id,
                    try_1=try_(
                        lambda: entry["metadata"]["description"],
                        lambda: None
                    ),
                    serviceTagsId=service_tags.id,
                    try_2=try_(
                        lambda: entry["metadata"]["tags"],
                        lambda: None
                    )
        ).apply(lambda resolved_outputs: {attribute: binding for attribute, binding in sorted({
                    resolved_outputs['serviceOwnerId']: {
                        "value": resolved_outputs['try_'],
                    },
                    resolved_outputs['serviceDescriptionId']: {
                        "value": resolved_outputs['try_1'],
                    },
                    resolved_outputs['serviceTagsId']: {
                        "arrayValue": resolved_outputs['try_2'],
                    },
                }.items()) if try_(
                    lambda: binding["value"],
                    lambda: binding["arrayValue"]
                ) is not None})
        ,
            } for externalId, entry in sorted(catalog.items())})
        ```

        ## Import

        The `pulumi import` command can be used, for example:

        !/bin/bash

        Import catalog entries using the catalog_type_id
        Replace the ID with a real catalog type ID from your incident.io organization

        ```sh
        $ pulumi import incident:index/catalogEntries:CatalogEntries example 01ABC123DEF456GHI789JKL
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] catalog_entries_id: ID of this catalog type
        :param pulumi.Input[Mapping[str, pulumi.Input[Union['CatalogEntriesEntriesArgs', 'CatalogEntriesEntriesArgsDict']]]] entries: Map of external ID to entry in the catalog.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] managed_attributes: The set of attributes that are managed by this resource. By default, all attributes are managed by this resource.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: CatalogEntriesArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        This resource manages all entries for a given catalog type and should be used when
        loading many (>100) catalog entries to ensure fast and reliable plans.

        Please note that this resource is authoritative, in that it will delete _all_ entries from
        the catalog type that it doesn't manage, even those created outside of Terraform.

        If you have a catalog source such as Backstage or some custom catalog you'd like to sync
        into incident.io, this is the recommended way of achieving that.

        ## External IDs

        As this resource loads content from an existing catalog source into the incident.io
        catalog, it requires that each entry is given a stable identifier that can uniquely
        identify it in the upstream system.

        We call this the 'external ID' and it might be something like:

        - The ID of the entry in a custom catalog, often the primary key of the entry
        - Any stable human identifier (often called a slug) that uniquely reference the entry

        This external ID is what we use as a map key for the entries attribute, and how we map
        changes to one entry to an update to that same entry when the upstream changes.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident
        import pulumi_std as std

        def try_(*fns):
            for fn in fns:
                try:
                    result = fn()
                    return result
                except:
                    continue
            return None


        ################################################################################
        # Simple Example
        ################################################################################
        simple_example = incident.CatalogType("simple_example",
            name="Simple Example",
            description="A simple example catalog type",
            source_repo_url="https://example.com/myrepo")
        example_url = incident.CatalogTypeAttribute("example_url",
            catalog_type_id=simple_example.id,
            name="url",
            type="String")
        simple_example_catalog_entries = incident.CatalogEntries("simple_example",
            catalog_entries_id=simple_example.id,
            entries={
                "foo": {
                    "name": "Foo",
                    "attribute_values": example_url.id.apply(lambda id: {
                        id: {
                            "value": "https://example.com/foo",
                        },
                    }),
                },
                "bar": {
                    "name": "Bar",
                    "attribute_values": example_url.id.apply(lambda id: {
                        id: {
                            "value": "https://example.com/bar",
                        },
                    }),
                },
            })
        catalog = {str(entry["uid"]): entry for entry in std.jsondecode(input=std.file(input="catalog.json")["result"])["result"]}
        # Define the catalog type, creating attributes that map to the values in the
        # catalog data source.
        service = incident.CatalogType("service",
            name="Service",
            description="All services that we run at Example Org")
        service_owner = incident.CatalogTypeAttribute("service_owner",
            catalog_type_id=service.id,
            name="Owner",
            type="String")
        service_description = incident.CatalogTypeAttribute("service_description",
            catalog_type_id=service.id,
            name="Description",
            type="String")
        service_tags = incident.CatalogTypeAttribute("service_tags",
            catalog_type_id=service.id,
            name="Tags",
            type="String",
            array=True)
        ################################################################################
        # Provision the entries
        ################################################################################
        # This is where we create all the entries. If we have any entries in Service
        # that are not defined in this resource, we will delete them.
        services = incident.CatalogEntries("services",
            catalog_entries_id=service.id,
            entries={external_id: {
                "name": entry["metadata"]["name"],
                "aliases": [entry["metadata"]["name"]],
                "attribute_values": pulumi.Output.all(
                    serviceOwnerId=service_owner.id,
                    try_=try_(
                        lambda: entry["spec"]["owner"],
                        lambda: None
                    ),
                    serviceDescriptionId=service_description.id,
                    try_1=try_(
                        lambda: entry["metadata"]["description"],
                        lambda: None
                    ),
                    serviceTagsId=service_tags.id,
                    try_2=try_(
                        lambda: entry["metadata"]["tags"],
                        lambda: None
                    )
        ).apply(lambda resolved_outputs: {attribute: binding for attribute, binding in sorted({
                    resolved_outputs['serviceOwnerId']: {
                        "value": resolved_outputs['try_'],
                    },
                    resolved_outputs['serviceDescriptionId']: {
                        "value": resolved_outputs['try_1'],
                    },
                    resolved_outputs['serviceTagsId']: {
                        "arrayValue": resolved_outputs['try_2'],
                    },
                }.items()) if try_(
                    lambda: binding["value"],
                    lambda: binding["arrayValue"]
                ) is not None})
        ,
            } for externalId, entry in sorted(catalog.items())})
        ```

        ## Import

        The `pulumi import` command can be used, for example:

        !/bin/bash

        Import catalog entries using the catalog_type_id
        Replace the ID with a real catalog type ID from your incident.io organization

        ```sh
        $ pulumi import incident:index/catalogEntries:CatalogEntries example 01ABC123DEF456GHI789JKL
        ```


        :param str resource_name: The name of the resource.
        :param CatalogEntriesArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(CatalogEntriesArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 catalog_entries_id: pulumi.Input[Optional[_builtins.str]] = None,
                 entries: pulumi.Input[Optional[Mapping[str, pulumi.Input[Union['CatalogEntriesEntriesArgs', 'CatalogEntriesEntriesArgsDict']]]]] = None,
                 managed_attributes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = CatalogEntriesArgs.__new__(CatalogEntriesArgs)

            if catalog_entries_id is None and not opts.urn:
                raise TypeError("Missing required property 'catalog_entries_id'")
            __props__.__dict__["catalog_entries_id"] = catalog_entries_id
            if entries is None and not opts.urn:
                raise TypeError("Missing required property 'entries'")
            __props__.__dict__["entries"] = entries
            __props__.__dict__["managed_attributes"] = managed_attributes
        super(CatalogEntries, __self__).__init__(
            'incident:index/catalogEntries:CatalogEntries',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            catalog_entries_id: pulumi.Input[Optional[_builtins.str]] = None,
            entries: pulumi.Input[Optional[Mapping[str, pulumi.Input[Union['CatalogEntriesEntriesArgs', 'CatalogEntriesEntriesArgsDict']]]]] = None,
            managed_attributes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None) -> 'CatalogEntries':
        """
        Get an existing CatalogEntries resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] catalog_entries_id: ID of this catalog type
        :param pulumi.Input[Mapping[str, pulumi.Input[Union['CatalogEntriesEntriesArgs', 'CatalogEntriesEntriesArgsDict']]]] entries: Map of external ID to entry in the catalog.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] managed_attributes: The set of attributes that are managed by this resource. By default, all attributes are managed by this resource.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _CatalogEntriesState.__new__(_CatalogEntriesState)

        __props__.__dict__["catalog_entries_id"] = catalog_entries_id
        __props__.__dict__["entries"] = entries
        __props__.__dict__["managed_attributes"] = managed_attributes
        return CatalogEntries(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="catalogEntriesId")
    def catalog_entries_id(self) -> pulumi.Output[_builtins.str]:
        """
        ID of this catalog type
        """
        return pulumi.get(self, "catalog_entries_id")

    @_builtins.property
    @pulumi.getter
    def entries(self) -> pulumi.Output[Mapping[str, 'outputs.CatalogEntriesEntries']]:
        """
        Map of external ID to entry in the catalog.
        """
        return pulumi.get(self, "entries")

    @_builtins.property
    @pulumi.getter(name="managedAttributes")
    def managed_attributes(self) -> pulumi.Output[Optional[Sequence[_builtins.str]]]:
        """
        The set of attributes that are managed by this resource. By default, all attributes are managed by this resource.
        """
        return pulumi.get(self, "managed_attributes")

