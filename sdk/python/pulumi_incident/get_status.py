# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = [
    'GetStatusResult',
    'AwaitableGetStatusResult',
    'get_status',
    'get_status_output',
]

@pulumi.output_type
class GetStatusResult:
    """
    A collection of values returned by getStatus.
    """
    def __init__(__self__, category=None, description=None, id=None, name=None, rank=None):
        if category and not isinstance(category, str):
            raise TypeError("Expected argument 'category' to be a str")
        pulumi.set(__self__, "category", category)
        if description and not isinstance(description, str):
            raise TypeError("Expected argument 'description' to be a str")
        pulumi.set(__self__, "description", description)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if rank and not isinstance(rank, int):
            raise TypeError("Expected argument 'rank' to be a int")
        pulumi.set(__self__, "rank", rank)

    @_builtins.property
    @pulumi.getter
    def category(self) -> _builtins.str:
        """
        What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`.
        """
        return pulumi.get(self, "category")

    @_builtins.property
    @pulumi.getter
    def description(self) -> _builtins.str:
        """
        Rich text description of the incident status
        """
        return pulumi.get(self, "description")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Unique ID of this incident status
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        Unique name of this status
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def rank(self) -> _builtins.int:
        """
        Order of this incident status
        """
        return pulumi.get(self, "rank")


class AwaitableGetStatusResult(GetStatusResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetStatusResult(
            category=self.category,
            description=self.description,
            id=self.id,
            name=self.name,
            rank=self.rank)


def get_status(id: Optional[_builtins.str] = None,
               name: Optional[_builtins.str] = None,
               opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetStatusResult:
    """
    Manage incident statuses.

    Each incident has a status, picked from one of the statuses configured in your
    organisations settings.

    Statuses help communicate where an incident is in its lifecycle. You can use
    statuses when filtering incidents in the dashboard, and in workflows and announcement
    rules.

    Use this data source to look up an existing incident status, either by `id` or by `name`. This is useful for referencing statuses that incident.io manages for you, such as Triage or Closed, which can't be created as an `Status` resource.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up a status that incident.io manages for you, by name.
    triage = incident.get_status(name="Triage")
    # Create an additional status called "Clean-up". Only the live and learning categories
    # can be added to: incident.io manages the rest, so they can't be customised.
    clean_up_status = incident.Status("clean_up",
        name="Clean-up",
        description="Not yet fully finished, but isn't a live incident anymore.",
        category="live")
    # Reference the status by its ID.
    clean_up = incident.get_status_output(id=clean_up_status.id)
    ```


    :param _builtins.str id: Unique ID of this incident status
    :param _builtins.str name: Unique name of this status
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('incident:index/getStatus:getStatus', __args__, opts=opts, typ=GetStatusResult).value

    return AwaitableGetStatusResult(
        category=pulumi.get(__ret__, 'category'),
        description=pulumi.get(__ret__, 'description'),
        id=pulumi.get(__ret__, 'id'),
        name=pulumi.get(__ret__, 'name'),
        rank=pulumi.get(__ret__, 'rank'))
def get_status_output(id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                      name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                      opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetStatusResult]:
    """
    Manage incident statuses.

    Each incident has a status, picked from one of the statuses configured in your
    organisations settings.

    Statuses help communicate where an incident is in its lifecycle. You can use
    statuses when filtering incidents in the dashboard, and in workflows and announcement
    rules.

    Use this data source to look up an existing incident status, either by `id` or by `name`. This is useful for referencing statuses that incident.io manages for you, such as Triage or Closed, which can't be created as an `Status` resource.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up a status that incident.io manages for you, by name.
    triage = incident.get_status(name="Triage")
    # Create an additional status called "Clean-up". Only the live and learning categories
    # can be added to: incident.io manages the rest, so they can't be customised.
    clean_up_status = incident.Status("clean_up",
        name="Clean-up",
        description="Not yet fully finished, but isn't a live incident anymore.",
        category="live")
    # Reference the status by its ID.
    clean_up = incident.get_status_output(id=clean_up_status.id)
    ```


    :param _builtins.str id: Unique ID of this incident status
    :param _builtins.str name: Unique name of this status
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('incident:index/getStatus:getStatus', __args__, opts=opts, typ=GetStatusResult)
    return __ret__.apply(lambda __response__: GetStatusResult(
        category=pulumi.get(__response__, 'category'),
        description=pulumi.get(__response__, 'description'),
        id=pulumi.get(__response__, 'id'),
        name=pulumi.get(__response__, 'name'),
        rank=pulumi.get(__response__, 'rank')))
