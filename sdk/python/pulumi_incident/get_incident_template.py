# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs

__all__ = [
    'GetIncidentTemplateResult',
    'AwaitableGetIncidentTemplateResult',
    'get_incident_template',
    'get_incident_template_output',
]

@pulumi.output_type
class GetIncidentTemplateResult:
    """
    A collection of values returned by getIncidentTemplate.
    """
    def __init__(__self__, expressions=None, id=None, name=None, template=None):
        if expressions and not isinstance(expressions, list):
            raise TypeError("Expected argument 'expressions' to be a list")
        pulumi.set(__self__, "expressions", expressions)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if template and not isinstance(template, dict):
            raise TypeError("Expected argument 'template' to be a dict")
        pulumi.set(__self__, "template", template)

    @_builtins.property
    @pulumi.getter
    def expressions(self) -> Sequence['outputs.GetIncidentTemplateExpressionResult']:
        """
        Expressions that make variables available in the scope
        """
        return pulumi.get(self, "expressions")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Unique identifier for this incident template
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        The name of this incident template, for the user's reference
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def template(self) -> 'outputs.GetIncidentTemplateTemplateResult':
        """
        The values an incident template applies to the incidents it creates.
        """
        return pulumi.get(self, "template")


class AwaitableGetIncidentTemplateResult(GetIncidentTemplateResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetIncidentTemplateResult(
            expressions=self.expressions,
            id=self.id,
            name=self.name,
            template=self.template)


def get_incident_template(id: Optional[_builtins.str] = None,
                          name: Optional[_builtins.str] = None,
                          opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetIncidentTemplateResult:
    """
    Manage incident templates: reusable sets of values applied to incidents created from alerts.

    Use this data source to look up an existing incident template, either by `id` or by `name`. This is useful for referencing a template the dashboard owns — for example pointing an `AlertRoute` at it via `incident_config.incident_template` — without managing the template as an `IncidentTemplate` resource. Set exactly one of the two lookup attributes; setting both, or neither, is rejected at plan time.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up an incident template by name, to reference one the dashboard owns
    # rather than managing it here — for example from an alert route.
    payments = incident.get_incident_template(name="Payments incidents")
    # Or by ID, for example one copied out of your organisation's settings.
    default = incident.get_incident_template(id="01FCNDV6P870EA6S7TK1DSYD5H")
    pulumi.export("paymentsTemplateId", payments.id)
    ```


    :param _builtins.str id: Unique identifier for this incident template
    :param _builtins.str name: The name of this incident template, for the user's reference
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('incident:index/getIncidentTemplate:getIncidentTemplate', __args__, opts=opts, typ=GetIncidentTemplateResult).value

    return AwaitableGetIncidentTemplateResult(
        expressions=pulumi.get(__ret__, 'expressions'),
        id=pulumi.get(__ret__, 'id'),
        name=pulumi.get(__ret__, 'name'),
        template=pulumi.get(__ret__, 'template'))
def get_incident_template_output(id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                                 name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                                 opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetIncidentTemplateResult]:
    """
    Manage incident templates: reusable sets of values applied to incidents created from alerts.

    Use this data source to look up an existing incident template, either by `id` or by `name`. This is useful for referencing a template the dashboard owns — for example pointing an `AlertRoute` at it via `incident_config.incident_template` — without managing the template as an `IncidentTemplate` resource. Set exactly one of the two lookup attributes; setting both, or neither, is rejected at plan time.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up an incident template by name, to reference one the dashboard owns
    # rather than managing it here — for example from an alert route.
    payments = incident.get_incident_template(name="Payments incidents")
    # Or by ID, for example one copied out of your organisation's settings.
    default = incident.get_incident_template(id="01FCNDV6P870EA6S7TK1DSYD5H")
    pulumi.export("paymentsTemplateId", payments.id)
    ```


    :param _builtins.str id: Unique identifier for this incident template
    :param _builtins.str name: The name of this incident template, for the user's reference
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('incident:index/getIncidentTemplate:getIncidentTemplate', __args__, opts=opts, typ=GetIncidentTemplateResult)
    return __ret__.apply(lambda __response__: GetIncidentTemplateResult(
        expressions=pulumi.get(__response__, 'expressions'),
        id=pulumi.get(__response__, 'id'),
        name=pulumi.get(__response__, 'name'),
        template=pulumi.get(__response__, 'template')))
