# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = [
    'GetScheduleBetaResult',
    'AwaitableGetScheduleBetaResult',
    'get_schedule_beta',
    'get_schedule_beta_output',
]

@pulumi.output_type
class GetScheduleBetaResult:
    """
    A collection of values returned by getScheduleBeta.
    """
    def __init__(__self__, id=None, name=None, team_ids=None, timezone=None):
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if team_ids and not isinstance(team_ids, list):
            raise TypeError("Expected argument 'team_ids' to be a list")
        pulumi.set(__self__, "team_ids", team_ids)
        if timezone and not isinstance(timezone, str):
            raise TypeError("Expected argument 'timezone' to be a str")
        pulumi.set(__self__, "timezone", timezone)

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Look up the schedule by ID.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        Look up the schedule by name. Names aren't unique, so this fails if more than one schedule matches.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="teamIds")
    def team_ids(self) -> Sequence[_builtins.str]:
        """
        IDs of teams that own this schedule
        """
        return pulumi.get(self, "team_ids")

    @_builtins.property
    @pulumi.getter
    def timezone(self) -> _builtins.str:
        """
        Timezone the schedule's rotations are anchored to, as an IANA name
        """
        return pulumi.get(self, "timezone")


class AwaitableGetScheduleBetaResult(GetScheduleBetaResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetScheduleBetaResult(
            id=self.id,
            name=self.name,
            team_ids=self.team_ids,
            timezone=self.timezone)


def get_schedule_beta(id: Optional[_builtins.str] = None,
                      name: Optional[_builtins.str] = None,
                      opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetScheduleBetaResult:
    """
    `ScheduleBeta` was renamed to `Schedule` in v7.0.

    This name still works and still manages the same thing, so upgrading to v7 needs no change
    to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
    be removed in v8.0.

    To move onto the new name:

    ```python
    import pulumi
    ```

    Both names are the same resource, backed by the same schema and the same API, so the move
    carries your state across and the plan after it is empty. See `Schedule` for the
    documentation.


    :param _builtins.str id: Look up the schedule by ID.
    :param _builtins.str name: Look up the schedule by name. Names aren't unique, so this fails if more than one schedule matches.
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('incident:index/getScheduleBeta:getScheduleBeta', __args__, opts=opts, typ=GetScheduleBetaResult).value

    return AwaitableGetScheduleBetaResult(
        id=pulumi.get(__ret__, 'id'),
        name=pulumi.get(__ret__, 'name'),
        team_ids=pulumi.get(__ret__, 'team_ids'),
        timezone=pulumi.get(__ret__, 'timezone'))
def get_schedule_beta_output(id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                             name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                             opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetScheduleBetaResult]:
    """
    `ScheduleBeta` was renamed to `Schedule` in v7.0.

    This name still works and still manages the same thing, so upgrading to v7 needs no change
    to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
    be removed in v8.0.

    To move onto the new name:

    ```python
    import pulumi
    ```

    Both names are the same resource, backed by the same schema and the same API, so the move
    carries your state across and the plan after it is empty. See `Schedule` for the
    documentation.


    :param _builtins.str id: Look up the schedule by ID.
    :param _builtins.str name: Look up the schedule by name. Names aren't unique, so this fails if more than one schedule matches.
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('incident:index/getScheduleBeta:getScheduleBeta', __args__, opts=opts, typ=GetScheduleBetaResult)
    return __ret__.apply(lambda __response__: GetScheduleBetaResult(
        id=pulumi.get(__response__, 'id'),
        name=pulumi.get(__response__, 'name'),
        team_ids=pulumi.get(__response__, 'team_ids'),
        timezone=pulumi.get(__response__, 'timezone')))
