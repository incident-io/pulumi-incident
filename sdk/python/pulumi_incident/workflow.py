# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = ['WorkflowArgs', 'Workflow']

@pulumi.input_type
class WorkflowArgs:
    def __init__(__self__, *,
                 condition_groups: pulumi.Input[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]],
                 continue_on_step_error: pulumi.Input[_builtins.bool],
                 expressions: pulumi.Input[Sequence[pulumi.Input['WorkflowExpressionArgs']]],
                 once_fors: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]],
                 runs_on_incident_modes: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]],
                 runs_on_incidents: pulumi.Input[_builtins.str],
                 state: pulumi.Input[_builtins.str],
                 steps: pulumi.Input[Sequence[pulumi.Input['WorkflowStepArgs']]],
                 trigger: pulumi.Input[_builtins.str],
                 delay: pulumi.Input[Optional['WorkflowDelayArgs']] = None,
                 folder: pulumi.Input[Optional[_builtins.str]] = None,
                 form_fields: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowFormFieldArgs']]]] = None,
                 include_private_escalations: pulumi.Input[Optional[_builtins.bool]] = None,
                 include_private_incidents: pulumi.Input[Optional[_builtins.bool]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 private_incident_scope: pulumi.Input[Optional[_builtins.str]] = None,
                 shortform: pulumi.Input[Optional[_builtins.str]] = None):
        """
        The set of arguments for constructing a Workflow resource.

        :param pulumi.Input[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]] condition_groups: Groups of prerequisite conditions. All conditions in at least one group must be satisfied
        :param pulumi.Input[_builtins.bool] continue_on_step_error: Whether to continue executing the workflow if a step fails
        :param pulumi.Input[Sequence[pulumi.Input['WorkflowExpressionArgs']]] expressions: The expressions to be prepared for use by steps and conditions
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] once_fors: This workflow will run 'once for' a list of references
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] runs_on_incident_modes: Which incident modes should this workflow run on? By default, workflows only run on standard incidents, but can also be configured to run on test and retrospective incidents. Possible values are: `standard`, `test`, `retrospective`.
        :param pulumi.Input[_builtins.str] runs_on_incidents: Which incidents should the workflow be applied to? Possible values are: `newly_created`, `newly_created_and_active`.
        :param pulumi.Input[_builtins.str] state: What state this workflow is in. Possible values are: `active`, `disabled`, `draft`, `error`.
        :param pulumi.Input[Sequence[pulumi.Input['WorkflowStepArgs']]] steps: Steps that are executed as part of the workflow
        :param pulumi.Input[_builtins.str] trigger: Unique name of the trigger
        :param pulumi.Input['WorkflowDelayArgs'] delay: Configuration controlling workflow delay behaviour
        :param pulumi.Input[_builtins.str] folder: Folder to display the workflow in
        :param pulumi.Input[Sequence[pulumi.Input['WorkflowFormFieldArgs']]] form_fields: User-configured form fields available in the workflow scope (manual triggers only)
        :param pulumi.Input[_builtins.bool] include_private_escalations: Whether to include private escalations
        :param pulumi.Input[_builtins.bool] include_private_incidents: DEPRECATED: use `private_incident_scope` instead. `true` when the workflow runs on private incidents (a `private_incident_scope` of `all` or `owning_teams`), `false` when the scope is `none`.
        :param pulumi.Input[_builtins.str] name: Name provided by the user when creating the workflow
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this workflow
        :param pulumi.Input[_builtins.str] private_incident_scope: Which private incidents this workflow acts on: every private incident (all), those an owning team can see (owning_teams), or none. Possible values are: `all`, `owning_teams`, `none`.
        :param pulumi.Input[_builtins.str] shortform: The shortform used to trigger this workflow (only applicable for manual triggers)
        """
        pulumi.set(__self__, "condition_groups", condition_groups)
        pulumi.set(__self__, "continue_on_step_error", continue_on_step_error)
        pulumi.set(__self__, "expressions", expressions)
        pulumi.set(__self__, "once_fors", once_fors)
        pulumi.set(__self__, "runs_on_incident_modes", runs_on_incident_modes)
        pulumi.set(__self__, "runs_on_incidents", runs_on_incidents)
        pulumi.set(__self__, "state", state)
        pulumi.set(__self__, "steps", steps)
        pulumi.set(__self__, "trigger", trigger)
        if delay is not None:
            pulumi.set(__self__, "delay", delay)
        if folder is not None:
            pulumi.set(__self__, "folder", folder)
        if form_fields is not None:
            pulumi.set(__self__, "form_fields", form_fields)
        if include_private_escalations is not None:
            pulumi.set(__self__, "include_private_escalations", include_private_escalations)
        if include_private_incidents is not None:
            warnings.warn("""Use `private_incident_scope` instead. When both are set they must agree (include_private_incidents is true for the all and owning_teams scopes, false for none).""", DeprecationWarning)
            pulumi.log.warn("""include_private_incidents is deprecated: Use `private_incident_scope` instead. When both are set they must agree (include_private_incidents is true for the all and owning_teams scopes, false for none).""")
        if include_private_incidents is not None:
            pulumi.set(__self__, "include_private_incidents", include_private_incidents)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if owning_team_ids is not None:
            pulumi.set(__self__, "owning_team_ids", owning_team_ids)
        if private_incident_scope is not None:
            pulumi.set(__self__, "private_incident_scope", private_incident_scope)
        if shortform is not None:
            pulumi.set(__self__, "shortform", shortform)

    @_builtins.property
    @pulumi.getter(name="conditionGroups")
    def condition_groups(self) -> pulumi.Input[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]]:
        """
        Groups of prerequisite conditions. All conditions in at least one group must be satisfied
        """
        return pulumi.get(self, "condition_groups")

    @condition_groups.setter
    def condition_groups(self, value: pulumi.Input[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]]):
        pulumi.set(self, "condition_groups", value)

    @_builtins.property
    @pulumi.getter(name="continueOnStepError")
    def continue_on_step_error(self) -> pulumi.Input[_builtins.bool]:
        """
        Whether to continue executing the workflow if a step fails
        """
        return pulumi.get(self, "continue_on_step_error")

    @continue_on_step_error.setter
    def continue_on_step_error(self, value: pulumi.Input[_builtins.bool]):
        pulumi.set(self, "continue_on_step_error", value)

    @_builtins.property
    @pulumi.getter
    def expressions(self) -> pulumi.Input[Sequence[pulumi.Input['WorkflowExpressionArgs']]]:
        """
        The expressions to be prepared for use by steps and conditions
        """
        return pulumi.get(self, "expressions")

    @expressions.setter
    def expressions(self, value: pulumi.Input[Sequence[pulumi.Input['WorkflowExpressionArgs']]]):
        pulumi.set(self, "expressions", value)

    @_builtins.property
    @pulumi.getter(name="onceFors")
    def once_fors(self) -> pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]:
        """
        This workflow will run 'once for' a list of references
        """
        return pulumi.get(self, "once_fors")

    @once_fors.setter
    def once_fors(self, value: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]):
        pulumi.set(self, "once_fors", value)

    @_builtins.property
    @pulumi.getter(name="runsOnIncidentModes")
    def runs_on_incident_modes(self) -> pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]:
        """
        Which incident modes should this workflow run on? By default, workflows only run on standard incidents, but can also be configured to run on test and retrospective incidents. Possible values are: `standard`, `test`, `retrospective`.
        """
        return pulumi.get(self, "runs_on_incident_modes")

    @runs_on_incident_modes.setter
    def runs_on_incident_modes(self, value: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]):
        pulumi.set(self, "runs_on_incident_modes", value)

    @_builtins.property
    @pulumi.getter(name="runsOnIncidents")
    def runs_on_incidents(self) -> pulumi.Input[_builtins.str]:
        """
        Which incidents should the workflow be applied to? Possible values are: `newly_created`, `newly_created_and_active`.
        """
        return pulumi.get(self, "runs_on_incidents")

    @runs_on_incidents.setter
    def runs_on_incidents(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "runs_on_incidents", value)

    @_builtins.property
    @pulumi.getter
    def state(self) -> pulumi.Input[_builtins.str]:
        """
        What state this workflow is in. Possible values are: `active`, `disabled`, `draft`, `error`.
        """
        return pulumi.get(self, "state")

    @state.setter
    def state(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "state", value)

    @_builtins.property
    @pulumi.getter
    def steps(self) -> pulumi.Input[Sequence[pulumi.Input['WorkflowStepArgs']]]:
        """
        Steps that are executed as part of the workflow
        """
        return pulumi.get(self, "steps")

    @steps.setter
    def steps(self, value: pulumi.Input[Sequence[pulumi.Input['WorkflowStepArgs']]]):
        pulumi.set(self, "steps", value)

    @_builtins.property
    @pulumi.getter
    def trigger(self) -> pulumi.Input[_builtins.str]:
        """
        Unique name of the trigger
        """
        return pulumi.get(self, "trigger")

    @trigger.setter
    def trigger(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "trigger", value)

    @_builtins.property
    @pulumi.getter
    def delay(self) -> pulumi.Input[Optional['WorkflowDelayArgs']]:
        """
        Configuration controlling workflow delay behaviour
        """
        return pulumi.get(self, "delay")

    @delay.setter
    def delay(self, value: pulumi.Input[Optional['WorkflowDelayArgs']]):
        pulumi.set(self, "delay", value)

    @_builtins.property
    @pulumi.getter
    def folder(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Folder to display the workflow in
        """
        return pulumi.get(self, "folder")

    @folder.setter
    def folder(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "folder", value)

    @_builtins.property
    @pulumi.getter(name="formFields")
    def form_fields(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowFormFieldArgs']]]]:
        """
        User-configured form fields available in the workflow scope (manual triggers only)
        """
        return pulumi.get(self, "form_fields")

    @form_fields.setter
    def form_fields(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowFormFieldArgs']]]]):
        pulumi.set(self, "form_fields", value)

    @_builtins.property
    @pulumi.getter(name="includePrivateEscalations")
    def include_private_escalations(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Whether to include private escalations
        """
        return pulumi.get(self, "include_private_escalations")

    @include_private_escalations.setter
    def include_private_escalations(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "include_private_escalations", value)

    @_builtins.property
    @pulumi.getter(name="includePrivateIncidents")
    @_utilities.deprecated("""Use `private_incident_scope` instead. When both are set they must agree (include_private_incidents is true for the all and owning_teams scopes, false for none).""")
    def include_private_incidents(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        DEPRECATED: use `private_incident_scope` instead. `true` when the workflow runs on private incidents (a `private_incident_scope` of `all` or `owning_teams`), `false` when the scope is `none`.
        """
        return pulumi.get(self, "include_private_incidents")

    @include_private_incidents.setter
    def include_private_incidents(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "include_private_incidents", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Name provided by the user when creating the workflow
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="owningTeamIds")
    def owning_team_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        IDs of the teams that own this workflow
        """
        return pulumi.get(self, "owning_team_ids")

    @owning_team_ids.setter
    def owning_team_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "owning_team_ids", value)

    @_builtins.property
    @pulumi.getter(name="privateIncidentScope")
    def private_incident_scope(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Which private incidents this workflow acts on: every private incident (all), those an owning team can see (owning_teams), or none. Possible values are: `all`, `owning_teams`, `none`.
        """
        return pulumi.get(self, "private_incident_scope")

    @private_incident_scope.setter
    def private_incident_scope(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "private_incident_scope", value)

    @_builtins.property
    @pulumi.getter
    def shortform(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The shortform used to trigger this workflow (only applicable for manual triggers)
        """
        return pulumi.get(self, "shortform")

    @shortform.setter
    def shortform(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "shortform", value)


@pulumi.input_type
class _WorkflowState:
    def __init__(__self__, *,
                 condition_groups: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]]] = None,
                 continue_on_step_error: pulumi.Input[Optional[_builtins.bool]] = None,
                 delay: pulumi.Input[Optional['WorkflowDelayArgs']] = None,
                 expressions: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowExpressionArgs']]]] = None,
                 folder: pulumi.Input[Optional[_builtins.str]] = None,
                 form_fields: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowFormFieldArgs']]]] = None,
                 include_private_escalations: pulumi.Input[Optional[_builtins.bool]] = None,
                 include_private_incidents: pulumi.Input[Optional[_builtins.bool]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 once_fors: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 private_incident_scope: pulumi.Input[Optional[_builtins.str]] = None,
                 runs_on_incident_modes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 runs_on_incidents: pulumi.Input[Optional[_builtins.str]] = None,
                 shortform: pulumi.Input[Optional[_builtins.str]] = None,
                 state: pulumi.Input[Optional[_builtins.str]] = None,
                 steps: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowStepArgs']]]] = None,
                 trigger: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering Workflow resources.

        :param pulumi.Input[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]] condition_groups: Groups of prerequisite conditions. All conditions in at least one group must be satisfied
        :param pulumi.Input[_builtins.bool] continue_on_step_error: Whether to continue executing the workflow if a step fails
        :param pulumi.Input['WorkflowDelayArgs'] delay: Configuration controlling workflow delay behaviour
        :param pulumi.Input[Sequence[pulumi.Input['WorkflowExpressionArgs']]] expressions: The expressions to be prepared for use by steps and conditions
        :param pulumi.Input[_builtins.str] folder: Folder to display the workflow in
        :param pulumi.Input[Sequence[pulumi.Input['WorkflowFormFieldArgs']]] form_fields: User-configured form fields available in the workflow scope (manual triggers only)
        :param pulumi.Input[_builtins.bool] include_private_escalations: Whether to include private escalations
        :param pulumi.Input[_builtins.bool] include_private_incidents: DEPRECATED: use `private_incident_scope` instead. `true` when the workflow runs on private incidents (a `private_incident_scope` of `all` or `owning_teams`), `false` when the scope is `none`.
        :param pulumi.Input[_builtins.str] name: Name provided by the user when creating the workflow
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] once_fors: This workflow will run 'once for' a list of references
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this workflow
        :param pulumi.Input[_builtins.str] private_incident_scope: Which private incidents this workflow acts on: every private incident (all), those an owning team can see (owning_teams), or none. Possible values are: `all`, `owning_teams`, `none`.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] runs_on_incident_modes: Which incident modes should this workflow run on? By default, workflows only run on standard incidents, but can also be configured to run on test and retrospective incidents. Possible values are: `standard`, `test`, `retrospective`.
        :param pulumi.Input[_builtins.str] runs_on_incidents: Which incidents should the workflow be applied to? Possible values are: `newly_created`, `newly_created_and_active`.
        :param pulumi.Input[_builtins.str] shortform: The shortform used to trigger this workflow (only applicable for manual triggers)
        :param pulumi.Input[_builtins.str] state: What state this workflow is in. Possible values are: `active`, `disabled`, `draft`, `error`.
        :param pulumi.Input[Sequence[pulumi.Input['WorkflowStepArgs']]] steps: Steps that are executed as part of the workflow
        :param pulumi.Input[_builtins.str] trigger: Unique name of the trigger
        """
        if condition_groups is not None:
            pulumi.set(__self__, "condition_groups", condition_groups)
        if continue_on_step_error is not None:
            pulumi.set(__self__, "continue_on_step_error", continue_on_step_error)
        if delay is not None:
            pulumi.set(__self__, "delay", delay)
        if expressions is not None:
            pulumi.set(__self__, "expressions", expressions)
        if folder is not None:
            pulumi.set(__self__, "folder", folder)
        if form_fields is not None:
            pulumi.set(__self__, "form_fields", form_fields)
        if include_private_escalations is not None:
            pulumi.set(__self__, "include_private_escalations", include_private_escalations)
        if include_private_incidents is not None:
            warnings.warn("""Use `private_incident_scope` instead. When both are set they must agree (include_private_incidents is true for the all and owning_teams scopes, false for none).""", DeprecationWarning)
            pulumi.log.warn("""include_private_incidents is deprecated: Use `private_incident_scope` instead. When both are set they must agree (include_private_incidents is true for the all and owning_teams scopes, false for none).""")
        if include_private_incidents is not None:
            pulumi.set(__self__, "include_private_incidents", include_private_incidents)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if once_fors is not None:
            pulumi.set(__self__, "once_fors", once_fors)
        if owning_team_ids is not None:
            pulumi.set(__self__, "owning_team_ids", owning_team_ids)
        if private_incident_scope is not None:
            pulumi.set(__self__, "private_incident_scope", private_incident_scope)
        if runs_on_incident_modes is not None:
            pulumi.set(__self__, "runs_on_incident_modes", runs_on_incident_modes)
        if runs_on_incidents is not None:
            pulumi.set(__self__, "runs_on_incidents", runs_on_incidents)
        if shortform is not None:
            pulumi.set(__self__, "shortform", shortform)
        if state is not None:
            pulumi.set(__self__, "state", state)
        if steps is not None:
            pulumi.set(__self__, "steps", steps)
        if trigger is not None:
            pulumi.set(__self__, "trigger", trigger)

    @_builtins.property
    @pulumi.getter(name="conditionGroups")
    def condition_groups(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]]]:
        """
        Groups of prerequisite conditions. All conditions in at least one group must be satisfied
        """
        return pulumi.get(self, "condition_groups")

    @condition_groups.setter
    def condition_groups(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowConditionGroupArgs']]]]):
        pulumi.set(self, "condition_groups", value)

    @_builtins.property
    @pulumi.getter(name="continueOnStepError")
    def continue_on_step_error(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Whether to continue executing the workflow if a step fails
        """
        return pulumi.get(self, "continue_on_step_error")

    @continue_on_step_error.setter
    def continue_on_step_error(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "continue_on_step_error", value)

    @_builtins.property
    @pulumi.getter
    def delay(self) -> pulumi.Input[Optional['WorkflowDelayArgs']]:
        """
        Configuration controlling workflow delay behaviour
        """
        return pulumi.get(self, "delay")

    @delay.setter
    def delay(self, value: pulumi.Input[Optional['WorkflowDelayArgs']]):
        pulumi.set(self, "delay", value)

    @_builtins.property
    @pulumi.getter
    def expressions(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowExpressionArgs']]]]:
        """
        The expressions to be prepared for use by steps and conditions
        """
        return pulumi.get(self, "expressions")

    @expressions.setter
    def expressions(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowExpressionArgs']]]]):
        pulumi.set(self, "expressions", value)

    @_builtins.property
    @pulumi.getter
    def folder(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Folder to display the workflow in
        """
        return pulumi.get(self, "folder")

    @folder.setter
    def folder(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "folder", value)

    @_builtins.property
    @pulumi.getter(name="formFields")
    def form_fields(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowFormFieldArgs']]]]:
        """
        User-configured form fields available in the workflow scope (manual triggers only)
        """
        return pulumi.get(self, "form_fields")

    @form_fields.setter
    def form_fields(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowFormFieldArgs']]]]):
        pulumi.set(self, "form_fields", value)

    @_builtins.property
    @pulumi.getter(name="includePrivateEscalations")
    def include_private_escalations(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Whether to include private escalations
        """
        return pulumi.get(self, "include_private_escalations")

    @include_private_escalations.setter
    def include_private_escalations(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "include_private_escalations", value)

    @_builtins.property
    @pulumi.getter(name="includePrivateIncidents")
    @_utilities.deprecated("""Use `private_incident_scope` instead. When both are set they must agree (include_private_incidents is true for the all and owning_teams scopes, false for none).""")
    def include_private_incidents(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        DEPRECATED: use `private_incident_scope` instead. `true` when the workflow runs on private incidents (a `private_incident_scope` of `all` or `owning_teams`), `false` when the scope is `none`.
        """
        return pulumi.get(self, "include_private_incidents")

    @include_private_incidents.setter
    def include_private_incidents(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "include_private_incidents", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Name provided by the user when creating the workflow
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="onceFors")
    def once_fors(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        This workflow will run 'once for' a list of references
        """
        return pulumi.get(self, "once_fors")

    @once_fors.setter
    def once_fors(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "once_fors", value)

    @_builtins.property
    @pulumi.getter(name="owningTeamIds")
    def owning_team_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        IDs of the teams that own this workflow
        """
        return pulumi.get(self, "owning_team_ids")

    @owning_team_ids.setter
    def owning_team_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "owning_team_ids", value)

    @_builtins.property
    @pulumi.getter(name="privateIncidentScope")
    def private_incident_scope(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Which private incidents this workflow acts on: every private incident (all), those an owning team can see (owning_teams), or none. Possible values are: `all`, `owning_teams`, `none`.
        """
        return pulumi.get(self, "private_incident_scope")

    @private_incident_scope.setter
    def private_incident_scope(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "private_incident_scope", value)

    @_builtins.property
    @pulumi.getter(name="runsOnIncidentModes")
    def runs_on_incident_modes(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Which incident modes should this workflow run on? By default, workflows only run on standard incidents, but can also be configured to run on test and retrospective incidents. Possible values are: `standard`, `test`, `retrospective`.
        """
        return pulumi.get(self, "runs_on_incident_modes")

    @runs_on_incident_modes.setter
    def runs_on_incident_modes(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "runs_on_incident_modes", value)

    @_builtins.property
    @pulumi.getter(name="runsOnIncidents")
    def runs_on_incidents(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Which incidents should the workflow be applied to? Possible values are: `newly_created`, `newly_created_and_active`.
        """
        return pulumi.get(self, "runs_on_incidents")

    @runs_on_incidents.setter
    def runs_on_incidents(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "runs_on_incidents", value)

    @_builtins.property
    @pulumi.getter
    def shortform(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The shortform used to trigger this workflow (only applicable for manual triggers)
        """
        return pulumi.get(self, "shortform")

    @shortform.setter
    def shortform(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "shortform", value)

    @_builtins.property
    @pulumi.getter
    def state(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        What state this workflow is in. Possible values are: `active`, `disabled`, `draft`, `error`.
        """
        return pulumi.get(self, "state")

    @state.setter
    def state(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "state", value)

    @_builtins.property
    @pulumi.getter
    def steps(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowStepArgs']]]]:
        """
        Steps that are executed as part of the workflow
        """
        return pulumi.get(self, "steps")

    @steps.setter
    def steps(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['WorkflowStepArgs']]]]):
        pulumi.set(self, "steps", value)

    @_builtins.property
    @pulumi.getter
    def trigger(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Unique name of the trigger
        """
        return pulumi.get(self, "trigger")

    @trigger.setter
    def trigger(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "trigger", value)


@pulumi.type_token("incident:index/workflow:Workflow")
class Workflow(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 condition_groups: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowConditionGroupArgs', 'WorkflowConditionGroupArgsDict']]]]] = None,
                 continue_on_step_error: pulumi.Input[Optional[_builtins.bool]] = None,
                 delay: pulumi.Input[Optional[Union['WorkflowDelayArgs', 'WorkflowDelayArgsDict']]] = None,
                 expressions: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowExpressionArgs', 'WorkflowExpressionArgsDict']]]]] = None,
                 folder: pulumi.Input[Optional[_builtins.str]] = None,
                 form_fields: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowFormFieldArgs', 'WorkflowFormFieldArgsDict']]]]] = None,
                 include_private_escalations: pulumi.Input[Optional[_builtins.bool]] = None,
                 include_private_incidents: pulumi.Input[Optional[_builtins.bool]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 once_fors: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 private_incident_scope: pulumi.Input[Optional[_builtins.str]] = None,
                 runs_on_incident_modes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 runs_on_incidents: pulumi.Input[Optional[_builtins.str]] = None,
                 shortform: pulumi.Input[Optional[_builtins.str]] = None,
                 state: pulumi.Input[Optional[_builtins.str]] = None,
                 steps: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowStepArgs', 'WorkflowStepArgsDict']]]]] = None,
                 trigger: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        """
        This resource is used to manage Workflows.

        We'd generally recommend building workflows in our [web dashboard](https://app.incident.io/~/workflows), and using the 'Export' flow to generate your Terraform, as it's easier to see what you've configured. You can also make changes to an existing workflow and copy the resulting Terraform without persisting it.

        ## Example - Assign the incident lead when an escalation is acked

        ```python
        import pulumi
        import pulumi_incident as incident

        # Teams are catalog entries, so an owning team is resolved through the catalog
        # rather than by pasting in its ID. The identifier is matched against entry
        # names, external IDs and aliases, so the team's name is enough.
        team = incident.get_catalog_type(name="Team")
        platform = incident.get_catalog_entry(catalog_type_id=team.id,
            identifier="Platform")
        # Look the incident lead role up by name, rather than pinning its ID into the
        # workflow below. The ID differs between workspaces, so a name keeps the config
        # portable.
        incident_lead = incident.get_incident_role(name="Incident Lead")
        # This is a workflow that automatically assigns the incident lead role to the user who acked an escalation.
        autoassign_incident_lead = incident.Workflow("autoassign_incident_lead",
            name="Auto-assign incident leader",
            trigger="escalation.acked",
            expressions=[],
            condition_groups=[{
                "conditions": [{
                    "subject": "user",
                    "operation": "is_set",
                    "param_bindings": [],
                }],
            }],
            steps=[{
                "id": "01HY0QG9WT62CEYJN8JD74MJNR",
                "name": "incident.assign_role",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "value": {
                            "literal": incident_lead.id,
                        },
                    },
                    {
                        "value": {
                            "reference": "user",
                        },
                    },
                ],
            }],
            once_fors=["incident"],
            owning_team_ids=[platform.id],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Page execs from a manual workflow, using form fields

        ```python
        import pulumi
        import pulumi_incident as incident

        # Rich text fields hold a document, not a string, so build one from markdown
        # rather than writing the JSON by hand. Variables written as {{ }} become
        # references into the workflow scope when the message is sent - a plain string
        # literal is stored as literal text and won't interpolate. The feature set must
        # match the field it's going into: a Slack message is "mrkdwn".
        exec_page = incident.get_rich_text(markdown=\"\"\"**Exec page for {{ incident.name }}**

        {{ form.reason }}

        Join the incident channel: {{ incident.slack_channel }}\"\"\",
            feature_set="mrkdwn")
        # This is a manually triggered workflow that collects information from the user
        # via form fields when they run it. Each field's value is available in the
        # workflow scope as `form.<key>`, for both conditions and steps to bind
        # against - as the message below does with `form.reason`. That's the prefix the
        # dashboard shows as "Form → <title>".
        #
        # The order of the list is the order the fields appear in the form. Either
        # form_fields = [] or omitting the attribute clears any existing fields.
        #
        # A field's `id` is computed, never written: the provider correlates fields
        # across applies by `key`, so retitling a field keeps its identity (and any
        # in-flight runs), while changing its key replaces it. That's also why the key,
        # not the title, is what the scope reference uses.
        page_execs = incident.Workflow("page_execs",
            name="Page execs",
            trigger="manual",
            expressions=[],
            condition_groups=[],
            steps=[{
                "id": "01KG1326KD7DT1Z785SYVXXP98",
                "name": "slack.send_message",
                "param_bindings": [
                    {
                        "array_value": [{
                            "reference": "form.execs",
                        }],
                    },
                    {
                        "value": {
                            "literal": exec_page.json,
                        },
                    },
                    {},
                ],
            }],
            form_fields=[
                {
                    "key": "reason",
                    "title": "Reason for paging",
                    "type": "String",
                    "description": "Why are we paging the execs?",
                    "array": False,
                    "required": True,
                },
                {
                    "key": "execs",
                    "title": "Who should we page?",
                    "type": "User",
                    "description": "The execs to message. Leave the on-call exec out, they're paged already.",
                    "array": True,
                    "required": True,
                },
            ],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Create the postmortem follow-up when an incident closes

        ```python
        import pulumi
        import pulumi_incident as incident

        # Statuses are managed by incident.io, so look one up by name rather than pasting
        # the ULID an export gives you: the ID differs between workspaces, the name is
        # what you see in the dashboard.
        closed = incident.get_status(name="Closed")
        # This workflow runs when someone moves an incident into Closed, and creates the
        # postmortem follow-up assigned to whoever closed it.
        #
        # The trigger's scope carries three references to bind against: "incident",
        # "previous-status" (the status the incident moved out of) and
        # "user-who-changed-the-status".
        #
        # Note that building this in the dashboard pre-fills a "status category is
        # Active" condition, which would stop this workflow ever running, Closed not
        # being an active category. Terraform writes conditions explicitly so there's
        # nothing to remove here, but check for that condition if you export a
        # status-driven workflow from the dashboard.
        postmortem_when_closed = incident.Workflow("postmortem_when_closed",
            name="Create the postmortem follow-up when an incident closes",
            trigger="incident.status-changed",
            expressions=[],
            condition_groups=[{
                "conditions": [{
                    "subject": "incident.status",
                    "operation": "one_of",
                    "param_bindings": [{
                        "array_value": [{
                            "literal": closed.id,
                        }],
                    }],
                }],
            }],
            steps=[{
                "id": "01KT8B9QQH67GF9S6SW4A7DNEX",
                "name": "incident.create_follow_ups",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "array_value": [{
                            "literal": "Write the postmortem",
                        }],
                    },
                    {
                        "value": {
                            "reference": "user-who-changed-the-status",
                        },
                    },
                    {},
                    {},
                    {},
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Invite the security responders to an incident

        ```python
        import pulumi
        import pulumi_incident as incident

        # Users have a data source, so a config doesn't need the ULIDs an export writes
        # for them: look people up by the email address they use with incident.io.
        security_lead = incident.get_user(email="security-lead@example.com")
        # Slack user groups reach the engine through the catalog, as entries of the
        # managed "Slack User Group" type. The identifier matches an entry's name,
        # external ID or aliases, so the group's handle is enough.
        slack_user_group = incident.get_catalog_type(name="Slack User Group")
        security_responders = incident.get_catalog_entry(catalog_type_id=slack_user_group.id,
            identifier="security-responders")
        # This workflow pulls the security responders into an incident's channel as soon
        # as the incident becomes active.
        #
        # Four steps sound alike but do different things, so pick deliberately:
        #
        #   slack.invite_user                   invites users and Slack user groups to
        #                                       the incident's channel (this one)
        #   incident.add_member                 grants access to a private incident, and
        #                                       does nothing on a public one
        #   incident.assign_role                gives someone an incident role, such as
        #                                       Incident Lead
        #   incident.subscribe_user_to_incident subscribes someone to updates without
        #                                       making them a participant
        #
        # Narrow this with conditions on whatever marks the incidents you care about -
        # a custom field, the incident's type, or its severity.
        invite_security_responders = incident.Workflow("invite_security_responders",
            name="Invite security responders",
            trigger="incident.updated",
            expressions=[],
            condition_groups=[{
                "conditions": [{
                    "subject": "incident.status.category",
                    "operation": "one_of",
                    "param_bindings": [{
                        "array_value": [{
                            "literal": "active",
                        }],
                    }],
                }],
            }],
            steps=[{
                "id": "01KFD47KKJS93YXR3W53MQSFVC",
                "name": "slack.invite_user",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "array_value": [{
                            "literal": security_lead.id,
                        }],
                    },
                    {
                        "array_value": [{
                            "literal": security_responders.id,
                        }],
                    },
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Share incident updates with leadership

        ```python
        import pulumi
        import pulumi_incident as incident

        # A message body is a rich text document, so build it from markdown here rather
        # than pasting the JSON an export produces. Anything the feature set can't hold
        # is dropped and listed in dropped_content, which you can output to see what
        # went missing.
        leadership_update = incident.get_rich_text(markdown=\"\"\"**{{ incident.name }}** — update from {{ update.author }}

        {{ update.message }}

        Now at {{ update.to_status }}, severity {{ update.to_severity }}. Follow along in {{ incident.slack_channel }}.
        \"\"\",
            feature_set="mrkdwn")
        # This workflow mirrors every incident update into a leadership channel.
        #
        # Three Slack steps send messages, and they take different first parameters:
        #
        #   slack.post_message     posts to a channel, which is where this one goes
        #   slack.send_message     sends a DM to users
        #   slack.reply_in_thread  replies to a message already in the scope
        #
        # On Microsoft Teams the equivalent is ms_teams.post_message, whose channel
        # parameter takes a MicrosoftTeamsChatChannel and defaults to
        # `incident.ms_teams_channel`. Only the steps for your workspace's primary
        # comms platform are available, so a config written for one won't apply to the
        # other.
        share_updates_with_leadership = incident.Workflow("share_updates_with_leadership",
            name="Share incident updates with leadership",
            trigger="incident.update_shared",
            expressions=[],
            condition_groups=[],
            steps=[{
                "id": "01K5TSNDBF7KBZY9AFDKYXJM3M",
                "name": "slack.post_message",
                "param_bindings": [
                    {
                        "value": {
                            "literal": "C02A1FSNEKC",
                        },
                    },
                    {
                        "value": {
                            "literal": leadership_update.json,
                        },
                    },
                    {},
                    {
                        "value": {
                            "literal": "Europe/London",
                        },
                    },
                ],
            }],
            once_fors=[],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Subscribe CSMs to customer-facing incidents

        ```python
        import pulumi
        import pulumi_incident as incident

        # Custom fields and their options both have data sources, so a condition on one
        # needs no ULIDs: look the field up by name, and each option by its value.
        affected_customers = incident.get_custom_field(name="Affected customers")
        all_customers = incident.get_custom_field_option(custom_field_id=affected_customers.id,
            value="All customers")
        csm_lead = incident.get_user(email="csm-lead@example.com")
        csm_deputy = incident.get_user(email="csm-deputy@example.com")
        # This is the workflow we run ourselves: subscribe the CSM team to every
        # Critical incident, and to Major incidents that affect all customers.
        #
        # It's the clearest example of how condition groups combine. Conditions within
        # a group are ANDed, and the groups are ORed with each other, so this reads as:
        #
        #   severity is Critical
        #     OR (severity is Major AND affected customers is All customers)
        #
        # Two conditions that must both hold go in one group. Two alternatives, as
        # here, need a group each - putting all three conditions in a single group
        # would subscribe nobody, since no incident is both Critical and Major.
        subscribe_csms = incident.Workflow("subscribe_csms",
            name="Subscribe CSMs to customer-facing incidents",
            trigger="incident.updated",
            expressions=[],
            condition_groups=[
                {
                    "conditions": [{
                        "subject": "incident.severity",
                        "operation": "one_of",
                        "param_bindings": [{
                            "array_value": [{
                                "literal": "01K675152G91VK4S8YYT7NKPAE",
                            }],
                        }],
                    }],
                },
                {
                    "conditions": [
                        {
                            "subject": "incident.severity",
                            "operation": "one_of",
                            "param_bindings": [{
                                "array_value": [{
                                    "literal": "01KCZFEB7DPVCQQM262JTKN5RH",
                                }],
                            }],
                        },
                        {
                            "subject": f"incident.custom_field[\\"{affected_customers.id}\\"]",
                            "operation": "one_of",
                            "param_bindings": [{
                                "array_value": [{
                                    "literal": all_customers.id,
                                }],
                            }],
                        },
                    ],
                },
            ],
            steps=[{
                "id": "01KDEMWQCS30P3GHAVNG9VGKWS",
                "name": "incident.subscribe_user_to_incident",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "array_value": [
                            {
                                "literal": csm_lead.id,
                            },
                            {
                                "literal": csm_deputy.id,
                            },
                        ],
                    },
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowConditionGroupArgs', 'WorkflowConditionGroupArgsDict']]]] condition_groups: Groups of prerequisite conditions. All conditions in at least one group must be satisfied
        :param pulumi.Input[_builtins.bool] continue_on_step_error: Whether to continue executing the workflow if a step fails
        :param pulumi.Input[Union['WorkflowDelayArgs', 'WorkflowDelayArgsDict']] delay: Configuration controlling workflow delay behaviour
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowExpressionArgs', 'WorkflowExpressionArgsDict']]]] expressions: The expressions to be prepared for use by steps and conditions
        :param pulumi.Input[_builtins.str] folder: Folder to display the workflow in
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowFormFieldArgs', 'WorkflowFormFieldArgsDict']]]] form_fields: User-configured form fields available in the workflow scope (manual triggers only)
        :param pulumi.Input[_builtins.bool] include_private_escalations: Whether to include private escalations
        :param pulumi.Input[_builtins.bool] include_private_incidents: DEPRECATED: use `private_incident_scope` instead. `true` when the workflow runs on private incidents (a `private_incident_scope` of `all` or `owning_teams`), `false` when the scope is `none`.
        :param pulumi.Input[_builtins.str] name: Name provided by the user when creating the workflow
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] once_fors: This workflow will run 'once for' a list of references
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this workflow
        :param pulumi.Input[_builtins.str] private_incident_scope: Which private incidents this workflow acts on: every private incident (all), those an owning team can see (owning_teams), or none. Possible values are: `all`, `owning_teams`, `none`.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] runs_on_incident_modes: Which incident modes should this workflow run on? By default, workflows only run on standard incidents, but can also be configured to run on test and retrospective incidents. Possible values are: `standard`, `test`, `retrospective`.
        :param pulumi.Input[_builtins.str] runs_on_incidents: Which incidents should the workflow be applied to? Possible values are: `newly_created`, `newly_created_and_active`.
        :param pulumi.Input[_builtins.str] shortform: The shortform used to trigger this workflow (only applicable for manual triggers)
        :param pulumi.Input[_builtins.str] state: What state this workflow is in. Possible values are: `active`, `disabled`, `draft`, `error`.
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowStepArgs', 'WorkflowStepArgsDict']]]] steps: Steps that are executed as part of the workflow
        :param pulumi.Input[_builtins.str] trigger: Unique name of the trigger
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: WorkflowArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        This resource is used to manage Workflows.

        We'd generally recommend building workflows in our [web dashboard](https://app.incident.io/~/workflows), and using the 'Export' flow to generate your Terraform, as it's easier to see what you've configured. You can also make changes to an existing workflow and copy the resulting Terraform without persisting it.

        ## Example - Assign the incident lead when an escalation is acked

        ```python
        import pulumi
        import pulumi_incident as incident

        # Teams are catalog entries, so an owning team is resolved through the catalog
        # rather than by pasting in its ID. The identifier is matched against entry
        # names, external IDs and aliases, so the team's name is enough.
        team = incident.get_catalog_type(name="Team")
        platform = incident.get_catalog_entry(catalog_type_id=team.id,
            identifier="Platform")
        # Look the incident lead role up by name, rather than pinning its ID into the
        # workflow below. The ID differs between workspaces, so a name keeps the config
        # portable.
        incident_lead = incident.get_incident_role(name="Incident Lead")
        # This is a workflow that automatically assigns the incident lead role to the user who acked an escalation.
        autoassign_incident_lead = incident.Workflow("autoassign_incident_lead",
            name="Auto-assign incident leader",
            trigger="escalation.acked",
            expressions=[],
            condition_groups=[{
                "conditions": [{
                    "subject": "user",
                    "operation": "is_set",
                    "param_bindings": [],
                }],
            }],
            steps=[{
                "id": "01HY0QG9WT62CEYJN8JD74MJNR",
                "name": "incident.assign_role",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "value": {
                            "literal": incident_lead.id,
                        },
                    },
                    {
                        "value": {
                            "reference": "user",
                        },
                    },
                ],
            }],
            once_fors=["incident"],
            owning_team_ids=[platform.id],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Page execs from a manual workflow, using form fields

        ```python
        import pulumi
        import pulumi_incident as incident

        # Rich text fields hold a document, not a string, so build one from markdown
        # rather than writing the JSON by hand. Variables written as {{ }} become
        # references into the workflow scope when the message is sent - a plain string
        # literal is stored as literal text and won't interpolate. The feature set must
        # match the field it's going into: a Slack message is "mrkdwn".
        exec_page = incident.get_rich_text(markdown=\"\"\"**Exec page for {{ incident.name }}**

        {{ form.reason }}

        Join the incident channel: {{ incident.slack_channel }}\"\"\",
            feature_set="mrkdwn")
        # This is a manually triggered workflow that collects information from the user
        # via form fields when they run it. Each field's value is available in the
        # workflow scope as `form.<key>`, for both conditions and steps to bind
        # against - as the message below does with `form.reason`. That's the prefix the
        # dashboard shows as "Form → <title>".
        #
        # The order of the list is the order the fields appear in the form. Either
        # form_fields = [] or omitting the attribute clears any existing fields.
        #
        # A field's `id` is computed, never written: the provider correlates fields
        # across applies by `key`, so retitling a field keeps its identity (and any
        # in-flight runs), while changing its key replaces it. That's also why the key,
        # not the title, is what the scope reference uses.
        page_execs = incident.Workflow("page_execs",
            name="Page execs",
            trigger="manual",
            expressions=[],
            condition_groups=[],
            steps=[{
                "id": "01KG1326KD7DT1Z785SYVXXP98",
                "name": "slack.send_message",
                "param_bindings": [
                    {
                        "array_value": [{
                            "reference": "form.execs",
                        }],
                    },
                    {
                        "value": {
                            "literal": exec_page.json,
                        },
                    },
                    {},
                ],
            }],
            form_fields=[
                {
                    "key": "reason",
                    "title": "Reason for paging",
                    "type": "String",
                    "description": "Why are we paging the execs?",
                    "array": False,
                    "required": True,
                },
                {
                    "key": "execs",
                    "title": "Who should we page?",
                    "type": "User",
                    "description": "The execs to message. Leave the on-call exec out, they're paged already.",
                    "array": True,
                    "required": True,
                },
            ],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Create the postmortem follow-up when an incident closes

        ```python
        import pulumi
        import pulumi_incident as incident

        # Statuses are managed by incident.io, so look one up by name rather than pasting
        # the ULID an export gives you: the ID differs between workspaces, the name is
        # what you see in the dashboard.
        closed = incident.get_status(name="Closed")
        # This workflow runs when someone moves an incident into Closed, and creates the
        # postmortem follow-up assigned to whoever closed it.
        #
        # The trigger's scope carries three references to bind against: "incident",
        # "previous-status" (the status the incident moved out of) and
        # "user-who-changed-the-status".
        #
        # Note that building this in the dashboard pre-fills a "status category is
        # Active" condition, which would stop this workflow ever running, Closed not
        # being an active category. Terraform writes conditions explicitly so there's
        # nothing to remove here, but check for that condition if you export a
        # status-driven workflow from the dashboard.
        postmortem_when_closed = incident.Workflow("postmortem_when_closed",
            name="Create the postmortem follow-up when an incident closes",
            trigger="incident.status-changed",
            expressions=[],
            condition_groups=[{
                "conditions": [{
                    "subject": "incident.status",
                    "operation": "one_of",
                    "param_bindings": [{
                        "array_value": [{
                            "literal": closed.id,
                        }],
                    }],
                }],
            }],
            steps=[{
                "id": "01KT8B9QQH67GF9S6SW4A7DNEX",
                "name": "incident.create_follow_ups",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "array_value": [{
                            "literal": "Write the postmortem",
                        }],
                    },
                    {
                        "value": {
                            "reference": "user-who-changed-the-status",
                        },
                    },
                    {},
                    {},
                    {},
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Invite the security responders to an incident

        ```python
        import pulumi
        import pulumi_incident as incident

        # Users have a data source, so a config doesn't need the ULIDs an export writes
        # for them: look people up by the email address they use with incident.io.
        security_lead = incident.get_user(email="security-lead@example.com")
        # Slack user groups reach the engine through the catalog, as entries of the
        # managed "Slack User Group" type. The identifier matches an entry's name,
        # external ID or aliases, so the group's handle is enough.
        slack_user_group = incident.get_catalog_type(name="Slack User Group")
        security_responders = incident.get_catalog_entry(catalog_type_id=slack_user_group.id,
            identifier="security-responders")
        # This workflow pulls the security responders into an incident's channel as soon
        # as the incident becomes active.
        #
        # Four steps sound alike but do different things, so pick deliberately:
        #
        #   slack.invite_user                   invites users and Slack user groups to
        #                                       the incident's channel (this one)
        #   incident.add_member                 grants access to a private incident, and
        #                                       does nothing on a public one
        #   incident.assign_role                gives someone an incident role, such as
        #                                       Incident Lead
        #   incident.subscribe_user_to_incident subscribes someone to updates without
        #                                       making them a participant
        #
        # Narrow this with conditions on whatever marks the incidents you care about -
        # a custom field, the incident's type, or its severity.
        invite_security_responders = incident.Workflow("invite_security_responders",
            name="Invite security responders",
            trigger="incident.updated",
            expressions=[],
            condition_groups=[{
                "conditions": [{
                    "subject": "incident.status.category",
                    "operation": "one_of",
                    "param_bindings": [{
                        "array_value": [{
                            "literal": "active",
                        }],
                    }],
                }],
            }],
            steps=[{
                "id": "01KFD47KKJS93YXR3W53MQSFVC",
                "name": "slack.invite_user",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "array_value": [{
                            "literal": security_lead.id,
                        }],
                    },
                    {
                        "array_value": [{
                            "literal": security_responders.id,
                        }],
                    },
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Share incident updates with leadership

        ```python
        import pulumi
        import pulumi_incident as incident

        # A message body is a rich text document, so build it from markdown here rather
        # than pasting the JSON an export produces. Anything the feature set can't hold
        # is dropped and listed in dropped_content, which you can output to see what
        # went missing.
        leadership_update = incident.get_rich_text(markdown=\"\"\"**{{ incident.name }}** — update from {{ update.author }}

        {{ update.message }}

        Now at {{ update.to_status }}, severity {{ update.to_severity }}. Follow along in {{ incident.slack_channel }}.
        \"\"\",
            feature_set="mrkdwn")
        # This workflow mirrors every incident update into a leadership channel.
        #
        # Three Slack steps send messages, and they take different first parameters:
        #
        #   slack.post_message     posts to a channel, which is where this one goes
        #   slack.send_message     sends a DM to users
        #   slack.reply_in_thread  replies to a message already in the scope
        #
        # On Microsoft Teams the equivalent is ms_teams.post_message, whose channel
        # parameter takes a MicrosoftTeamsChatChannel and defaults to
        # `incident.ms_teams_channel`. Only the steps for your workspace's primary
        # comms platform are available, so a config written for one won't apply to the
        # other.
        share_updates_with_leadership = incident.Workflow("share_updates_with_leadership",
            name="Share incident updates with leadership",
            trigger="incident.update_shared",
            expressions=[],
            condition_groups=[],
            steps=[{
                "id": "01K5TSNDBF7KBZY9AFDKYXJM3M",
                "name": "slack.post_message",
                "param_bindings": [
                    {
                        "value": {
                            "literal": "C02A1FSNEKC",
                        },
                    },
                    {
                        "value": {
                            "literal": leadership_update.json,
                        },
                    },
                    {},
                    {
                        "value": {
                            "literal": "Europe/London",
                        },
                    },
                ],
            }],
            once_fors=[],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Example - Subscribe CSMs to customer-facing incidents

        ```python
        import pulumi
        import pulumi_incident as incident

        # Custom fields and their options both have data sources, so a condition on one
        # needs no ULIDs: look the field up by name, and each option by its value.
        affected_customers = incident.get_custom_field(name="Affected customers")
        all_customers = incident.get_custom_field_option(custom_field_id=affected_customers.id,
            value="All customers")
        csm_lead = incident.get_user(email="csm-lead@example.com")
        csm_deputy = incident.get_user(email="csm-deputy@example.com")
        # This is the workflow we run ourselves: subscribe the CSM team to every
        # Critical incident, and to Major incidents that affect all customers.
        #
        # It's the clearest example of how condition groups combine. Conditions within
        # a group are ANDed, and the groups are ORed with each other, so this reads as:
        #
        #   severity is Critical
        #     OR (severity is Major AND affected customers is All customers)
        #
        # Two conditions that must both hold go in one group. Two alternatives, as
        # here, need a group each - putting all three conditions in a single group
        # would subscribe nobody, since no incident is both Critical and Major.
        subscribe_csms = incident.Workflow("subscribe_csms",
            name="Subscribe CSMs to customer-facing incidents",
            trigger="incident.updated",
            expressions=[],
            condition_groups=[
                {
                    "conditions": [{
                        "subject": "incident.severity",
                        "operation": "one_of",
                        "param_bindings": [{
                            "array_value": [{
                                "literal": "01K675152G91VK4S8YYT7NKPAE",
                            }],
                        }],
                    }],
                },
                {
                    "conditions": [
                        {
                            "subject": "incident.severity",
                            "operation": "one_of",
                            "param_bindings": [{
                                "array_value": [{
                                    "literal": "01KCZFEB7DPVCQQM262JTKN5RH",
                                }],
                            }],
                        },
                        {
                            "subject": f"incident.custom_field[\\"{affected_customers.id}\\"]",
                            "operation": "one_of",
                            "param_bindings": [{
                                "array_value": [{
                                    "literal": all_customers.id,
                                }],
                            }],
                        },
                    ],
                },
            ],
            steps=[{
                "id": "01KDEMWQCS30P3GHAVNG9VGKWS",
                "name": "incident.subscribe_user_to_incident",
                "param_bindings": [
                    {
                        "value": {
                            "reference": "incident",
                        },
                    },
                    {
                        "array_value": [
                            {
                                "literal": csm_lead.id,
                            },
                            {
                                "literal": csm_deputy.id,
                            },
                        ],
                    },
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```


        :param str resource_name: The name of the resource.
        :param WorkflowArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(WorkflowArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 condition_groups: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowConditionGroupArgs', 'WorkflowConditionGroupArgsDict']]]]] = None,
                 continue_on_step_error: pulumi.Input[Optional[_builtins.bool]] = None,
                 delay: pulumi.Input[Optional[Union['WorkflowDelayArgs', 'WorkflowDelayArgsDict']]] = None,
                 expressions: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowExpressionArgs', 'WorkflowExpressionArgsDict']]]]] = None,
                 folder: pulumi.Input[Optional[_builtins.str]] = None,
                 form_fields: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowFormFieldArgs', 'WorkflowFormFieldArgsDict']]]]] = None,
                 include_private_escalations: pulumi.Input[Optional[_builtins.bool]] = None,
                 include_private_incidents: pulumi.Input[Optional[_builtins.bool]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 once_fors: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 private_incident_scope: pulumi.Input[Optional[_builtins.str]] = None,
                 runs_on_incident_modes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 runs_on_incidents: pulumi.Input[Optional[_builtins.str]] = None,
                 shortform: pulumi.Input[Optional[_builtins.str]] = None,
                 state: pulumi.Input[Optional[_builtins.str]] = None,
                 steps: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowStepArgs', 'WorkflowStepArgsDict']]]]] = None,
                 trigger: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = WorkflowArgs.__new__(WorkflowArgs)

            if condition_groups is None and not opts.urn:
                raise TypeError("Missing required property 'condition_groups'")
            __props__.__dict__["condition_groups"] = condition_groups
            if continue_on_step_error is None and not opts.urn:
                raise TypeError("Missing required property 'continue_on_step_error'")
            __props__.__dict__["continue_on_step_error"] = continue_on_step_error
            __props__.__dict__["delay"] = delay
            if expressions is None and not opts.urn:
                raise TypeError("Missing required property 'expressions'")
            __props__.__dict__["expressions"] = expressions
            __props__.__dict__["folder"] = folder
            __props__.__dict__["form_fields"] = form_fields
            __props__.__dict__["include_private_escalations"] = include_private_escalations
            __props__.__dict__["include_private_incidents"] = include_private_incidents
            __props__.__dict__["name"] = name
            if once_fors is None and not opts.urn:
                raise TypeError("Missing required property 'once_fors'")
            __props__.__dict__["once_fors"] = once_fors
            __props__.__dict__["owning_team_ids"] = owning_team_ids
            __props__.__dict__["private_incident_scope"] = private_incident_scope
            if runs_on_incident_modes is None and not opts.urn:
                raise TypeError("Missing required property 'runs_on_incident_modes'")
            __props__.__dict__["runs_on_incident_modes"] = runs_on_incident_modes
            if runs_on_incidents is None and not opts.urn:
                raise TypeError("Missing required property 'runs_on_incidents'")
            __props__.__dict__["runs_on_incidents"] = runs_on_incidents
            __props__.__dict__["shortform"] = shortform
            if state is None and not opts.urn:
                raise TypeError("Missing required property 'state'")
            __props__.__dict__["state"] = state
            if steps is None and not opts.urn:
                raise TypeError("Missing required property 'steps'")
            __props__.__dict__["steps"] = steps
            if trigger is None and not opts.urn:
                raise TypeError("Missing required property 'trigger'")
            __props__.__dict__["trigger"] = trigger
        super(Workflow, __self__).__init__(
            'incident:index/workflow:Workflow',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            condition_groups: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowConditionGroupArgs', 'WorkflowConditionGroupArgsDict']]]]] = None,
            continue_on_step_error: pulumi.Input[Optional[_builtins.bool]] = None,
            delay: pulumi.Input[Optional[Union['WorkflowDelayArgs', 'WorkflowDelayArgsDict']]] = None,
            expressions: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowExpressionArgs', 'WorkflowExpressionArgsDict']]]]] = None,
            folder: pulumi.Input[Optional[_builtins.str]] = None,
            form_fields: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowFormFieldArgs', 'WorkflowFormFieldArgsDict']]]]] = None,
            include_private_escalations: pulumi.Input[Optional[_builtins.bool]] = None,
            include_private_incidents: pulumi.Input[Optional[_builtins.bool]] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            once_fors: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            private_incident_scope: pulumi.Input[Optional[_builtins.str]] = None,
            runs_on_incident_modes: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            runs_on_incidents: pulumi.Input[Optional[_builtins.str]] = None,
            shortform: pulumi.Input[Optional[_builtins.str]] = None,
            state: pulumi.Input[Optional[_builtins.str]] = None,
            steps: pulumi.Input[Optional[Sequence[pulumi.Input[Union['WorkflowStepArgs', 'WorkflowStepArgsDict']]]]] = None,
            trigger: pulumi.Input[Optional[_builtins.str]] = None) -> 'Workflow':
        """
        Get an existing Workflow resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowConditionGroupArgs', 'WorkflowConditionGroupArgsDict']]]] condition_groups: Groups of prerequisite conditions. All conditions in at least one group must be satisfied
        :param pulumi.Input[_builtins.bool] continue_on_step_error: Whether to continue executing the workflow if a step fails
        :param pulumi.Input[Union['WorkflowDelayArgs', 'WorkflowDelayArgsDict']] delay: Configuration controlling workflow delay behaviour
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowExpressionArgs', 'WorkflowExpressionArgsDict']]]] expressions: The expressions to be prepared for use by steps and conditions
        :param pulumi.Input[_builtins.str] folder: Folder to display the workflow in
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowFormFieldArgs', 'WorkflowFormFieldArgsDict']]]] form_fields: User-configured form fields available in the workflow scope (manual triggers only)
        :param pulumi.Input[_builtins.bool] include_private_escalations: Whether to include private escalations
        :param pulumi.Input[_builtins.bool] include_private_incidents: DEPRECATED: use `private_incident_scope` instead. `true` when the workflow runs on private incidents (a `private_incident_scope` of `all` or `owning_teams`), `false` when the scope is `none`.
        :param pulumi.Input[_builtins.str] name: Name provided by the user when creating the workflow
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] once_fors: This workflow will run 'once for' a list of references
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this workflow
        :param pulumi.Input[_builtins.str] private_incident_scope: Which private incidents this workflow acts on: every private incident (all), those an owning team can see (owning_teams), or none. Possible values are: `all`, `owning_teams`, `none`.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] runs_on_incident_modes: Which incident modes should this workflow run on? By default, workflows only run on standard incidents, but can also be configured to run on test and retrospective incidents. Possible values are: `standard`, `test`, `retrospective`.
        :param pulumi.Input[_builtins.str] runs_on_incidents: Which incidents should the workflow be applied to? Possible values are: `newly_created`, `newly_created_and_active`.
        :param pulumi.Input[_builtins.str] shortform: The shortform used to trigger this workflow (only applicable for manual triggers)
        :param pulumi.Input[_builtins.str] state: What state this workflow is in. Possible values are: `active`, `disabled`, `draft`, `error`.
        :param pulumi.Input[Sequence[pulumi.Input[Union['WorkflowStepArgs', 'WorkflowStepArgsDict']]]] steps: Steps that are executed as part of the workflow
        :param pulumi.Input[_builtins.str] trigger: Unique name of the trigger
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _WorkflowState.__new__(_WorkflowState)

        __props__.__dict__["condition_groups"] = condition_groups
        __props__.__dict__["continue_on_step_error"] = continue_on_step_error
        __props__.__dict__["delay"] = delay
        __props__.__dict__["expressions"] = expressions
        __props__.__dict__["folder"] = folder
        __props__.__dict__["form_fields"] = form_fields
        __props__.__dict__["include_private_escalations"] = include_private_escalations
        __props__.__dict__["include_private_incidents"] = include_private_incidents
        __props__.__dict__["name"] = name
        __props__.__dict__["once_fors"] = once_fors
        __props__.__dict__["owning_team_ids"] = owning_team_ids
        __props__.__dict__["private_incident_scope"] = private_incident_scope
        __props__.__dict__["runs_on_incident_modes"] = runs_on_incident_modes
        __props__.__dict__["runs_on_incidents"] = runs_on_incidents
        __props__.__dict__["shortform"] = shortform
        __props__.__dict__["state"] = state
        __props__.__dict__["steps"] = steps
        __props__.__dict__["trigger"] = trigger
        return Workflow(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="conditionGroups")
    def condition_groups(self) -> pulumi.Output[Sequence['outputs.WorkflowConditionGroup']]:
        """
        Groups of prerequisite conditions. All conditions in at least one group must be satisfied
        """
        return pulumi.get(self, "condition_groups")

    @_builtins.property
    @pulumi.getter(name="continueOnStepError")
    def continue_on_step_error(self) -> pulumi.Output[_builtins.bool]:
        """
        Whether to continue executing the workflow if a step fails
        """
        return pulumi.get(self, "continue_on_step_error")

    @_builtins.property
    @pulumi.getter
    def delay(self) -> pulumi.Output[Optional['outputs.WorkflowDelay']]:
        """
        Configuration controlling workflow delay behaviour
        """
        return pulumi.get(self, "delay")

    @_builtins.property
    @pulumi.getter
    def expressions(self) -> pulumi.Output[Sequence['outputs.WorkflowExpression']]:
        """
        The expressions to be prepared for use by steps and conditions
        """
        return pulumi.get(self, "expressions")

    @_builtins.property
    @pulumi.getter
    def folder(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        Folder to display the workflow in
        """
        return pulumi.get(self, "folder")

    @_builtins.property
    @pulumi.getter(name="formFields")
    def form_fields(self) -> pulumi.Output[Optional[Sequence['outputs.WorkflowFormField']]]:
        """
        User-configured form fields available in the workflow scope (manual triggers only)
        """
        return pulumi.get(self, "form_fields")

    @_builtins.property
    @pulumi.getter(name="includePrivateEscalations")
    def include_private_escalations(self) -> pulumi.Output[_builtins.bool]:
        """
        Whether to include private escalations
        """
        return pulumi.get(self, "include_private_escalations")

    @_builtins.property
    @pulumi.getter(name="includePrivateIncidents")
    @_utilities.deprecated("""Use `private_incident_scope` instead. When both are set they must agree (include_private_incidents is true for the all and owning_teams scopes, false for none).""")
    def include_private_incidents(self) -> pulumi.Output[_builtins.bool]:
        """
        DEPRECATED: use `private_incident_scope` instead. `true` when the workflow runs on private incidents (a `private_incident_scope` of `all` or `owning_teams`), `false` when the scope is `none`.
        """
        return pulumi.get(self, "include_private_incidents")

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        Name provided by the user when creating the workflow
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="onceFors")
    def once_fors(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        This workflow will run 'once for' a list of references
        """
        return pulumi.get(self, "once_fors")

    @_builtins.property
    @pulumi.getter(name="owningTeamIds")
    def owning_team_ids(self) -> pulumi.Output[Optional[Sequence[_builtins.str]]]:
        """
        IDs of the teams that own this workflow
        """
        return pulumi.get(self, "owning_team_ids")

    @_builtins.property
    @pulumi.getter(name="privateIncidentScope")
    def private_incident_scope(self) -> pulumi.Output[_builtins.str]:
        """
        Which private incidents this workflow acts on: every private incident (all), those an owning team can see (owning_teams), or none. Possible values are: `all`, `owning_teams`, `none`.
        """
        return pulumi.get(self, "private_incident_scope")

    @_builtins.property
    @pulumi.getter(name="runsOnIncidentModes")
    def runs_on_incident_modes(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        Which incident modes should this workflow run on? By default, workflows only run on standard incidents, but can also be configured to run on test and retrospective incidents. Possible values are: `standard`, `test`, `retrospective`.
        """
        return pulumi.get(self, "runs_on_incident_modes")

    @_builtins.property
    @pulumi.getter(name="runsOnIncidents")
    def runs_on_incidents(self) -> pulumi.Output[_builtins.str]:
        """
        Which incidents should the workflow be applied to? Possible values are: `newly_created`, `newly_created_and_active`.
        """
        return pulumi.get(self, "runs_on_incidents")

    @_builtins.property
    @pulumi.getter
    def shortform(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        The shortform used to trigger this workflow (only applicable for manual triggers)
        """
        return pulumi.get(self, "shortform")

    @_builtins.property
    @pulumi.getter
    def state(self) -> pulumi.Output[_builtins.str]:
        """
        What state this workflow is in. Possible values are: `active`, `disabled`, `draft`, `error`.
        """
        return pulumi.get(self, "state")

    @_builtins.property
    @pulumi.getter
    def steps(self) -> pulumi.Output[Sequence['outputs.WorkflowStep']]:
        """
        Steps that are executed as part of the workflow
        """
        return pulumi.get(self, "steps")

    @_builtins.property
    @pulumi.getter
    def trigger(self) -> pulumi.Output[_builtins.str]:
        """
        Unique name of the trigger
        """
        return pulumi.get(self, "trigger")

