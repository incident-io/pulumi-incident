# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['AlertAttributeArgs', 'AlertAttribute']

@pulumi.input_type
class AlertAttributeArgs:
    def __init__(__self__, *,
                 array: pulumi.Input[_builtins.bool],
                 type: pulumi.Input[_builtins.str],
                 emoji: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 required: pulumi.Input[Optional[_builtins.bool]] = None):
        """
        The set of arguments for constructing a AlertAttribute resource.

        :param pulumi.Input[_builtins.bool] array: Whether this attribute is an array
        :param pulumi.Input[_builtins.str] type: Engine resource name for this attribute
        :param pulumi.Input[_builtins.str] emoji: The emoji to display alongside this attribute in chat messages, stored without colons
        :param pulumi.Input[_builtins.str] name: Unique name of this attribute
        :param pulumi.Input[_builtins.bool] required: Whether this attribute is required. If this field is not set, the existing setting will be preserved.
        """
        pulumi.set(__self__, "array", array)
        pulumi.set(__self__, "type", type)
        if emoji is not None:
            pulumi.set(__self__, "emoji", emoji)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if required is not None:
            pulumi.set(__self__, "required", required)

    @_builtins.property
    @pulumi.getter
    def array(self) -> pulumi.Input[_builtins.bool]:
        """
        Whether this attribute is an array
        """
        return pulumi.get(self, "array")

    @array.setter
    def array(self, value: pulumi.Input[_builtins.bool]):
        pulumi.set(self, "array", value)

    @_builtins.property
    @pulumi.getter
    def type(self) -> pulumi.Input[_builtins.str]:
        """
        Engine resource name for this attribute
        """
        return pulumi.get(self, "type")

    @type.setter
    def type(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "type", value)

    @_builtins.property
    @pulumi.getter
    def emoji(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The emoji to display alongside this attribute in chat messages, stored without colons
        """
        return pulumi.get(self, "emoji")

    @emoji.setter
    def emoji(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "emoji", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Unique name of this attribute
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def required(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Whether this attribute is required. If this field is not set, the existing setting will be preserved.
        """
        return pulumi.get(self, "required")

    @required.setter
    def required(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "required", value)


@pulumi.input_type
class _AlertAttributeState:
    def __init__(__self__, *,
                 array: pulumi.Input[Optional[_builtins.bool]] = None,
                 emoji: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 required: pulumi.Input[Optional[_builtins.bool]] = None,
                 type: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering AlertAttribute resources.

        :param pulumi.Input[_builtins.bool] array: Whether this attribute is an array
        :param pulumi.Input[_builtins.str] emoji: The emoji to display alongside this attribute in chat messages, stored without colons
        :param pulumi.Input[_builtins.str] name: Unique name of this attribute
        :param pulumi.Input[_builtins.bool] required: Whether this attribute is required. If this field is not set, the existing setting will be preserved.
        :param pulumi.Input[_builtins.str] type: Engine resource name for this attribute
        """
        if array is not None:
            pulumi.set(__self__, "array", array)
        if emoji is not None:
            pulumi.set(__self__, "emoji", emoji)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if required is not None:
            pulumi.set(__self__, "required", required)
        if type is not None:
            pulumi.set(__self__, "type", type)

    @_builtins.property
    @pulumi.getter
    def array(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Whether this attribute is an array
        """
        return pulumi.get(self, "array")

    @array.setter
    def array(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "array", value)

    @_builtins.property
    @pulumi.getter
    def emoji(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The emoji to display alongside this attribute in chat messages, stored without colons
        """
        return pulumi.get(self, "emoji")

    @emoji.setter
    def emoji(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "emoji", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Unique name of this attribute
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def required(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Whether this attribute is required. If this field is not set, the existing setting will be preserved.
        """
        return pulumi.get(self, "required")

    @required.setter
    def required(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "required", value)

    @_builtins.property
    @pulumi.getter
    def type(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Engine resource name for this attribute
        """
        return pulumi.get(self, "type")

    @type.setter
    def type(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "type", value)


@pulumi.type_token("incident:index/alertAttribute:AlertAttribute")
class AlertAttribute(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 array: pulumi.Input[Optional[_builtins.bool]] = None,
                 emoji: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 required: pulumi.Input[Optional[_builtins.bool]] = None,
                 type: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        """
        View and manage alert attributes.

        Alert attributes are used to parse structured data from alerts coming in via alert sources.

        ## An attribute is account-wide; what fills it in is per source

        An alert attribute is the column, not the rule that populates it. It belongs to your whole
        incident.io organization: its `name` has to be unique across the account, and every alert
        source draws on the same set of attributes. That is deliberate — one attribute means the same
        thing whichever source fired the alert, so an alert route spanning several sources can match
        on it.

        What varies per source is the *binding*: the rule parsing a value for this attribute out of an
        incoming event. A source binds a given attribute at most once, and two sources can fill the
        same attribute from completely different parts of their payloads. Write each binding as an
        `AlertSourceAttribute` resource, naming the attribute and the
        `AlertSource` that fills it in.

        So a `GCP service` attribute is declared once, then bound separately by each source that sets it.

        ## Declaring one attribute from several workspaces

        If your Terraform is split across workspaces — one per environment, say — only one of them
        should declare an attribute as a resource. The others should read it with the
        `AlertAttribute` data source, which looks an attribute up by name:

        ```python
        import pulumi
        import pulumi_incident as incident

        gcp_service = incident.get_alert_attribute(name="GCP service")
        ```

        Declaring the same `name` in two workspaces plans cleanly in both, then fails when the second
        applies: the attribute it means to create already exists. To bring an attribute that already
        exists under a workspace's management instead, `import` it.

        Per-environment differences belong on the binding rather than the attribute, so both
        environments share one attribute and each parses it from its own source.

        ## Reserved names

        `Title`, `Description` and `Priority` collide with properties every alert already has, and are
        rejected regardless of casing. `Priority` does exist as an attribute you can bind and filter on,
        but it is read-only through the API: read it with the data source rather than declaring it.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident

        # Create an alert attribute that points at a single Github user in the catalog.
        #
        # Take the engine type from the catalog type itself rather than writing it out: a type one
        # of our integrations owns is referenced by its registry name, and a type you manage by its
        # ID, so the two don't have the same shape.
        github_user = incident.get_catalog_type(type_name="GithubUser")
        github_user_alert_attribute = incident.AlertAttribute("github_user",
            name="Github user",
            type=github_user.attribute_type,
            array=False,
            required=True)
        # Create an optional alert attribute for severity information
        severity = incident.AlertAttribute("severity",
            name="Severity",
            type="String",
            array=False,
            required=False,
            emoji="warning")
        # An attribute is account-wide, so declare it once. What differs between environments is the
        # binding that fills it in, which belongs to an alert source.
        gcp_service = incident.AlertAttribute("gcp_service",
            name="GCP service",
            type="String",
            array=False)
        # Staging and production each parse the same attribute out of their own source, so both
        # environments' alerts are labelled with one attribute that routes can match on.
        gcp_service_staging = incident.AlertSourceAttribute("gcp_service_staging",
            expression={
                "operations": [{
                    "parse": {
                        "function": "$.resource.labels.service_name",
                        "as_": "String",
                    },
                }],
                "start_from": "payload",
            },
            alert_source_id=gcp_staging["id"],
            alert_attribute_id=gcp_service.id)
        gcp_service_production = incident.AlertSourceAttribute("gcp_service_production",
            expression={
                "operations": [{
                    "parse": {
                        "function": "$.resource.labels.service_name",
                        "as_": "String",
                    },
                }],
                "start_from": "payload",
            },
            alert_source_id=gcp_production["id"],
            alert_attribute_id=gcp_service.id)
        # Where a source lives in a different workspace to the attribute it binds, read the attribute
        # by name rather than declaring it again. Two workspaces declaring the same name will both plan
        # cleanly, and then the second one to apply will fail.
        existing_gcp_service = incident.get_alert_attribute(name="GCP service")
        gcp_service_other_workspace = incident.AlertSourceAttribute("gcp_service_other_workspace",
            expression={
                "operations": [{
                    "parse": {
                        "function": "$.resource.labels.service_name",
                        "as_": "String",
                    },
                }],
                "start_from": "payload",
            },
            alert_source_id=gcp_other["id"],
            alert_attribute_id=existing_gcp_service.id)
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.bool] array: Whether this attribute is an array
        :param pulumi.Input[_builtins.str] emoji: The emoji to display alongside this attribute in chat messages, stored without colons
        :param pulumi.Input[_builtins.str] name: Unique name of this attribute
        :param pulumi.Input[_builtins.bool] required: Whether this attribute is required. If this field is not set, the existing setting will be preserved.
        :param pulumi.Input[_builtins.str] type: Engine resource name for this attribute
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: AlertAttributeArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        View and manage alert attributes.

        Alert attributes are used to parse structured data from alerts coming in via alert sources.

        ## An attribute is account-wide; what fills it in is per source

        An alert attribute is the column, not the rule that populates it. It belongs to your whole
        incident.io organization: its `name` has to be unique across the account, and every alert
        source draws on the same set of attributes. That is deliberate — one attribute means the same
        thing whichever source fired the alert, so an alert route spanning several sources can match
        on it.

        What varies per source is the *binding*: the rule parsing a value for this attribute out of an
        incoming event. A source binds a given attribute at most once, and two sources can fill the
        same attribute from completely different parts of their payloads. Write each binding as an
        `AlertSourceAttribute` resource, naming the attribute and the
        `AlertSource` that fills it in.

        So a `GCP service` attribute is declared once, then bound separately by each source that sets it.

        ## Declaring one attribute from several workspaces

        If your Terraform is split across workspaces — one per environment, say — only one of them
        should declare an attribute as a resource. The others should read it with the
        `AlertAttribute` data source, which looks an attribute up by name:

        ```python
        import pulumi
        import pulumi_incident as incident

        gcp_service = incident.get_alert_attribute(name="GCP service")
        ```

        Declaring the same `name` in two workspaces plans cleanly in both, then fails when the second
        applies: the attribute it means to create already exists. To bring an attribute that already
        exists under a workspace's management instead, `import` it.

        Per-environment differences belong on the binding rather than the attribute, so both
        environments share one attribute and each parses it from its own source.

        ## Reserved names

        `Title`, `Description` and `Priority` collide with properties every alert already has, and are
        rejected regardless of casing. `Priority` does exist as an attribute you can bind and filter on,
        but it is read-only through the API: read it with the data source rather than declaring it.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident

        # Create an alert attribute that points at a single Github user in the catalog.
        #
        # Take the engine type from the catalog type itself rather than writing it out: a type one
        # of our integrations owns is referenced by its registry name, and a type you manage by its
        # ID, so the two don't have the same shape.
        github_user = incident.get_catalog_type(type_name="GithubUser")
        github_user_alert_attribute = incident.AlertAttribute("github_user",
            name="Github user",
            type=github_user.attribute_type,
            array=False,
            required=True)
        # Create an optional alert attribute for severity information
        severity = incident.AlertAttribute("severity",
            name="Severity",
            type="String",
            array=False,
            required=False,
            emoji="warning")
        # An attribute is account-wide, so declare it once. What differs between environments is the
        # binding that fills it in, which belongs to an alert source.
        gcp_service = incident.AlertAttribute("gcp_service",
            name="GCP service",
            type="String",
            array=False)
        # Staging and production each parse the same attribute out of their own source, so both
        # environments' alerts are labelled with one attribute that routes can match on.
        gcp_service_staging = incident.AlertSourceAttribute("gcp_service_staging",
            expression={
                "operations": [{
                    "parse": {
                        "function": "$.resource.labels.service_name",
                        "as_": "String",
                    },
                }],
                "start_from": "payload",
            },
            alert_source_id=gcp_staging["id"],
            alert_attribute_id=gcp_service.id)
        gcp_service_production = incident.AlertSourceAttribute("gcp_service_production",
            expression={
                "operations": [{
                    "parse": {
                        "function": "$.resource.labels.service_name",
                        "as_": "String",
                    },
                }],
                "start_from": "payload",
            },
            alert_source_id=gcp_production["id"],
            alert_attribute_id=gcp_service.id)
        # Where a source lives in a different workspace to the attribute it binds, read the attribute
        # by name rather than declaring it again. Two workspaces declaring the same name will both plan
        # cleanly, and then the second one to apply will fail.
        existing_gcp_service = incident.get_alert_attribute(name="GCP service")
        gcp_service_other_workspace = incident.AlertSourceAttribute("gcp_service_other_workspace",
            expression={
                "operations": [{
                    "parse": {
                        "function": "$.resource.labels.service_name",
                        "as_": "String",
                    },
                }],
                "start_from": "payload",
            },
            alert_source_id=gcp_other["id"],
            alert_attribute_id=existing_gcp_service.id)
        ```


        :param str resource_name: The name of the resource.
        :param AlertAttributeArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(AlertAttributeArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 array: pulumi.Input[Optional[_builtins.bool]] = None,
                 emoji: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 required: pulumi.Input[Optional[_builtins.bool]] = None,
                 type: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = AlertAttributeArgs.__new__(AlertAttributeArgs)

            if array is None and not opts.urn:
                raise TypeError("Missing required property 'array'")
            __props__.__dict__["array"] = array
            __props__.__dict__["emoji"] = emoji
            __props__.__dict__["name"] = name
            __props__.__dict__["required"] = required
            if type is None and not opts.urn:
                raise TypeError("Missing required property 'type'")
            __props__.__dict__["type"] = type
        super(AlertAttribute, __self__).__init__(
            'incident:index/alertAttribute:AlertAttribute',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            array: pulumi.Input[Optional[_builtins.bool]] = None,
            emoji: pulumi.Input[Optional[_builtins.str]] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            required: pulumi.Input[Optional[_builtins.bool]] = None,
            type: pulumi.Input[Optional[_builtins.str]] = None) -> 'AlertAttribute':
        """
        Get an existing AlertAttribute resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.bool] array: Whether this attribute is an array
        :param pulumi.Input[_builtins.str] emoji: The emoji to display alongside this attribute in chat messages, stored without colons
        :param pulumi.Input[_builtins.str] name: Unique name of this attribute
        :param pulumi.Input[_builtins.bool] required: Whether this attribute is required. If this field is not set, the existing setting will be preserved.
        :param pulumi.Input[_builtins.str] type: Engine resource name for this attribute
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _AlertAttributeState.__new__(_AlertAttributeState)

        __props__.__dict__["array"] = array
        __props__.__dict__["emoji"] = emoji
        __props__.__dict__["name"] = name
        __props__.__dict__["required"] = required
        __props__.__dict__["type"] = type
        return AlertAttribute(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter
    def array(self) -> pulumi.Output[_builtins.bool]:
        """
        Whether this attribute is an array
        """
        return pulumi.get(self, "array")

    @_builtins.property
    @pulumi.getter
    def emoji(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        The emoji to display alongside this attribute in chat messages, stored without colons
        """
        return pulumi.get(self, "emoji")

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        Unique name of this attribute
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def required(self) -> pulumi.Output[_builtins.bool]:
        """
        Whether this attribute is required. If this field is not set, the existing setting will be preserved.
        """
        return pulumi.get(self, "required")

    @_builtins.property
    @pulumi.getter
    def type(self) -> pulumi.Output[_builtins.str]:
        """
        Engine resource name for this attribute
        """
        return pulumi.get(self, "type")

