# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs

__all__ = [
    'GetScheduleRotationBetaResult',
    'AwaitableGetScheduleRotationBetaResult',
    'get_schedule_rotation_beta',
    'get_schedule_rotation_beta_output',
]

@pulumi.output_type
class GetScheduleRotationBetaResult:
    """
    A collection of values returned by getScheduleRotationBeta.
    """
    def __init__(__self__, concurrent_shifts=None, effective_from=None, first_interval_starts_at=None, handovers=None, id=None, name=None, rank=None, schedule_id=None, scheduling_mode=None, users=None, working_intervals=None):
        if concurrent_shifts and not isinstance(concurrent_shifts, int):
            raise TypeError("Expected argument 'concurrent_shifts' to be a int")
        pulumi.set(__self__, "concurrent_shifts", concurrent_shifts)
        if effective_from and not isinstance(effective_from, str):
            raise TypeError("Expected argument 'effective_from' to be a str")
        pulumi.set(__self__, "effective_from", effective_from)
        if first_interval_starts_at and not isinstance(first_interval_starts_at, str):
            raise TypeError("Expected argument 'first_interval_starts_at' to be a str")
        pulumi.set(__self__, "first_interval_starts_at", first_interval_starts_at)
        if handovers and not isinstance(handovers, list):
            raise TypeError("Expected argument 'handovers' to be a list")
        pulumi.set(__self__, "handovers", handovers)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if rank and not isinstance(rank, int):
            raise TypeError("Expected argument 'rank' to be a int")
        pulumi.set(__self__, "rank", rank)
        if schedule_id and not isinstance(schedule_id, str):
            raise TypeError("Expected argument 'schedule_id' to be a str")
        pulumi.set(__self__, "schedule_id", schedule_id)
        if scheduling_mode and not isinstance(scheduling_mode, str):
            raise TypeError("Expected argument 'scheduling_mode' to be a str")
        pulumi.set(__self__, "scheduling_mode", scheduling_mode)
        if users and not isinstance(users, list):
            raise TypeError("Expected argument 'users' to be a list")
        pulumi.set(__self__, "users", users)
        if working_intervals and not isinstance(working_intervals, list):
            raise TypeError("Expected argument 'working_intervals' to be a list")
        pulumi.set(__self__, "working_intervals", working_intervals)

    @_builtins.property
    @pulumi.getter(name="concurrentShifts")
    def concurrent_shifts(self) -> _builtins.int:
        """
        How many shifts run at the same time, which is how many people are on call at once
        """
        return pulumi.get(self, "concurrent_shifts")

    @_builtins.property
    @pulumi.getter(name="effectiveFrom")
    def effective_from(self) -> _builtins.str:
        """
        When this shape of the rotation took over. Unset when the rotation has only ever had one.
        """
        return pulumi.get(self, "effective_from")

    @_builtins.property
    @pulumi.getter(name="firstIntervalStartsAt")
    def first_interval_starts_at(self) -> _builtins.str:
        """
        When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.
        """
        return pulumi.get(self, "first_interval_starts_at")

    @_builtins.property
    @pulumi.getter
    def handovers(self) -> Sequence['outputs.GetScheduleRotationBetaHandoverResult']:
        """
        The cadence shifts hand over on, applied in turn
        """
        return pulumi.get(self, "handovers")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Look up the rotation by ID.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        Look up the rotation by name, which is unique within the schedule.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def rank(self) -> _builtins.int:
        """
        Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
        """
        return pulumi.get(self, "rank")

    @_builtins.property
    @pulumi.getter(name="scheduleId")
    def schedule_id(self) -> _builtins.str:
        """
        ID of the schedule this rotation belongs to
        """
        return pulumi.get(self, "schedule_id")

    @_builtins.property
    @pulumi.getter(name="schedulingMode")
    def scheduling_mode(self) -> _builtins.str:
        """
        How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`.
        """
        return pulumi.get(self, "scheduling_mode")

    @_builtins.property
    @pulumi.getter
    def users(self) -> Sequence[_builtins.str]:
        """
        IDs of the people in the rotation, in the order they take shifts.
        """
        return pulumi.get(self, "users")

    @_builtins.property
    @pulumi.getter(name="workingIntervals")
    def working_intervals(self) -> Sequence['outputs.GetScheduleRotationBetaWorkingIntervalResult']:
        """
        Weekday intervals the rotation is on call for, if it's restricted to any.
        """
        return pulumi.get(self, "working_intervals")


class AwaitableGetScheduleRotationBetaResult(GetScheduleRotationBetaResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetScheduleRotationBetaResult(
            concurrent_shifts=self.concurrent_shifts,
            effective_from=self.effective_from,
            first_interval_starts_at=self.first_interval_starts_at,
            handovers=self.handovers,
            id=self.id,
            name=self.name,
            rank=self.rank,
            schedule_id=self.schedule_id,
            scheduling_mode=self.scheduling_mode,
            users=self.users,
            working_intervals=self.working_intervals)


def get_schedule_rotation_beta(id: Optional[_builtins.str] = None,
                               name: Optional[_builtins.str] = None,
                               schedule_id: Optional[_builtins.str] = None,
                               opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetScheduleRotationBetaResult:
    """
    `ScheduleRotationBeta` was renamed to `ScheduleRotation` in v7.0.

    This name still works and still manages the same thing, so upgrading to v7 needs no change
    to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
    be removed in v8.0.

    To move onto the new name:

    ```python
    import pulumi
    ```

    Both names are the same resource, backed by the same schema and the same API, so the move
    carries your state across and the plan after it is empty. See `ScheduleRotation` for the
    documentation.


    :param _builtins.str id: Look up the rotation by ID.
    :param _builtins.str name: Look up the rotation by name, which is unique within the schedule.
    :param _builtins.str schedule_id: ID of the schedule this rotation belongs to
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    __args__['scheduleId'] = schedule_id
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('incident:index/getScheduleRotationBeta:getScheduleRotationBeta', __args__, opts=opts, typ=GetScheduleRotationBetaResult).value

    return AwaitableGetScheduleRotationBetaResult(
        concurrent_shifts=pulumi.get(__ret__, 'concurrent_shifts'),
        effective_from=pulumi.get(__ret__, 'effective_from'),
        first_interval_starts_at=pulumi.get(__ret__, 'first_interval_starts_at'),
        handovers=pulumi.get(__ret__, 'handovers'),
        id=pulumi.get(__ret__, 'id'),
        name=pulumi.get(__ret__, 'name'),
        rank=pulumi.get(__ret__, 'rank'),
        schedule_id=pulumi.get(__ret__, 'schedule_id'),
        scheduling_mode=pulumi.get(__ret__, 'scheduling_mode'),
        users=pulumi.get(__ret__, 'users'),
        working_intervals=pulumi.get(__ret__, 'working_intervals'))
def get_schedule_rotation_beta_output(id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                                      name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                                      schedule_id: pulumi.Input[Optional[_builtins.str]] = None,
                                      opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetScheduleRotationBetaResult]:
    """
    `ScheduleRotationBeta` was renamed to `ScheduleRotation` in v7.0.

    This name still works and still manages the same thing, so upgrading to v7 needs no change
    to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
    be removed in v8.0.

    To move onto the new name:

    ```python
    import pulumi
    ```

    Both names are the same resource, backed by the same schema and the same API, so the move
    carries your state across and the plan after it is empty. See `ScheduleRotation` for the
    documentation.


    :param _builtins.str id: Look up the rotation by ID.
    :param _builtins.str name: Look up the rotation by name, which is unique within the schedule.
    :param _builtins.str schedule_id: ID of the schedule this rotation belongs to
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    __args__['scheduleId'] = schedule_id
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('incident:index/getScheduleRotationBeta:getScheduleRotationBeta', __args__, opts=opts, typ=GetScheduleRotationBetaResult)
    return __ret__.apply(lambda __response__: GetScheduleRotationBetaResult(
        concurrent_shifts=pulumi.get(__response__, 'concurrent_shifts'),
        effective_from=pulumi.get(__response__, 'effective_from'),
        first_interval_starts_at=pulumi.get(__response__, 'first_interval_starts_at'),
        handovers=pulumi.get(__response__, 'handovers'),
        id=pulumi.get(__response__, 'id'),
        name=pulumi.get(__response__, 'name'),
        rank=pulumi.get(__response__, 'rank'),
        schedule_id=pulumi.get(__response__, 'schedule_id'),
        scheduling_mode=pulumi.get(__response__, 'scheduling_mode'),
        users=pulumi.get(__response__, 'users'),
        working_intervals=pulumi.get(__response__, 'working_intervals')))
