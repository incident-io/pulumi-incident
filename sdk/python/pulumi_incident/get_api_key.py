# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = [
    'GetApiKeyResult',
    'AwaitableGetApiKeyResult',
    'get_api_key',
    'get_api_key_output',
]

@pulumi.output_type
class GetApiKeyResult:
    """
    A collection of values returned by getApiKey.
    """
    def __init__(__self__, comments=None, created_at=None, id=None, last_used_at=None, name=None, role_names=None, team_ids=None, team_role_names=None, token_last_issued_at=None):
        if comments and not isinstance(comments, str):
            raise TypeError("Expected argument 'comments' to be a str")
        pulumi.set(__self__, "comments", comments)
        if created_at and not isinstance(created_at, str):
            raise TypeError("Expected argument 'created_at' to be a str")
        pulumi.set(__self__, "created_at", created_at)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if last_used_at and not isinstance(last_used_at, str):
            raise TypeError("Expected argument 'last_used_at' to be a str")
        pulumi.set(__self__, "last_used_at", last_used_at)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if role_names and not isinstance(role_names, list):
            raise TypeError("Expected argument 'role_names' to be a list")
        pulumi.set(__self__, "role_names", role_names)
        if team_ids and not isinstance(team_ids, list):
            raise TypeError("Expected argument 'team_ids' to be a list")
        pulumi.set(__self__, "team_ids", team_ids)
        if team_role_names and not isinstance(team_role_names, list):
            raise TypeError("Expected argument 'team_role_names' to be a list")
        pulumi.set(__self__, "team_role_names", team_role_names)
        if token_last_issued_at and not isinstance(token_last_issued_at, str):
            raise TypeError("Expected argument 'token_last_issued_at' to be a str")
        pulumi.set(__self__, "token_last_issued_at", token_last_issued_at)

    @_builtins.property
    @pulumi.getter
    def comments(self) -> _builtins.str:
        """
        Freeform notes about this API key
        """
        return pulumi.get(self, "comments")

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> _builtins.str:
        """
        When the API key was created
        """
        return pulumi.get(self, "created_at")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Unique identifier for this API key
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter(name="lastUsedAt")
    def last_used_at(self) -> _builtins.str:
        """
        When the key was last used to authenticate a request
        """
        return pulumi.get(self, "last_used_at")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        The name of the API key, for the user's reference
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="roleNames")
    def role_names(self) -> Sequence[_builtins.str]:
        """
        The account-level roles assigned to this API key
        """
        return pulumi.get(self, "role_names")

    @_builtins.property
    @pulumi.getter(name="teamIds")
    def team_ids(self) -> Sequence[_builtins.str]:
        """
        IDs of teams that this API key is scoped to
        """
        return pulumi.get(self, "team_ids")

    @_builtins.property
    @pulumi.getter(name="teamRoleNames")
    def team_role_names(self) -> Sequence[_builtins.str]:
        """
        The team-level roles assigned to this API key
        """
        return pulumi.get(self, "team_role_names")

    @_builtins.property
    @pulumi.getter(name="tokenLastIssuedAt")
    def token_last_issued_at(self) -> _builtins.str:
        """
        When the current token for this API was last issued. This is the last time the token was rotated, or when it was initially created. Older tokens may remain valid for up to an hour after they have been rotated, configured when you call the rotate endpoint.
        """
        return pulumi.get(self, "token_last_issued_at")


class AwaitableGetApiKeyResult(GetApiKeyResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetApiKeyResult(
            comments=self.comments,
            created_at=self.created_at,
            id=self.id,
            last_used_at=self.last_used_at,
            name=self.name,
            role_names=self.role_names,
            team_ids=self.team_ids,
            team_role_names=self.team_role_names,
            token_last_issued_at=self.token_last_issued_at)


def get_api_key(id: Optional[_builtins.str] = None,
                name: Optional[_builtins.str] = None,
                opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetApiKeyResult:
    """
    Manage API keys for your organization.

    This API lets you view, create, update, delete, and rotate API keys programmatically, including configuring account-level roles and roles scoped to specific teams.

    Common use cases include:
    - **API key rotation**: You can issue a new access token for any API key using the `Rotate` endpoint.
    - **Managing keys at scale**: use the `Show`, `List`, `Create`, `Update`, and `Delete` endpoints to automate API key lifecycle management.

    ## Permissions model

    To use these endpoints, your API key must have the `api_keys_manage` role granted at either the account level or for specific teams.

    In addition, the following rules apply, when a "caller" API key is managing a "target" API key:

    - A caller key can only assign roles whose scopes are a subset of its own scopes. For example, a key with `viewer` and `incident_creator` can assign those roles to a target key, but cannot assign the `incident_editor` role, since it has additional scopes.
    - The same applies at the team level: A caller key with `schedules_editor` at the account level can create a target key with that role for a specific team, but a key with `schedules_editor` only for one team (Team A, say) cannot assign it at the account level or for another team, Team B.
    - The `api_keys_manage` role cannot be assigned via the API. To create a key with that role, you must go to Settings → API keys in the dashboard, and click "Add new", creating a key with the "View, create, edit, delete or rotate API keys" role (`api_keys_manage`).
    - The `Delete` endpoint does not check whether the calling API key holds the scopes of the key being deleted. However, a team-scoped key can only delete keys belonging to its teams.
    - To rotate the token of an API key, use the Rotate endpoint or by clicking "Rotate token" on any API key listed in the dashboard. The same permissions limitations apply as when creating or updating an API key.

    ## Finding role names and team IDs

    To find valid values for `role_names`, `team_ids`, and `team_role_names`, go to Settings → API keys in the dashboard. Click to either edit an existing key or create a new one, select the desired roles and teams, and then use the copy button to get hold of the role and team identifiers as JSON.

    Use this data source to look up an existing API key, either by `id` or by `name`. This is how you read the roles held by a key somebody created in the dashboard without managing it as an `ApiKey` resource. Set exactly one of the two lookup attributes; setting both, or neither, is rejected at plan time.

    A key's token is returned only when incident.io issues one, so there is no token attribute here: a lookup can tell you what a key may do and when it was last used, but never how to authenticate as it.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up an existing API key by name, such as one created in the dashboard.
    ci = incident.get_api_key(name="CI deploy key")
    # Or by ID, to reference a key another module manages. Names aren't unique, so an ID is
    # the way through an ambiguous lookup.
    reporting = incident.get_api_key(id="01G0J1EXE7AXZ2C93K61WBPYEH")
    pulumi.export("ciKeyRoles", ci.role_names)
    pulumi.export("ciKeyLastUsedAt", ci.last_used_at)
    ```


    :param _builtins.str id: Unique identifier for this API key
    :param _builtins.str name: The name of the API key, for the user's reference
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('incident:index/getApiKey:getApiKey', __args__, opts=opts, typ=GetApiKeyResult).value

    return AwaitableGetApiKeyResult(
        comments=pulumi.get(__ret__, 'comments'),
        created_at=pulumi.get(__ret__, 'created_at'),
        id=pulumi.get(__ret__, 'id'),
        last_used_at=pulumi.get(__ret__, 'last_used_at'),
        name=pulumi.get(__ret__, 'name'),
        role_names=pulumi.get(__ret__, 'role_names'),
        team_ids=pulumi.get(__ret__, 'team_ids'),
        team_role_names=pulumi.get(__ret__, 'team_role_names'),
        token_last_issued_at=pulumi.get(__ret__, 'token_last_issued_at'))
def get_api_key_output(id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                       name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                       opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetApiKeyResult]:
    """
    Manage API keys for your organization.

    This API lets you view, create, update, delete, and rotate API keys programmatically, including configuring account-level roles and roles scoped to specific teams.

    Common use cases include:
    - **API key rotation**: You can issue a new access token for any API key using the `Rotate` endpoint.
    - **Managing keys at scale**: use the `Show`, `List`, `Create`, `Update`, and `Delete` endpoints to automate API key lifecycle management.

    ## Permissions model

    To use these endpoints, your API key must have the `api_keys_manage` role granted at either the account level or for specific teams.

    In addition, the following rules apply, when a "caller" API key is managing a "target" API key:

    - A caller key can only assign roles whose scopes are a subset of its own scopes. For example, a key with `viewer` and `incident_creator` can assign those roles to a target key, but cannot assign the `incident_editor` role, since it has additional scopes.
    - The same applies at the team level: A caller key with `schedules_editor` at the account level can create a target key with that role for a specific team, but a key with `schedules_editor` only for one team (Team A, say) cannot assign it at the account level or for another team, Team B.
    - The `api_keys_manage` role cannot be assigned via the API. To create a key with that role, you must go to Settings → API keys in the dashboard, and click "Add new", creating a key with the "View, create, edit, delete or rotate API keys" role (`api_keys_manage`).
    - The `Delete` endpoint does not check whether the calling API key holds the scopes of the key being deleted. However, a team-scoped key can only delete keys belonging to its teams.
    - To rotate the token of an API key, use the Rotate endpoint or by clicking "Rotate token" on any API key listed in the dashboard. The same permissions limitations apply as when creating or updating an API key.

    ## Finding role names and team IDs

    To find valid values for `role_names`, `team_ids`, and `team_role_names`, go to Settings → API keys in the dashboard. Click to either edit an existing key or create a new one, select the desired roles and teams, and then use the copy button to get hold of the role and team identifiers as JSON.

    Use this data source to look up an existing API key, either by `id` or by `name`. This is how you read the roles held by a key somebody created in the dashboard without managing it as an `ApiKey` resource. Set exactly one of the two lookup attributes; setting both, or neither, is rejected at plan time.

    A key's token is returned only when incident.io issues one, so there is no token attribute here: a lookup can tell you what a key may do and when it was last used, but never how to authenticate as it.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up an existing API key by name, such as one created in the dashboard.
    ci = incident.get_api_key(name="CI deploy key")
    # Or by ID, to reference a key another module manages. Names aren't unique, so an ID is
    # the way through an ambiguous lookup.
    reporting = incident.get_api_key(id="01G0J1EXE7AXZ2C93K61WBPYEH")
    pulumi.export("ciKeyRoles", ci.role_names)
    pulumi.export("ciKeyLastUsedAt", ci.last_used_at)
    ```


    :param _builtins.str id: Unique identifier for this API key
    :param _builtins.str name: The name of the API key, for the user's reference
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('incident:index/getApiKey:getApiKey', __args__, opts=opts, typ=GetApiKeyResult)
    return __ret__.apply(lambda __response__: GetApiKeyResult(
        comments=pulumi.get(__response__, 'comments'),
        created_at=pulumi.get(__response__, 'created_at'),
        id=pulumi.get(__response__, 'id'),
        last_used_at=pulumi.get(__response__, 'last_used_at'),
        name=pulumi.get(__response__, 'name'),
        role_names=pulumi.get(__response__, 'role_names'),
        team_ids=pulumi.get(__response__, 'team_ids'),
        team_role_names=pulumi.get(__response__, 'team_role_names'),
        token_last_issued_at=pulumi.get(__response__, 'token_last_issued_at')))
