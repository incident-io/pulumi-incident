# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['ApiKeyArgs', 'ApiKey']

@pulumi.input_type
class ApiKeyArgs:
    def __init__(__self__, *,
                 comments: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 rotation_grace_period_minutes: pulumi.Input[Optional[_builtins.int]] = None,
                 team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 token_version: pulumi.Input[Optional[_builtins.int]] = None):
        """
        The set of arguments for constructing a ApiKey resource.

        :param pulumi.Input[_builtins.str] comments: Freeform notes about this API key
        :param pulumi.Input[_builtins.str] name: The name of the API key, for the user's reference
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] role_names: Account-level roles to assign to the API key. These roles apply across the entire account, not scoped to specific teams. Pass an empty array if no account-level roles are needed.
        :param pulumi.Input[_builtins.int] rotation_grace_period_minutes: How long the previous token keeps working after a rotation, in minutes, giving whatever holds it a window to pick up the new one. Defaults to 30. Set it to 0 to retire the old token immediately, at the cost of breaking anything still using it. incident.io documents an hour as the longest a rotated token stays valid, so it may reject a longer period. Only read when a change to `token_version` rotates the key.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_ids: IDs of teams to scope the `team_role_names` to. If provided, `team_role_names` must also be a non-empty array, and vice versa. Pass an empty array if the key should not be scoped to any teams.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_role_names: Roles to grant for the teams specified in `team_ids`. If provided, `team_ids` must also be a non-empty array, and vice versa. Pass an empty array if no team-level roles are needed.
               
               API key role name that may be granted for team-scoped access. Possible values are: `catalog_editor`, `schedules_editor`, `schedules_reader`, `schedule_overrides_editor`, `on_call_editor`, `escalation_creator`, `api_keys_manage`, `workflows_editor`, `private_workflows_editor`, `secrets_manage`, `secrets_use`, `heartbeats_ping`, `telemetry_query_restricted`, `telemetry_data_source_update`.
        :param pulumi.Input[_builtins.int] token_version: The version of the token this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. It is your own counter, and incident.io never sees it. Leave it unset to manage a key's name and roles without ever rotating it.
        """
        if comments is not None:
            pulumi.set(__self__, "comments", comments)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if role_names is not None:
            pulumi.set(__self__, "role_names", role_names)
        if rotation_grace_period_minutes is not None:
            pulumi.set(__self__, "rotation_grace_period_minutes", rotation_grace_period_minutes)
        if team_ids is not None:
            pulumi.set(__self__, "team_ids", team_ids)
        if team_role_names is not None:
            pulumi.set(__self__, "team_role_names", team_role_names)
        if token_version is not None:
            pulumi.set(__self__, "token_version", token_version)

    @_builtins.property
    @pulumi.getter
    def comments(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Freeform notes about this API key
        """
        return pulumi.get(self, "comments")

    @comments.setter
    def comments(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "comments", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The name of the API key, for the user's reference
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="roleNames")
    def role_names(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Account-level roles to assign to the API key. These roles apply across the entire account, not scoped to specific teams. Pass an empty array if no account-level roles are needed.
        """
        return pulumi.get(self, "role_names")

    @role_names.setter
    def role_names(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "role_names", value)

    @_builtins.property
    @pulumi.getter(name="rotationGracePeriodMinutes")
    def rotation_grace_period_minutes(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        How long the previous token keeps working after a rotation, in minutes, giving whatever holds it a window to pick up the new one. Defaults to 30. Set it to 0 to retire the old token immediately, at the cost of breaking anything still using it. incident.io documents an hour as the longest a rotated token stays valid, so it may reject a longer period. Only read when a change to `token_version` rotates the key.
        """
        return pulumi.get(self, "rotation_grace_period_minutes")

    @rotation_grace_period_minutes.setter
    def rotation_grace_period_minutes(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "rotation_grace_period_minutes", value)

    @_builtins.property
    @pulumi.getter(name="teamIds")
    def team_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        IDs of teams to scope the `team_role_names` to. If provided, `team_role_names` must also be a non-empty array, and vice versa. Pass an empty array if the key should not be scoped to any teams.
        """
        return pulumi.get(self, "team_ids")

    @team_ids.setter
    def team_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "team_ids", value)

    @_builtins.property
    @pulumi.getter(name="teamRoleNames")
    def team_role_names(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Roles to grant for the teams specified in `team_ids`. If provided, `team_ids` must also be a non-empty array, and vice versa. Pass an empty array if no team-level roles are needed.

        API key role name that may be granted for team-scoped access. Possible values are: `catalog_editor`, `schedules_editor`, `schedules_reader`, `schedule_overrides_editor`, `on_call_editor`, `escalation_creator`, `api_keys_manage`, `workflows_editor`, `private_workflows_editor`, `secrets_manage`, `secrets_use`, `heartbeats_ping`, `telemetry_query_restricted`, `telemetry_data_source_update`.
        """
        return pulumi.get(self, "team_role_names")

    @team_role_names.setter
    def team_role_names(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "team_role_names", value)

    @_builtins.property
    @pulumi.getter(name="tokenVersion")
    def token_version(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The version of the token this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. It is your own counter, and incident.io never sees it. Leave it unset to manage a key's name and roles without ever rotating it.
        """
        return pulumi.get(self, "token_version")

    @token_version.setter
    def token_version(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "token_version", value)


@pulumi.input_type
class _ApiKeyState:
    def __init__(__self__, *,
                 comments: pulumi.Input[Optional[_builtins.str]] = None,
                 created_at: pulumi.Input[Optional[_builtins.str]] = None,
                 last_used_at: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 rotation_grace_period_minutes: pulumi.Input[Optional[_builtins.int]] = None,
                 team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 token: pulumi.Input[Optional[_builtins.str]] = None,
                 token_last_issued_at: pulumi.Input[Optional[_builtins.str]] = None,
                 token_version: pulumi.Input[Optional[_builtins.int]] = None):
        """
        Input properties used for looking up and filtering ApiKey resources.

        :param pulumi.Input[_builtins.str] comments: Freeform notes about this API key
        :param pulumi.Input[_builtins.str] created_at: When the API key was created
        :param pulumi.Input[_builtins.str] last_used_at: When the key was last used to authenticate a request
        :param pulumi.Input[_builtins.str] name: The name of the API key, for the user's reference
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] role_names: Account-level roles to assign to the API key. These roles apply across the entire account, not scoped to specific teams. Pass an empty array if no account-level roles are needed.
        :param pulumi.Input[_builtins.int] rotation_grace_period_minutes: How long the previous token keeps working after a rotation, in minutes, giving whatever holds it a window to pick up the new one. Defaults to 30. Set it to 0 to retire the old token immediately, at the cost of breaking anything still using it. incident.io documents an hour as the longest a rotated token stays valid, so it may reject a longer period. Only read when a change to `token_version` rotates the key.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_ids: IDs of teams to scope the `team_role_names` to. If provided, `team_role_names` must also be a non-empty array, and vice versa. Pass an empty array if the key should not be scoped to any teams.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_role_names: Roles to grant for the teams specified in `team_ids`. If provided, `team_ids` must also be a non-empty array, and vice versa. Pass an empty array if no team-level roles are needed.
               
               API key role name that may be granted for team-scoped access. Possible values are: `catalog_editor`, `schedules_editor`, `schedules_reader`, `schedule_overrides_editor`, `on_call_editor`, `escalation_creator`, `api_keys_manage`, `workflows_editor`, `private_workflows_editor`, `secrets_manage`, `secrets_use`, `heartbeats_ping`, `telemetry_query_restricted`, `telemetry_data_source_update`.
        :param pulumi.Input[_builtins.str] token: The bearer token to authenticate as this key, which incident.io returns only when it issues one: on create, and on each rotation. Terraform stores it, because nothing can read it back afterwards, so anything with access to your state can read it. Null for a key adopted with `terraform import`, whose token was issued before Terraform knew about it - rotate the key to get one.
        :param pulumi.Input[_builtins.str] token_last_issued_at: When the current token for this API was last issued. This is the last time the token was rotated, or when it was initially created. Older tokens may remain valid for up to an hour after they have been rotated, configured when you call the rotate endpoint.
        :param pulumi.Input[_builtins.int] token_version: The version of the token this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. It is your own counter, and incident.io never sees it. Leave it unset to manage a key's name and roles without ever rotating it.
        """
        if comments is not None:
            pulumi.set(__self__, "comments", comments)
        if created_at is not None:
            pulumi.set(__self__, "created_at", created_at)
        if last_used_at is not None:
            pulumi.set(__self__, "last_used_at", last_used_at)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if role_names is not None:
            pulumi.set(__self__, "role_names", role_names)
        if rotation_grace_period_minutes is not None:
            pulumi.set(__self__, "rotation_grace_period_minutes", rotation_grace_period_minutes)
        if team_ids is not None:
            pulumi.set(__self__, "team_ids", team_ids)
        if team_role_names is not None:
            pulumi.set(__self__, "team_role_names", team_role_names)
        if token is not None:
            pulumi.set(__self__, "token", token)
        if token_last_issued_at is not None:
            pulumi.set(__self__, "token_last_issued_at", token_last_issued_at)
        if token_version is not None:
            pulumi.set(__self__, "token_version", token_version)

    @_builtins.property
    @pulumi.getter
    def comments(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Freeform notes about this API key
        """
        return pulumi.get(self, "comments")

    @comments.setter
    def comments(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "comments", value)

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        When the API key was created
        """
        return pulumi.get(self, "created_at")

    @created_at.setter
    def created_at(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "created_at", value)

    @_builtins.property
    @pulumi.getter(name="lastUsedAt")
    def last_used_at(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        When the key was last used to authenticate a request
        """
        return pulumi.get(self, "last_used_at")

    @last_used_at.setter
    def last_used_at(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "last_used_at", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The name of the API key, for the user's reference
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="roleNames")
    def role_names(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Account-level roles to assign to the API key. These roles apply across the entire account, not scoped to specific teams. Pass an empty array if no account-level roles are needed.
        """
        return pulumi.get(self, "role_names")

    @role_names.setter
    def role_names(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "role_names", value)

    @_builtins.property
    @pulumi.getter(name="rotationGracePeriodMinutes")
    def rotation_grace_period_minutes(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        How long the previous token keeps working after a rotation, in minutes, giving whatever holds it a window to pick up the new one. Defaults to 30. Set it to 0 to retire the old token immediately, at the cost of breaking anything still using it. incident.io documents an hour as the longest a rotated token stays valid, so it may reject a longer period. Only read when a change to `token_version` rotates the key.
        """
        return pulumi.get(self, "rotation_grace_period_minutes")

    @rotation_grace_period_minutes.setter
    def rotation_grace_period_minutes(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "rotation_grace_period_minutes", value)

    @_builtins.property
    @pulumi.getter(name="teamIds")
    def team_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        IDs of teams to scope the `team_role_names` to. If provided, `team_role_names` must also be a non-empty array, and vice versa. Pass an empty array if the key should not be scoped to any teams.
        """
        return pulumi.get(self, "team_ids")

    @team_ids.setter
    def team_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "team_ids", value)

    @_builtins.property
    @pulumi.getter(name="teamRoleNames")
    def team_role_names(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Roles to grant for the teams specified in `team_ids`. If provided, `team_ids` must also be a non-empty array, and vice versa. Pass an empty array if no team-level roles are needed.

        API key role name that may be granted for team-scoped access. Possible values are: `catalog_editor`, `schedules_editor`, `schedules_reader`, `schedule_overrides_editor`, `on_call_editor`, `escalation_creator`, `api_keys_manage`, `workflows_editor`, `private_workflows_editor`, `secrets_manage`, `secrets_use`, `heartbeats_ping`, `telemetry_query_restricted`, `telemetry_data_source_update`.
        """
        return pulumi.get(self, "team_role_names")

    @team_role_names.setter
    def team_role_names(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "team_role_names", value)

    @_builtins.property
    @pulumi.getter
    def token(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The bearer token to authenticate as this key, which incident.io returns only when it issues one: on create, and on each rotation. Terraform stores it, because nothing can read it back afterwards, so anything with access to your state can read it. Null for a key adopted with `terraform import`, whose token was issued before Terraform knew about it - rotate the key to get one.
        """
        return pulumi.get(self, "token")

    @token.setter
    def token(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "token", value)

    @_builtins.property
    @pulumi.getter(name="tokenLastIssuedAt")
    def token_last_issued_at(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        When the current token for this API was last issued. This is the last time the token was rotated, or when it was initially created. Older tokens may remain valid for up to an hour after they have been rotated, configured when you call the rotate endpoint.
        """
        return pulumi.get(self, "token_last_issued_at")

    @token_last_issued_at.setter
    def token_last_issued_at(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "token_last_issued_at", value)

    @_builtins.property
    @pulumi.getter(name="tokenVersion")
    def token_version(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The version of the token this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. It is your own counter, and incident.io never sees it. Leave it unset to manage a key's name and roles without ever rotating it.
        """
        return pulumi.get(self, "token_version")

    @token_version.setter
    def token_version(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "token_version", value)


@pulumi.type_token("incident:index/apiKey:ApiKey")
class ApiKey(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 comments: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 rotation_grace_period_minutes: pulumi.Input[Optional[_builtins.int]] = None,
                 team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 token_version: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        """
        Manage API keys for your organization.

        This API lets you view, create, update, delete, and rotate API keys programmatically, including configuring account-level roles and roles scoped to specific teams.

        Common use cases include:
        - **API key rotation**: You can issue a new access token for any API key using the `Rotate` endpoint.
        - **Managing keys at scale**: use the `Show`, `List`, `Create`, `Update`, and `Delete` endpoints to automate API key lifecycle management.

        ## Permissions model

        To use these endpoints, your API key must have the `api_keys_manage` role granted at either the account level or for specific teams.

        In addition, the following rules apply, when a "caller" API key is managing a "target" API key:

        - A caller key can only assign roles whose scopes are a subset of its own scopes. For example, a key with `viewer` and `incident_creator` can assign those roles to a target key, but cannot assign the `incident_editor` role, since it has additional scopes.
        - The same applies at the team level: A caller key with `schedules_editor` at the account level can create a target key with that role for a specific team, but a key with `schedules_editor` only for one team (Team A, say) cannot assign it at the account level or for another team, Team B.
        - The `api_keys_manage` role cannot be assigned via the API. To create a key with that role, you must go to Settings → API keys in the dashboard, and click "Add new", creating a key with the "View, create, edit, delete or rotate API keys" role (`api_keys_manage`).
        - The `Delete` endpoint does not check whether the calling API key holds the scopes of the key being deleted. However, a team-scoped key can only delete keys belonging to its teams.
        - To rotate the token of an API key, use the Rotate endpoint or by clicking "Rotate token" on any API key listed in the dashboard. The same permissions limitations apply as when creating or updating an API key.

        ## Finding role names and team IDs

        To find valid values for `role_names`, `team_ids`, and `team_role_names`, go to Settings → API keys in the dashboard. Click to either edit an existing key or create a new one, select the desired roles and teams, and then use the copy button to get hold of the role and team identifiers as JSON.

        Manages an API key: the credential an integration authenticates to incident.io with, and the roles that decide what it may do.

        ## The token

        incident.io returns a key's token when it issues one - when the key is created, and each time it is rotated - and at no other time. A read cannot recover it, so Terraform keeps it in state, in the `token` attribute. It is marked sensitive, so Terraform will not print it, but **anything that can read your state can read the token**: treat the state file as the credential it now contains, and pass `token` on to wherever it is needed through a sensitive output or variable rather than copying it around.

        A key adopted with `pulumi import` has no token, because the one it was created with is gone and cannot be re-read. Rotate it to get a token Terraform can hand on.

        ## Rotating

        `token_version` is your own counter, and changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. The plan says so, `token` becomes known after apply, and the previous token keeps working for `rotation_grace_period_minutes` so that whatever holds it has a window to pick up the new one.

        Editing a key's roles or name never rotates it: the token outlives its permissions, and a key whose scopes you have just narrowed carries on working. Rotate deliberately if a permissions change means the old token should stop being accepted.

        ## Roles

        `role_names` grants a role across the whole account. `team_role_names` grants roles only for the teams in `team_ids`, so those two go together: set both, or neither.

        incident.io will not let a key grant more than its own bearer holds, so the key running Terraform has to already have every role it assigns. It also refuses to assign `api_keys_manage` at all, which means a Terraform-managed key cannot itself manage keys.

        Neither rule is decided by this provider - which roles your own key holds is not something a configuration can know - so the plan asks incident.io whether the key it describes would be accepted. A role you cannot grant is reported when you plan, rather than part way through an apply. If that check cannot be reached the plan warns and carries on, so the rejection may still arrive at apply time.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident

        # incident.io returns an API key's token when it issues one - on create, and on each
        # rotation - and never again. Terraform keeps it in state, so anything that can read your
        # state can read the token: treat the state file as the credential it now holds.
        ci = incident.ApiKey("ci",
            name="CI deploy key",
            comments="Requested in #ask-infra, used by the deploy pipeline",
            role_names=[
                "viewer",
                "catalog_viewer",
            ])
        pulumi.export("ciApiKeyToken", ci.token)
        # Rotating is asked for by changing token_version - conventionally by incrementing it.
        # That's the change Terraform can see, since the token itself is invisible to a plan.
        # The previous token keeps working for rotation_grace_period_minutes, giving whatever
        # holds it a window to pick up the new one.
        rotated_quarterly = incident.ApiKey("rotated_quarterly",
            name="Terraform state reader",
            role_names=["viewer"],
            token_version=2,
            rotation_grace_period_minutes=60)
        # A key can be scoped to particular teams instead of the whole account. team_ids says
        # which teams, and team_role_names says what the key may do for them, so the two go
        # together: set both, or neither. An account with no account-level roles leaves
        # role_names out entirely.
        platform_team_schedules = incident.ApiKey("platform_team_schedules",
            name="Platform team schedule sync",
            team_ids=["01G0J1EXE7AXZ2C93K61WBPYEH"],
            team_role_names=[
                "schedules_editor",
                "on_call_editor",
            ])
        # Editing a key's roles never rotates it: the token outlives its permissions. Rotate
        # deliberately if narrowing a key's scopes should also stop the old token being accepted.
        narrowed = incident.ApiKey("narrowed",
            name="Read-only reporting key",
            role_names=["viewer"],
            token_version=3,
            rotation_grace_period_minutes=0)
        ```

        ## Import

        Import is supported using an `import` block or the `pulumi import` command:

        The `pulumi import` command can be used, for example:

        !/bin/bash

        Import an API key using its ID
        Replace the ID with a real ID from your incident.io organization

        ```sh
        $ pulumi import incident:index/apiKey:ApiKey example 01ABC123DEF456GHI789JKL
        ```

        An imported key has no token: the one it was issued with went to whoever created it and
        can't be read back, so `token` is null. It has no token_version either, so a config that
        sets one will rotate the key on the first apply - which is also the only way to get a
        token for a key Terraform has adopted. The plan says so before it happens.


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] comments: Freeform notes about this API key
        :param pulumi.Input[_builtins.str] name: The name of the API key, for the user's reference
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] role_names: Account-level roles to assign to the API key. These roles apply across the entire account, not scoped to specific teams. Pass an empty array if no account-level roles are needed.
        :param pulumi.Input[_builtins.int] rotation_grace_period_minutes: How long the previous token keeps working after a rotation, in minutes, giving whatever holds it a window to pick up the new one. Defaults to 30. Set it to 0 to retire the old token immediately, at the cost of breaking anything still using it. incident.io documents an hour as the longest a rotated token stays valid, so it may reject a longer period. Only read when a change to `token_version` rotates the key.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_ids: IDs of teams to scope the `team_role_names` to. If provided, `team_role_names` must also be a non-empty array, and vice versa. Pass an empty array if the key should not be scoped to any teams.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_role_names: Roles to grant for the teams specified in `team_ids`. If provided, `team_ids` must also be a non-empty array, and vice versa. Pass an empty array if no team-level roles are needed.
               
               API key role name that may be granted for team-scoped access. Possible values are: `catalog_editor`, `schedules_editor`, `schedules_reader`, `schedule_overrides_editor`, `on_call_editor`, `escalation_creator`, `api_keys_manage`, `workflows_editor`, `private_workflows_editor`, `secrets_manage`, `secrets_use`, `heartbeats_ping`, `telemetry_query_restricted`, `telemetry_data_source_update`.
        :param pulumi.Input[_builtins.int] token_version: The version of the token this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. It is your own counter, and incident.io never sees it. Leave it unset to manage a key's name and roles without ever rotating it.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: Optional[ApiKeyArgs] = None,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manage API keys for your organization.

        This API lets you view, create, update, delete, and rotate API keys programmatically, including configuring account-level roles and roles scoped to specific teams.

        Common use cases include:
        - **API key rotation**: You can issue a new access token for any API key using the `Rotate` endpoint.
        - **Managing keys at scale**: use the `Show`, `List`, `Create`, `Update`, and `Delete` endpoints to automate API key lifecycle management.

        ## Permissions model

        To use these endpoints, your API key must have the `api_keys_manage` role granted at either the account level or for specific teams.

        In addition, the following rules apply, when a "caller" API key is managing a "target" API key:

        - A caller key can only assign roles whose scopes are a subset of its own scopes. For example, a key with `viewer` and `incident_creator` can assign those roles to a target key, but cannot assign the `incident_editor` role, since it has additional scopes.
        - The same applies at the team level: A caller key with `schedules_editor` at the account level can create a target key with that role for a specific team, but a key with `schedules_editor` only for one team (Team A, say) cannot assign it at the account level or for another team, Team B.
        - The `api_keys_manage` role cannot be assigned via the API. To create a key with that role, you must go to Settings → API keys in the dashboard, and click "Add new", creating a key with the "View, create, edit, delete or rotate API keys" role (`api_keys_manage`).
        - The `Delete` endpoint does not check whether the calling API key holds the scopes of the key being deleted. However, a team-scoped key can only delete keys belonging to its teams.
        - To rotate the token of an API key, use the Rotate endpoint or by clicking "Rotate token" on any API key listed in the dashboard. The same permissions limitations apply as when creating or updating an API key.

        ## Finding role names and team IDs

        To find valid values for `role_names`, `team_ids`, and `team_role_names`, go to Settings → API keys in the dashboard. Click to either edit an existing key or create a new one, select the desired roles and teams, and then use the copy button to get hold of the role and team identifiers as JSON.

        Manages an API key: the credential an integration authenticates to incident.io with, and the roles that decide what it may do.

        ## The token

        incident.io returns a key's token when it issues one - when the key is created, and each time it is rotated - and at no other time. A read cannot recover it, so Terraform keeps it in state, in the `token` attribute. It is marked sensitive, so Terraform will not print it, but **anything that can read your state can read the token**: treat the state file as the credential it now contains, and pass `token` on to wherever it is needed through a sensitive output or variable rather than copying it around.

        A key adopted with `pulumi import` has no token, because the one it was created with is gone and cannot be re-read. Rotate it to get a token Terraform can hand on.

        ## Rotating

        `token_version` is your own counter, and changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. The plan says so, `token` becomes known after apply, and the previous token keeps working for `rotation_grace_period_minutes` so that whatever holds it has a window to pick up the new one.

        Editing a key's roles or name never rotates it: the token outlives its permissions, and a key whose scopes you have just narrowed carries on working. Rotate deliberately if a permissions change means the old token should stop being accepted.

        ## Roles

        `role_names` grants a role across the whole account. `team_role_names` grants roles only for the teams in `team_ids`, so those two go together: set both, or neither.

        incident.io will not let a key grant more than its own bearer holds, so the key running Terraform has to already have every role it assigns. It also refuses to assign `api_keys_manage` at all, which means a Terraform-managed key cannot itself manage keys.

        Neither rule is decided by this provider - which roles your own key holds is not something a configuration can know - so the plan asks incident.io whether the key it describes would be accepted. A role you cannot grant is reported when you plan, rather than part way through an apply. If that check cannot be reached the plan warns and carries on, so the rejection may still arrive at apply time.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident

        # incident.io returns an API key's token when it issues one - on create, and on each
        # rotation - and never again. Terraform keeps it in state, so anything that can read your
        # state can read the token: treat the state file as the credential it now holds.
        ci = incident.ApiKey("ci",
            name="CI deploy key",
            comments="Requested in #ask-infra, used by the deploy pipeline",
            role_names=[
                "viewer",
                "catalog_viewer",
            ])
        pulumi.export("ciApiKeyToken", ci.token)
        # Rotating is asked for by changing token_version - conventionally by incrementing it.
        # That's the change Terraform can see, since the token itself is invisible to a plan.
        # The previous token keeps working for rotation_grace_period_minutes, giving whatever
        # holds it a window to pick up the new one.
        rotated_quarterly = incident.ApiKey("rotated_quarterly",
            name="Terraform state reader",
            role_names=["viewer"],
            token_version=2,
            rotation_grace_period_minutes=60)
        # A key can be scoped to particular teams instead of the whole account. team_ids says
        # which teams, and team_role_names says what the key may do for them, so the two go
        # together: set both, or neither. An account with no account-level roles leaves
        # role_names out entirely.
        platform_team_schedules = incident.ApiKey("platform_team_schedules",
            name="Platform team schedule sync",
            team_ids=["01G0J1EXE7AXZ2C93K61WBPYEH"],
            team_role_names=[
                "schedules_editor",
                "on_call_editor",
            ])
        # Editing a key's roles never rotates it: the token outlives its permissions. Rotate
        # deliberately if narrowing a key's scopes should also stop the old token being accepted.
        narrowed = incident.ApiKey("narrowed",
            name="Read-only reporting key",
            role_names=["viewer"],
            token_version=3,
            rotation_grace_period_minutes=0)
        ```

        ## Import

        Import is supported using an `import` block or the `pulumi import` command:

        The `pulumi import` command can be used, for example:

        !/bin/bash

        Import an API key using its ID
        Replace the ID with a real ID from your incident.io organization

        ```sh
        $ pulumi import incident:index/apiKey:ApiKey example 01ABC123DEF456GHI789JKL
        ```

        An imported key has no token: the one it was issued with went to whoever created it and
        can't be read back, so `token` is null. It has no token_version either, so a config that
        sets one will rotate the key on the first apply - which is also the only way to get a
        token for a key Terraform has adopted. The plan says so before it happens.


        :param str resource_name: The name of the resource.
        :param ApiKeyArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(ApiKeyArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 comments: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 rotation_grace_period_minutes: pulumi.Input[Optional[_builtins.int]] = None,
                 team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 token_version: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = ApiKeyArgs.__new__(ApiKeyArgs)

            __props__.__dict__["comments"] = comments
            __props__.__dict__["name"] = name
            __props__.__dict__["role_names"] = role_names
            __props__.__dict__["rotation_grace_period_minutes"] = rotation_grace_period_minutes
            __props__.__dict__["team_ids"] = team_ids
            __props__.__dict__["team_role_names"] = team_role_names
            __props__.__dict__["token_version"] = token_version
            __props__.__dict__["created_at"] = None
            __props__.__dict__["last_used_at"] = None
            __props__.__dict__["token"] = None
            __props__.__dict__["token_last_issued_at"] = None
        secret_opts = pulumi.ResourceOptions(additional_secret_outputs=["token"])
        opts = pulumi.ResourceOptions.merge(opts, secret_opts)
        super(ApiKey, __self__).__init__(
            'incident:index/apiKey:ApiKey',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            comments: pulumi.Input[Optional[_builtins.str]] = None,
            created_at: pulumi.Input[Optional[_builtins.str]] = None,
            last_used_at: pulumi.Input[Optional[_builtins.str]] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            rotation_grace_period_minutes: pulumi.Input[Optional[_builtins.int]] = None,
            team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            team_role_names: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            token: pulumi.Input[Optional[_builtins.str]] = None,
            token_last_issued_at: pulumi.Input[Optional[_builtins.str]] = None,
            token_version: pulumi.Input[Optional[_builtins.int]] = None) -> 'ApiKey':
        """
        Get an existing ApiKey resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] comments: Freeform notes about this API key
        :param pulumi.Input[_builtins.str] created_at: When the API key was created
        :param pulumi.Input[_builtins.str] last_used_at: When the key was last used to authenticate a request
        :param pulumi.Input[_builtins.str] name: The name of the API key, for the user's reference
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] role_names: Account-level roles to assign to the API key. These roles apply across the entire account, not scoped to specific teams. Pass an empty array if no account-level roles are needed.
        :param pulumi.Input[_builtins.int] rotation_grace_period_minutes: How long the previous token keeps working after a rotation, in minutes, giving whatever holds it a window to pick up the new one. Defaults to 30. Set it to 0 to retire the old token immediately, at the cost of breaking anything still using it. incident.io documents an hour as the longest a rotated token stays valid, so it may reject a longer period. Only read when a change to `token_version` rotates the key.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_ids: IDs of teams to scope the `team_role_names` to. If provided, `team_role_names` must also be a non-empty array, and vice versa. Pass an empty array if the key should not be scoped to any teams.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] team_role_names: Roles to grant for the teams specified in `team_ids`. If provided, `team_ids` must also be a non-empty array, and vice versa. Pass an empty array if no team-level roles are needed.
               
               API key role name that may be granted for team-scoped access. Possible values are: `catalog_editor`, `schedules_editor`, `schedules_reader`, `schedule_overrides_editor`, `on_call_editor`, `escalation_creator`, `api_keys_manage`, `workflows_editor`, `private_workflows_editor`, `secrets_manage`, `secrets_use`, `heartbeats_ping`, `telemetry_query_restricted`, `telemetry_data_source_update`.
        :param pulumi.Input[_builtins.str] token: The bearer token to authenticate as this key, which incident.io returns only when it issues one: on create, and on each rotation. Terraform stores it, because nothing can read it back afterwards, so anything with access to your state can read it. Null for a key adopted with `terraform import`, whose token was issued before Terraform knew about it - rotate the key to get one.
        :param pulumi.Input[_builtins.str] token_last_issued_at: When the current token for this API was last issued. This is the last time the token was rotated, or when it was initially created. Older tokens may remain valid for up to an hour after they have been rotated, configured when you call the rotate endpoint.
        :param pulumi.Input[_builtins.int] token_version: The version of the token this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. It is your own counter, and incident.io never sees it. Leave it unset to manage a key's name and roles without ever rotating it.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _ApiKeyState.__new__(_ApiKeyState)

        __props__.__dict__["comments"] = comments
        __props__.__dict__["created_at"] = created_at
        __props__.__dict__["last_used_at"] = last_used_at
        __props__.__dict__["name"] = name
        __props__.__dict__["role_names"] = role_names
        __props__.__dict__["rotation_grace_period_minutes"] = rotation_grace_period_minutes
        __props__.__dict__["team_ids"] = team_ids
        __props__.__dict__["team_role_names"] = team_role_names
        __props__.__dict__["token"] = token
        __props__.__dict__["token_last_issued_at"] = token_last_issued_at
        __props__.__dict__["token_version"] = token_version
        return ApiKey(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter
    def comments(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        Freeform notes about this API key
        """
        return pulumi.get(self, "comments")

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> pulumi.Output[_builtins.str]:
        """
        When the API key was created
        """
        return pulumi.get(self, "created_at")

    @_builtins.property
    @pulumi.getter(name="lastUsedAt")
    def last_used_at(self) -> pulumi.Output[_builtins.str]:
        """
        When the key was last used to authenticate a request
        """
        return pulumi.get(self, "last_used_at")

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        The name of the API key, for the user's reference
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="roleNames")
    def role_names(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        Account-level roles to assign to the API key. These roles apply across the entire account, not scoped to specific teams. Pass an empty array if no account-level roles are needed.
        """
        return pulumi.get(self, "role_names")

    @_builtins.property
    @pulumi.getter(name="rotationGracePeriodMinutes")
    def rotation_grace_period_minutes(self) -> pulumi.Output[_builtins.int]:
        """
        How long the previous token keeps working after a rotation, in minutes, giving whatever holds it a window to pick up the new one. Defaults to 30. Set it to 0 to retire the old token immediately, at the cost of breaking anything still using it. incident.io documents an hour as the longest a rotated token stays valid, so it may reject a longer period. Only read when a change to `token_version` rotates the key.
        """
        return pulumi.get(self, "rotation_grace_period_minutes")

    @_builtins.property
    @pulumi.getter(name="teamIds")
    def team_ids(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        IDs of teams to scope the `team_role_names` to. If provided, `team_role_names` must also be a non-empty array, and vice versa. Pass an empty array if the key should not be scoped to any teams.
        """
        return pulumi.get(self, "team_ids")

    @_builtins.property
    @pulumi.getter(name="teamRoleNames")
    def team_role_names(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        Roles to grant for the teams specified in `team_ids`. If provided, `team_ids` must also be a non-empty array, and vice versa. Pass an empty array if no team-level roles are needed.

        API key role name that may be granted for team-scoped access. Possible values are: `catalog_editor`, `schedules_editor`, `schedules_reader`, `schedule_overrides_editor`, `on_call_editor`, `escalation_creator`, `api_keys_manage`, `workflows_editor`, `private_workflows_editor`, `secrets_manage`, `secrets_use`, `heartbeats_ping`, `telemetry_query_restricted`, `telemetry_data_source_update`.
        """
        return pulumi.get(self, "team_role_names")

    @_builtins.property
    @pulumi.getter
    def token(self) -> pulumi.Output[_builtins.str]:
        """
        The bearer token to authenticate as this key, which incident.io returns only when it issues one: on create, and on each rotation. Terraform stores it, because nothing can read it back afterwards, so anything with access to your state can read it. Null for a key adopted with `terraform import`, whose token was issued before Terraform knew about it - rotate the key to get one.
        """
        return pulumi.get(self, "token")

    @_builtins.property
    @pulumi.getter(name="tokenLastIssuedAt")
    def token_last_issued_at(self) -> pulumi.Output[_builtins.str]:
        """
        When the current token for this API was last issued. This is the last time the token was rotated, or when it was initially created. Older tokens may remain valid for up to an hour after they have been rotated, configured when you call the rotate endpoint.
        """
        return pulumi.get(self, "token_last_issued_at")

    @_builtins.property
    @pulumi.getter(name="tokenVersion")
    def token_version(self) -> pulumi.Output[Optional[_builtins.int]]:
        """
        The version of the token this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the key. It is your own counter, and incident.io never sees it. Leave it unset to manage a key's name and roles without ever rotating it.
        """
        return pulumi.get(self, "token_version")

