# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs

__all__ = [
    'GetAlertSourcesResult',
    'AwaitableGetAlertSourcesResult',
    'get_alert_sources',
    'get_alert_sources_output',
]

@pulumi.output_type
class GetAlertSourcesResult:
    """
    A collection of values returned by getAlertSources.
    """
    def __init__(__self__, alert_sources=None, source_type=None):
        if alert_sources and not isinstance(alert_sources, list):
            raise TypeError("Expected argument 'alert_sources' to be a list")
        pulumi.set(__self__, "alert_sources", alert_sources)
        if source_type and not isinstance(source_type, str):
            raise TypeError("Expected argument 'source_type' to be a str")
        pulumi.set(__self__, "source_type", source_type)

    @_builtins.property
    @pulumi.getter(name="alertSources")
    def alert_sources(self) -> Sequence['outputs.GetAlertSourcesAlertSourceResult']:
        """
        List of alert sources matching the specified criteria. If no filters are provided, all alert sources are returned.
        """
        return pulumi.get(self, "alert_sources")

    @_builtins.property
    @pulumi.getter(name="sourceType")
    def source_type(self) -> Optional[_builtins.str]:
        """
        Filter alert sources by source type. If provided, only alert sources of this type will be returned. Possible values are: `alertmanager`, `app_optics`, `azure_monitor`, `azure_devops`, `big_panda`, `bugsnag`, `checkly`, `chronosphere`, `cloudwatch`, `cloudflare`, `coralogix`, `cronitor`, `crowdstrike_falcon`, `dash0`, `datadog`, `dynatrace`, `elasticsearch`, `email`, `expel`, `github_issue`, `google_cloud`, `grafana`, `heartbeat`, `http`, `http_custom`, `honeycomb`, `icinga2`, `incoming_calls`, `jira`, `jsm`, `monte_carlo`, `nagios`, `new_relic`, `opsgenie`, `prtg`, `pager_duty`, `panther`, `pingdom`, `runscope`, `sns`, `sentry`, `sentry_metric`, `service_now`, `splunk`, `status_cake`, `status_page_views`, `sumo_logic`, `uptime`, `vercel`, `wiz`, `zendesk`.
        """
        return pulumi.get(self, "source_type")


class AwaitableGetAlertSourcesResult(GetAlertSourcesResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetAlertSourcesResult(
            alert_sources=self.alert_sources,
            source_type=self.source_type)


def get_alert_sources(source_type: Optional[_builtins.str] = None,
                      opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetAlertSourcesResult:
    """
    Configure your alert sources in incident.io.

    Alert sources are the systems that send alerts to incident.io, which can then be routed to the right people and teams.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident
    import pulumi_std as std

    def can_(fn):
        try:
            _result = fn()
            return True
        except:
            return False


    # Example 1: Get all alert sources
    all = incident.get_alert_sources()
    # Example 2: Filter by source type
    webhooks_only = incident.get_alert_sources(source_type="http")
    # Example 3: Filter by different source types
    email_sources = incident.get_alert_sources(source_type="email")
    jira_sources = incident.get_alert_sources(source_type="jira")
    pulumi.export("allAlertSourceNames", [source.name for source in all.alert_sources])
    pulumi.export("allAlertSourceIds", [source.id for source in all.alert_sources])
    pulumi.export("webhookAlertSourcesCount", len(webhooks_only.alert_sources))
    pulumi.export("webhookAlertSourceDetails", [{
        "id": source.id,
        "name": source.name,
        "sourceType": source.source_type,
        "secretToken": source.secret_token,
    } for source in webhooks_only.alert_sources])
    # Group alert sources by type
    alert_sources_by_type = {source.source_type: source for source in all.alert_sources}
    # Get only webhook sources with specific naming pattern
    production_webhooks = [source for source in webhooks_only.alert_sources if can_(lambda: std.regex(pattern="^Production",
        string=source.name)["result"])]
    # Create a map of alert source names to IDs
    alert_source_name_to_id = {source.name: source.id for source in all.alert_sources}
    pulumi.export("alertSourcesByType", alert_sources_by_type)
    pulumi.export("productionWebhookCount", len(production_webhooks))
    pulumi.export("alertSourceLookup", alert_source_name_to_id)
    ```


    :param _builtins.str source_type: Filter alert sources by source type. If provided, only alert sources of this type will be returned. Possible values are: `alertmanager`, `app_optics`, `azure_monitor`, `azure_devops`, `big_panda`, `bugsnag`, `checkly`, `chronosphere`, `cloudwatch`, `cloudflare`, `coralogix`, `cronitor`, `crowdstrike_falcon`, `dash0`, `datadog`, `dynatrace`, `elasticsearch`, `email`, `expel`, `github_issue`, `google_cloud`, `grafana`, `heartbeat`, `http`, `http_custom`, `honeycomb`, `icinga2`, `incoming_calls`, `jira`, `jsm`, `monte_carlo`, `nagios`, `new_relic`, `opsgenie`, `prtg`, `pager_duty`, `panther`, `pingdom`, `runscope`, `sns`, `sentry`, `sentry_metric`, `service_now`, `splunk`, `status_cake`, `status_page_views`, `sumo_logic`, `uptime`, `vercel`, `wiz`, `zendesk`.
    """
    __args__ = dict()
    __args__['sourceType'] = source_type
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('incident:index/getAlertSources:getAlertSources', __args__, opts=opts, typ=GetAlertSourcesResult).value

    return AwaitableGetAlertSourcesResult(
        alert_sources=pulumi.get(__ret__, 'alert_sources'),
        source_type=pulumi.get(__ret__, 'source_type'))
def get_alert_sources_output(source_type: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                             opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetAlertSourcesResult]:
    """
    Configure your alert sources in incident.io.

    Alert sources are the systems that send alerts to incident.io, which can then be routed to the right people and teams.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident
    import pulumi_std as std

    def can_(fn):
        try:
            _result = fn()
            return True
        except:
            return False


    # Example 1: Get all alert sources
    all = incident.get_alert_sources()
    # Example 2: Filter by source type
    webhooks_only = incident.get_alert_sources(source_type="http")
    # Example 3: Filter by different source types
    email_sources = incident.get_alert_sources(source_type="email")
    jira_sources = incident.get_alert_sources(source_type="jira")
    pulumi.export("allAlertSourceNames", [source.name for source in all.alert_sources])
    pulumi.export("allAlertSourceIds", [source.id for source in all.alert_sources])
    pulumi.export("webhookAlertSourcesCount", len(webhooks_only.alert_sources))
    pulumi.export("webhookAlertSourceDetails", [{
        "id": source.id,
        "name": source.name,
        "sourceType": source.source_type,
        "secretToken": source.secret_token,
    } for source in webhooks_only.alert_sources])
    # Group alert sources by type
    alert_sources_by_type = {source.source_type: source for source in all.alert_sources}
    # Get only webhook sources with specific naming pattern
    production_webhooks = [source for source in webhooks_only.alert_sources if can_(lambda: std.regex(pattern="^Production",
        string=source.name)["result"])]
    # Create a map of alert source names to IDs
    alert_source_name_to_id = {source.name: source.id for source in all.alert_sources}
    pulumi.export("alertSourcesByType", alert_sources_by_type)
    pulumi.export("productionWebhookCount", len(production_webhooks))
    pulumi.export("alertSourceLookup", alert_source_name_to_id)
    ```


    :param _builtins.str source_type: Filter alert sources by source type. If provided, only alert sources of this type will be returned. Possible values are: `alertmanager`, `app_optics`, `azure_monitor`, `azure_devops`, `big_panda`, `bugsnag`, `checkly`, `chronosphere`, `cloudwatch`, `cloudflare`, `coralogix`, `cronitor`, `crowdstrike_falcon`, `dash0`, `datadog`, `dynatrace`, `elasticsearch`, `email`, `expel`, `github_issue`, `google_cloud`, `grafana`, `heartbeat`, `http`, `http_custom`, `honeycomb`, `icinga2`, `incoming_calls`, `jira`, `jsm`, `monte_carlo`, `nagios`, `new_relic`, `opsgenie`, `prtg`, `pager_duty`, `panther`, `pingdom`, `runscope`, `sns`, `sentry`, `sentry_metric`, `service_now`, `splunk`, `status_cake`, `status_page_views`, `sumo_logic`, `uptime`, `vercel`, `wiz`, `zendesk`.
    """
    __args__ = dict()
    __args__['sourceType'] = source_type
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('incident:index/getAlertSources:getAlertSources', __args__, opts=opts, typ=GetAlertSourcesResult)
    return __ret__.apply(lambda __response__: GetAlertSourcesResult(
        alert_sources=pulumi.get(__response__, 'alert_sources'),
        source_type=pulumi.get(__response__, 'source_type')))
