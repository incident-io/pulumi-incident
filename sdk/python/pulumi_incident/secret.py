# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['SecretArgs', 'Secret']

@pulumi.input_type
class SecretArgs:
    def __init__(__self__, *,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 value_wo: pulumi.Input[Optional[_builtins.str]] = None,
                 value_wo_version: pulumi.Input[Optional[_builtins.int]] = None):
        """
        The set of arguments for constructing a Secret resource.

        :param pulumi.Input[_builtins.str] description: Optional description of what this secret is for
        :param pulumi.Input[_builtins.str] name: Human-readable name, unique within the organisation amongst unarchived secrets. At most 1024 bytes, counted in bytes rather than characters, so a name using multi-byte characters reaches the limit sooner.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        :param pulumi.Input[_builtins.str] value_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               The secret's plaintext value, as a [write-only attribute](https://developer.hashicorp.com/terraform/language/resources/ephemeral/write-only): it is sent to incident.io and never written to state or to a plan file. Required when creating a secret. Changing it alone has no effect, as Terraform cannot see that it changed: change `value_wo_version` to rotate the secret.
        :param pulumi.Input[_builtins.int] value_wo_version: The version of `value_wo` this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the secret to the current `value_wo`. It is your own counter, unrelated to the `version` incident.io reports.
        """
        if description is not None:
            pulumi.set(__self__, "description", description)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if owning_team_ids is not None:
            pulumi.set(__self__, "owning_team_ids", owning_team_ids)
        if value_wo is not None:
            pulumi.set(__self__, "value_wo", value_wo)
        if value_wo_version is not None:
            pulumi.set(__self__, "value_wo_version", value_wo_version)

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Optional description of what this secret is for
        """
        return pulumi.get(self, "description")

    @description.setter
    def description(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "description", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Human-readable name, unique within the organisation amongst unarchived secrets. At most 1024 bytes, counted in bytes rather than characters, so a name using multi-byte characters reaches the limit sooner.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="owningTeamIds")
    def owning_team_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        """
        return pulumi.get(self, "owning_team_ids")

    @owning_team_ids.setter
    def owning_team_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "owning_team_ids", value)

    @_builtins.property
    @pulumi.getter(name="valueWo")
    def value_wo(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
        The secret's plaintext value, as a [write-only attribute](https://developer.hashicorp.com/terraform/language/resources/ephemeral/write-only): it is sent to incident.io and never written to state or to a plan file. Required when creating a secret. Changing it alone has no effect, as Terraform cannot see that it changed: change `value_wo_version` to rotate the secret.
        """
        return pulumi.get(self, "value_wo")

    @value_wo.setter
    def value_wo(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "value_wo", value)

    @_builtins.property
    @pulumi.getter(name="valueWoVersion")
    def value_wo_version(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The version of `value_wo` this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the secret to the current `value_wo`. It is your own counter, unrelated to the `version` incident.io reports.
        """
        return pulumi.get(self, "value_wo_version")

    @value_wo_version.setter
    def value_wo_version(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "value_wo_version", value)


@pulumi.input_type
class _SecretState:
    def __init__(__self__, *,
                 created_at: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 last_four_chars: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 updated_at: pulumi.Input[Optional[_builtins.str]] = None,
                 value_wo: pulumi.Input[Optional[_builtins.str]] = None,
                 value_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
                 version: pulumi.Input[Optional[_builtins.int]] = None):
        """
        Input properties used for looking up and filtering Secret resources.

        :param pulumi.Input[_builtins.str] created_at: When this secret was created.
        :param pulumi.Input[_builtins.str] description: Optional description of what this secret is for
        :param pulumi.Input[_builtins.str] last_four_chars: The last four characters of the current value, for masked display. Absent when the value is four characters or shorter.
        :param pulumi.Input[_builtins.str] name: Human-readable name, unique within the organisation amongst unarchived secrets. At most 1024 bytes, counted in bytes rather than characters, so a name using multi-byte characters reaches the limit sooner.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        :param pulumi.Input[_builtins.str] updated_at: When this secret was last changed, which includes being rotated as well as having its metadata edited.
        :param pulumi.Input[_builtins.str] value_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               The secret's plaintext value, as a [write-only attribute](https://developer.hashicorp.com/terraform/language/resources/ephemeral/write-only): it is sent to incident.io and never written to state or to a plan file. Required when creating a secret. Changing it alone has no effect, as Terraform cannot see that it changed: change `value_wo_version` to rotate the secret.
        :param pulumi.Input[_builtins.int] value_wo_version: The version of `value_wo` this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the secret to the current `value_wo`. It is your own counter, unrelated to the `version` incident.io reports.
        :param pulumi.Input[_builtins.int] version: The current version number, incremented on each rotation
        """
        if created_at is not None:
            pulumi.set(__self__, "created_at", created_at)
        if description is not None:
            pulumi.set(__self__, "description", description)
        if last_four_chars is not None:
            pulumi.set(__self__, "last_four_chars", last_four_chars)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if owning_team_ids is not None:
            pulumi.set(__self__, "owning_team_ids", owning_team_ids)
        if updated_at is not None:
            pulumi.set(__self__, "updated_at", updated_at)
        if value_wo is not None:
            pulumi.set(__self__, "value_wo", value_wo)
        if value_wo_version is not None:
            pulumi.set(__self__, "value_wo_version", value_wo_version)
        if version is not None:
            pulumi.set(__self__, "version", version)

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        When this secret was created.
        """
        return pulumi.get(self, "created_at")

    @created_at.setter
    def created_at(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "created_at", value)

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Optional description of what this secret is for
        """
        return pulumi.get(self, "description")

    @description.setter
    def description(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "description", value)

    @_builtins.property
    @pulumi.getter(name="lastFourChars")
    def last_four_chars(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The last four characters of the current value, for masked display. Absent when the value is four characters or shorter.
        """
        return pulumi.get(self, "last_four_chars")

    @last_four_chars.setter
    def last_four_chars(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "last_four_chars", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Human-readable name, unique within the organisation amongst unarchived secrets. At most 1024 bytes, counted in bytes rather than characters, so a name using multi-byte characters reaches the limit sooner.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="owningTeamIds")
    def owning_team_ids(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        """
        return pulumi.get(self, "owning_team_ids")

    @owning_team_ids.setter
    def owning_team_ids(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "owning_team_ids", value)

    @_builtins.property
    @pulumi.getter(name="updatedAt")
    def updated_at(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        When this secret was last changed, which includes being rotated as well as having its metadata edited.
        """
        return pulumi.get(self, "updated_at")

    @updated_at.setter
    def updated_at(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "updated_at", value)

    @_builtins.property
    @pulumi.getter(name="valueWo")
    def value_wo(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
        The secret's plaintext value, as a [write-only attribute](https://developer.hashicorp.com/terraform/language/resources/ephemeral/write-only): it is sent to incident.io and never written to state or to a plan file. Required when creating a secret. Changing it alone has no effect, as Terraform cannot see that it changed: change `value_wo_version` to rotate the secret.
        """
        return pulumi.get(self, "value_wo")

    @value_wo.setter
    def value_wo(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "value_wo", value)

    @_builtins.property
    @pulumi.getter(name="valueWoVersion")
    def value_wo_version(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The version of `value_wo` this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the secret to the current `value_wo`. It is your own counter, unrelated to the `version` incident.io reports.
        """
        return pulumi.get(self, "value_wo_version")

    @value_wo_version.setter
    def value_wo_version(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "value_wo_version", value)

    @_builtins.property
    @pulumi.getter
    def version(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The current version number, incremented on each rotation
        """
        return pulumi.get(self, "version")

    @version.setter
    def version(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "version", value)


@pulumi.type_token("incident:index/secret:Secret")
class Secret(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 value_wo: pulumi.Input[Optional[_builtins.str]] = None,
                 value_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        """
        Manage secrets: named credentials that workflows can reference. A secret's value can be set and rotated but is never returned by the API.

        A secret's value is write-only: incident.io stores it encrypted and never returns it, so
        Terraform cannot read it back to compare against your configuration. Set it with `value_wo`, a
        write-only attribute,
        which needs Terraform 1.11 or OpenTofu 1.11 and above. The value is never written to state or to a
        plan file.

        Because the value is invisible to Terraform, changing `value_wo` on its own does nothing. Rotating a
        secret means changing `value_wo_version` as well, conventionally by incrementing it: that is the
        change Terraform can see, and it is what asks incident.io to rotate. A rotation replaces the value
        and advances `version`; the previous value is retired and cannot be recovered.

        Both value attributes are optional after the secret exists, so Terraform can own a secret's name,
        description and owning teams while something else rotates it. Leave them unset and the value is
        never touched.

        ## Example - Manage a secret

        ```python
        import pulumi
        import pulumi_incident as incident

        config = pulumi.Config()
        pagerduty_webhook_token = config.require("pagerdutyWebhookToken")
        pagerduty_webhook_token_secret = incident.Secret("pagerduty_webhook_token",
            name="PagerDuty webhook token",
            description="Auth token for the PagerDuty outgoing webhook",
            value_wo=pagerduty_webhook_token,
            value_wo_version=1)
        billing_api_key = config.require("billingApiKey")
        billing_api_key_secret = incident.Secret("billing_api_key",
            name="Billing API key",
            value_wo=billing_api_key,
            value_wo_version=1,
            owning_team_ids=["01G0J1EXE7AXZ2C93K61WBPYEH"])
        # Terraform doesn't have to own the value. Leave both value attributes off an existing
        # secret and Terraform manages its name, description and owning teams while something
        # else - the dashboard, or a rotation job - looks after the value.
        rotated_elsewhere = incident.Secret("rotated_elsewhere",
            name="Datadog API key",
            description="Rotated nightly by our key-rotation job")
        ```

        ## Example - Use a secret in a workflow's webhook step

        ```python
        import pulumi
        import pulumi_incident as incident

        config = pulumi.Config()
        partner_webhook_token = config.require("partnerWebhookToken")
        partner_webhook_token_secret = incident.Secret("partner_webhook_token",
            name="Partner webhook token",
            description="Bearer token for the partner's incident webhook",
            value_wo=partner_webhook_token,
            value_wo_version=1)
        # The first way: put the secret's value inside a field's text.
        #
        # A field whose feature set ends in `_with_secrets` - which the webhook step's headers do -
        # accepts a variable naming a secret by ID, and incident.io substitutes the value when the
        # workflow runs. Write it as `{{ <secret id> }}` and reference the secret's `id`, never its
        # value: Terraform is arranging for the value to be used, not handling it.
        authorization_header = incident.get_rich_text_output(markdown=partner_webhook_token_secret.id.apply(lambda id: f"Authorization: Bearer {{{{ {id} }}}}"),
            feature_set="plain_single_line_with_secrets")
        content_type_header = incident.get_rich_text(markdown="Content-Type: application/json",
            feature_set="plain_single_line_with_secrets")
        webhook_endpoint = incident.get_rich_text(markdown="https://partner.example.com/hooks/incident",
            feature_set="plain_single_line")
        webhook_body = incident.get_rich_text(markdown="{{ incident.name }} is now {{ incident.status }} at severity {{ incident.severity }}.\\n",
            feature_set="rich")
        notify_partner = incident.Workflow("notify_partner",
            name="Notify our partner of incident updates",
            trigger="incident.updated",
            expressions=[],
            condition_groups=[],
            steps=[{
                "id": "01K5TSNDBF7KBZY9AFDKYXJM3N",
                "name": "webhook.send",
                "param_bindings": [
                    {
                        "value_literal": webhook_endpoint.json,
                    },
                    {
                        "value_literal": "POST",
                    },
                    {
                        "values": [
                            authorization_header.json,
                            content_type_header.json,
                        ],
                    },
                    {
                        "value_literal": webhook_body.json,
                    },
                    {
                        "value_literal": partner_webhook_token_secret.id,
                    },
                    {},
                    {},
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Import

        Import is supported using an `import` block or the `pulumi import` command:

        The `pulumi import` command can be used, for example:

        !/bin/bash

        Import a secret using its ID
        Replace the ID with a real ID from your incident.io organization

        ```sh
        $ pulumi import incident:index/secret:Secret example 01ABC123DEF456GHI789JKL
        ```

        An imported secret has no value_wo_version, because the value it already holds didn't
        come from your configuration. If your config sets value_wo and value_wo_version, the
        first apply after importing will rotate the secret to that value - the plan says so.
        To adopt a secret without touching its value, leave both attributes unset.


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] description: Optional description of what this secret is for
        :param pulumi.Input[_builtins.str] name: Human-readable name, unique within the organisation amongst unarchived secrets. At most 1024 bytes, counted in bytes rather than characters, so a name using multi-byte characters reaches the limit sooner.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        :param pulumi.Input[_builtins.str] value_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               The secret's plaintext value, as a [write-only attribute](https://developer.hashicorp.com/terraform/language/resources/ephemeral/write-only): it is sent to incident.io and never written to state or to a plan file. Required when creating a secret. Changing it alone has no effect, as Terraform cannot see that it changed: change `value_wo_version` to rotate the secret.
        :param pulumi.Input[_builtins.int] value_wo_version: The version of `value_wo` this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the secret to the current `value_wo`. It is your own counter, unrelated to the `version` incident.io reports.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: Optional[SecretArgs] = None,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manage secrets: named credentials that workflows can reference. A secret's value can be set and rotated but is never returned by the API.

        A secret's value is write-only: incident.io stores it encrypted and never returns it, so
        Terraform cannot read it back to compare against your configuration. Set it with `value_wo`, a
        write-only attribute,
        which needs Terraform 1.11 or OpenTofu 1.11 and above. The value is never written to state or to a
        plan file.

        Because the value is invisible to Terraform, changing `value_wo` on its own does nothing. Rotating a
        secret means changing `value_wo_version` as well, conventionally by incrementing it: that is the
        change Terraform can see, and it is what asks incident.io to rotate. A rotation replaces the value
        and advances `version`; the previous value is retired and cannot be recovered.

        Both value attributes are optional after the secret exists, so Terraform can own a secret's name,
        description and owning teams while something else rotates it. Leave them unset and the value is
        never touched.

        ## Example - Manage a secret

        ```python
        import pulumi
        import pulumi_incident as incident

        config = pulumi.Config()
        pagerduty_webhook_token = config.require("pagerdutyWebhookToken")
        pagerduty_webhook_token_secret = incident.Secret("pagerduty_webhook_token",
            name="PagerDuty webhook token",
            description="Auth token for the PagerDuty outgoing webhook",
            value_wo=pagerduty_webhook_token,
            value_wo_version=1)
        billing_api_key = config.require("billingApiKey")
        billing_api_key_secret = incident.Secret("billing_api_key",
            name="Billing API key",
            value_wo=billing_api_key,
            value_wo_version=1,
            owning_team_ids=["01G0J1EXE7AXZ2C93K61WBPYEH"])
        # Terraform doesn't have to own the value. Leave both value attributes off an existing
        # secret and Terraform manages its name, description and owning teams while something
        # else - the dashboard, or a rotation job - looks after the value.
        rotated_elsewhere = incident.Secret("rotated_elsewhere",
            name="Datadog API key",
            description="Rotated nightly by our key-rotation job")
        ```

        ## Example - Use a secret in a workflow's webhook step

        ```python
        import pulumi
        import pulumi_incident as incident

        config = pulumi.Config()
        partner_webhook_token = config.require("partnerWebhookToken")
        partner_webhook_token_secret = incident.Secret("partner_webhook_token",
            name="Partner webhook token",
            description="Bearer token for the partner's incident webhook",
            value_wo=partner_webhook_token,
            value_wo_version=1)
        # The first way: put the secret's value inside a field's text.
        #
        # A field whose feature set ends in `_with_secrets` - which the webhook step's headers do -
        # accepts a variable naming a secret by ID, and incident.io substitutes the value when the
        # workflow runs. Write it as `{{ <secret id> }}` and reference the secret's `id`, never its
        # value: Terraform is arranging for the value to be used, not handling it.
        authorization_header = incident.get_rich_text_output(markdown=partner_webhook_token_secret.id.apply(lambda id: f"Authorization: Bearer {{{{ {id} }}}}"),
            feature_set="plain_single_line_with_secrets")
        content_type_header = incident.get_rich_text(markdown="Content-Type: application/json",
            feature_set="plain_single_line_with_secrets")
        webhook_endpoint = incident.get_rich_text(markdown="https://partner.example.com/hooks/incident",
            feature_set="plain_single_line")
        webhook_body = incident.get_rich_text(markdown="{{ incident.name }} is now {{ incident.status }} at severity {{ incident.severity }}.\\n",
            feature_set="rich")
        notify_partner = incident.Workflow("notify_partner",
            name="Notify our partner of incident updates",
            trigger="incident.updated",
            expressions=[],
            condition_groups=[],
            steps=[{
                "id": "01K5TSNDBF7KBZY9AFDKYXJM3N",
                "name": "webhook.send",
                "param_bindings": [
                    {
                        "value_literal": webhook_endpoint.json,
                    },
                    {
                        "value_literal": "POST",
                    },
                    {
                        "values": [
                            authorization_header.json,
                            content_type_header.json,
                        ],
                    },
                    {
                        "value_literal": webhook_body.json,
                    },
                    {
                        "value_literal": partner_webhook_token_secret.id,
                    },
                    {},
                    {},
                ],
            }],
            once_fors=["incident"],
            private_incident_scope="none",
            continue_on_step_error=False,
            runs_on_incidents="newly_created_and_active",
            runs_on_incident_modes=["standard"],
            state="draft")
        ```

        ## Import

        Import is supported using an `import` block or the `pulumi import` command:

        The `pulumi import` command can be used, for example:

        !/bin/bash

        Import a secret using its ID
        Replace the ID with a real ID from your incident.io organization

        ```sh
        $ pulumi import incident:index/secret:Secret example 01ABC123DEF456GHI789JKL
        ```

        An imported secret has no value_wo_version, because the value it already holds didn't
        come from your configuration. If your config sets value_wo and value_wo_version, the
        first apply after importing will rotate the secret to that value - the plan says so.
        To adopt a secret without touching its value, leave both attributes unset.


        :param str resource_name: The name of the resource.
        :param SecretArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(SecretArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 value_wo: pulumi.Input[Optional[_builtins.str]] = None,
                 value_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = SecretArgs.__new__(SecretArgs)

            __props__.__dict__["description"] = description
            __props__.__dict__["name"] = name
            __props__.__dict__["owning_team_ids"] = owning_team_ids
            __props__.__dict__["value_wo"] = None if value_wo is None else pulumi.Output.secret(value_wo)
            __props__.__dict__["value_wo_version"] = value_wo_version
            __props__.__dict__["created_at"] = None
            __props__.__dict__["last_four_chars"] = None
            __props__.__dict__["updated_at"] = None
            __props__.__dict__["version"] = None
        secret_opts = pulumi.ResourceOptions(additional_secret_outputs=["valueWo"])
        opts = pulumi.ResourceOptions.merge(opts, secret_opts)
        super(Secret, __self__).__init__(
            'incident:index/secret:Secret',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            created_at: pulumi.Input[Optional[_builtins.str]] = None,
            description: pulumi.Input[Optional[_builtins.str]] = None,
            last_four_chars: pulumi.Input[Optional[_builtins.str]] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            owning_team_ids: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            updated_at: pulumi.Input[Optional[_builtins.str]] = None,
            value_wo: pulumi.Input[Optional[_builtins.str]] = None,
            value_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
            version: pulumi.Input[Optional[_builtins.int]] = None) -> 'Secret':
        """
        Get an existing Secret resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] created_at: When this secret was created.
        :param pulumi.Input[_builtins.str] description: Optional description of what this secret is for
        :param pulumi.Input[_builtins.str] last_four_chars: The last four characters of the current value, for masked display. Absent when the value is four characters or shorter.
        :param pulumi.Input[_builtins.str] name: Human-readable name, unique within the organisation amongst unarchived secrets. At most 1024 bytes, counted in bytes rather than characters, so a name using multi-byte characters reaches the limit sooner.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] owning_team_ids: IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        :param pulumi.Input[_builtins.str] updated_at: When this secret was last changed, which includes being rotated as well as having its metadata edited.
        :param pulumi.Input[_builtins.str] value_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               The secret's plaintext value, as a [write-only attribute](https://developer.hashicorp.com/terraform/language/resources/ephemeral/write-only): it is sent to incident.io and never written to state or to a plan file. Required when creating a secret. Changing it alone has no effect, as Terraform cannot see that it changed: change `value_wo_version` to rotate the secret.
        :param pulumi.Input[_builtins.int] value_wo_version: The version of `value_wo` this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the secret to the current `value_wo`. It is your own counter, unrelated to the `version` incident.io reports.
        :param pulumi.Input[_builtins.int] version: The current version number, incremented on each rotation
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _SecretState.__new__(_SecretState)

        __props__.__dict__["created_at"] = created_at
        __props__.__dict__["description"] = description
        __props__.__dict__["last_four_chars"] = last_four_chars
        __props__.__dict__["name"] = name
        __props__.__dict__["owning_team_ids"] = owning_team_ids
        __props__.__dict__["updated_at"] = updated_at
        __props__.__dict__["value_wo"] = value_wo
        __props__.__dict__["value_wo_version"] = value_wo_version
        __props__.__dict__["version"] = version
        return Secret(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> pulumi.Output[_builtins.str]:
        """
        When this secret was created.
        """
        return pulumi.get(self, "created_at")

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        Optional description of what this secret is for
        """
        return pulumi.get(self, "description")

    @_builtins.property
    @pulumi.getter(name="lastFourChars")
    def last_four_chars(self) -> pulumi.Output[_builtins.str]:
        """
        The last four characters of the current value, for masked display. Absent when the value is four characters or shorter.
        """
        return pulumi.get(self, "last_four_chars")

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        Human-readable name, unique within the organisation amongst unarchived secrets. At most 1024 bytes, counted in bytes rather than characters, so a name using multi-byte characters reaches the limit sooner.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="owningTeamIds")
    def owning_team_ids(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        """
        return pulumi.get(self, "owning_team_ids")

    @_builtins.property
    @pulumi.getter(name="updatedAt")
    def updated_at(self) -> pulumi.Output[_builtins.str]:
        """
        When this secret was last changed, which includes being rotated as well as having its metadata edited.
        """
        return pulumi.get(self, "updated_at")

    @_builtins.property
    @pulumi.getter(name="valueWo")
    def value_wo(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
        The secret's plaintext value, as a [write-only attribute](https://developer.hashicorp.com/terraform/language/resources/ephemeral/write-only): it is sent to incident.io and never written to state or to a plan file. Required when creating a secret. Changing it alone has no effect, as Terraform cannot see that it changed: change `value_wo_version` to rotate the secret.
        """
        return pulumi.get(self, "value_wo")

    @_builtins.property
    @pulumi.getter(name="valueWoVersion")
    def value_wo_version(self) -> pulumi.Output[Optional[_builtins.int]]:
        """
        The version of `value_wo` this configuration holds. Terraform stores this number, so changing it - conventionally by incrementing it - is what asks incident.io to rotate the secret to the current `value_wo`. It is your own counter, unrelated to the `version` incident.io reports.
        """
        return pulumi.get(self, "value_wo_version")

    @_builtins.property
    @pulumi.getter
    def version(self) -> pulumi.Output[_builtins.int]:
        """
        The current version number, incremented on each rotation
        """
        return pulumi.get(self, "version")

