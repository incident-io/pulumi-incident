# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = [
    'GetSecretResult',
    'AwaitableGetSecretResult',
    'get_secret',
    'get_secret_output',
]

@pulumi.output_type
class GetSecretResult:
    """
    A collection of values returned by getSecret.
    """
    def __init__(__self__, created_at=None, description=None, id=None, last_four_chars=None, name=None, owning_team_ids=None, updated_at=None, version=None):
        if created_at and not isinstance(created_at, str):
            raise TypeError("Expected argument 'created_at' to be a str")
        pulumi.set(__self__, "created_at", created_at)
        if description and not isinstance(description, str):
            raise TypeError("Expected argument 'description' to be a str")
        pulumi.set(__self__, "description", description)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if last_four_chars and not isinstance(last_four_chars, str):
            raise TypeError("Expected argument 'last_four_chars' to be a str")
        pulumi.set(__self__, "last_four_chars", last_four_chars)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if owning_team_ids and not isinstance(owning_team_ids, list):
            raise TypeError("Expected argument 'owning_team_ids' to be a list")
        pulumi.set(__self__, "owning_team_ids", owning_team_ids)
        if updated_at and not isinstance(updated_at, str):
            raise TypeError("Expected argument 'updated_at' to be a str")
        pulumi.set(__self__, "updated_at", updated_at)
        if version and not isinstance(version, int):
            raise TypeError("Expected argument 'version' to be a int")
        pulumi.set(__self__, "version", version)

    @_builtins.property
    @pulumi.getter(name="createdAt")
    def created_at(self) -> _builtins.str:
        """
        When this secret was created.
        """
        return pulumi.get(self, "created_at")

    @_builtins.property
    @pulumi.getter
    def description(self) -> _builtins.str:
        """
        Optional description of what this secret is for
        """
        return pulumi.get(self, "description")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Unique identifier for this secret
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter(name="lastFourChars")
    def last_four_chars(self) -> _builtins.str:
        """
        The last four characters of the current value, for masked display. Absent when the value is four characters or shorter.
        """
        return pulumi.get(self, "last_four_chars")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        Human-readable name, unique within the organisation amongst unarchived secrets
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="owningTeamIds")
    def owning_team_ids(self) -> Sequence[_builtins.str]:
        """
        IDs of the teams that own this secret. Empty means the secret is owned by the whole organisation.
        """
        return pulumi.get(self, "owning_team_ids")

    @_builtins.property
    @pulumi.getter(name="updatedAt")
    def updated_at(self) -> _builtins.str:
        """
        When this secret was last changed, which includes being rotated as well as having its metadata edited.
        """
        return pulumi.get(self, "updated_at")

    @_builtins.property
    @pulumi.getter
    def version(self) -> _builtins.int:
        """
        The current version number, incremented on each rotation
        """
        return pulumi.get(self, "version")


class AwaitableGetSecretResult(GetSecretResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetSecretResult(
            created_at=self.created_at,
            description=self.description,
            id=self.id,
            last_four_chars=self.last_four_chars,
            name=self.name,
            owning_team_ids=self.owning_team_ids,
            updated_at=self.updated_at,
            version=self.version)


def get_secret(id: Optional[_builtins.str] = None,
               name: Optional[_builtins.str] = None,
               opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetSecretResult:
    """
    Manage secrets: named credentials that workflows can reference. A secret's value can be set and rotated but is never returned by the API.

    Use this data source to look up an existing secret, either by `id` or by `name`. This is how you reference a secret somebody created in the dashboard without managing it as an `Secret` resource. Set exactly one of the two lookup attributes; setting both, or neither, is rejected at plan time.

    A secret's value is never returned by the API, so there is no value attribute here: `last_four_chars` and `version` are all a read can tell you about it.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up an existing secret by name, such as one created in the dashboard.
    pagerduty_webhook_token = incident.get_secret(name="PagerDuty webhook token")
    # Or by ID, to reference a secret another module manages.
    billing_api_key = incident.get_secret(id="01G0J1EXE7AXZ2C93K61WBPYEH")
    pulumi.export("pagerdutyTokenVersion", pagerduty_webhook_token.version)
    ```


    :param _builtins.str id: Unique identifier for this secret
    :param _builtins.str name: Human-readable name, unique within the organisation amongst unarchived secrets
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('incident:index/getSecret:getSecret', __args__, opts=opts, typ=GetSecretResult).value

    return AwaitableGetSecretResult(
        created_at=pulumi.get(__ret__, 'created_at'),
        description=pulumi.get(__ret__, 'description'),
        id=pulumi.get(__ret__, 'id'),
        last_four_chars=pulumi.get(__ret__, 'last_four_chars'),
        name=pulumi.get(__ret__, 'name'),
        owning_team_ids=pulumi.get(__ret__, 'owning_team_ids'),
        updated_at=pulumi.get(__ret__, 'updated_at'),
        version=pulumi.get(__ret__, 'version'))
def get_secret_output(id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                      name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                      opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetSecretResult]:
    """
    Manage secrets: named credentials that workflows can reference. A secret's value can be set and rotated but is never returned by the API.

    Use this data source to look up an existing secret, either by `id` or by `name`. This is how you reference a secret somebody created in the dashboard without managing it as an `Secret` resource. Set exactly one of the two lookup attributes; setting both, or neither, is rejected at plan time.

    A secret's value is never returned by the API, so there is no value attribute here: `last_four_chars` and `version` are all a read can tell you about it.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_incident as incident

    # Look up an existing secret by name, such as one created in the dashboard.
    pagerduty_webhook_token = incident.get_secret(name="PagerDuty webhook token")
    # Or by ID, to reference a secret another module manages.
    billing_api_key = incident.get_secret(id="01G0J1EXE7AXZ2C93K61WBPYEH")
    pulumi.export("pagerdutyTokenVersion", pagerduty_webhook_token.version)
    ```


    :param _builtins.str id: Unique identifier for this secret
    :param _builtins.str name: Human-readable name, unique within the organisation amongst unarchived secrets
    """
    __args__ = dict()
    __args__['id'] = id
    __args__['name'] = name
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('incident:index/getSecret:getSecret', __args__, opts=opts, typ=GetSecretResult)
    return __ret__.apply(lambda __response__: GetSecretResult(
        created_at=pulumi.get(__response__, 'created_at'),
        description=pulumi.get(__response__, 'description'),
        id=pulumi.get(__response__, 'id'),
        last_four_chars=pulumi.get(__response__, 'last_four_chars'),
        name=pulumi.get(__response__, 'name'),
        owning_team_ids=pulumi.get(__response__, 'owning_team_ids'),
        updated_at=pulumi.get(__response__, 'updated_at'),
        version=pulumi.get(__response__, 'version')))
