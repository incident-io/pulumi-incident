# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['StatusArgs', 'Status']

@pulumi.input_type
class StatusArgs:
    def __init__(__self__, *,
                 category: pulumi.Input[_builtins.str],
                 description: pulumi.Input[_builtins.str],
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 rank: pulumi.Input[Optional[_builtins.int]] = None):
        """
        The set of arguments for constructing a Status resource.

        :param pulumi.Input[_builtins.str] category: What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`. Changing it replaces the status, as a status can't move between categories. Set `rank` if you want the replacement to keep its place.
        :param pulumi.Input[_builtins.str] description: Rich text description of the incident status
        :param pulumi.Input[_builtins.str] name: Unique name of this status
        :param pulumi.Input[_builtins.int] rank: Where this status sits within its category, lowest rank first. Set it on every status in a category to fix their order: without it each status lands after the last one created, which for a single apply means whichever order Terraform happened to create them in. No two statuses in a category can share a rank, and ranks needn't be consecutive — leaving gaps (10, 20, 30) means you can insert a status between two others without renumbering them. Because a rank belongs to one status at a time, swapping a pair of them in a single apply fails: move a status to a rank nothing holds instead.
        """
        pulumi.set(__self__, "category", category)
        pulumi.set(__self__, "description", description)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if rank is not None:
            pulumi.set(__self__, "rank", rank)

    @_builtins.property
    @pulumi.getter
    def category(self) -> pulumi.Input[_builtins.str]:
        """
        What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`. Changing it replaces the status, as a status can't move between categories. Set `rank` if you want the replacement to keep its place.
        """
        return pulumi.get(self, "category")

    @category.setter
    def category(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "category", value)

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Input[_builtins.str]:
        """
        Rich text description of the incident status
        """
        return pulumi.get(self, "description")

    @description.setter
    def description(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "description", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Unique name of this status
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def rank(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        Where this status sits within its category, lowest rank first. Set it on every status in a category to fix their order: without it each status lands after the last one created, which for a single apply means whichever order Terraform happened to create them in. No two statuses in a category can share a rank, and ranks needn't be consecutive — leaving gaps (10, 20, 30) means you can insert a status between two others without renumbering them. Because a rank belongs to one status at a time, swapping a pair of them in a single apply fails: move a status to a rank nothing holds instead.
        """
        return pulumi.get(self, "rank")

    @rank.setter
    def rank(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "rank", value)


@pulumi.input_type
class _StatusState:
    def __init__(__self__, *,
                 category: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 rank: pulumi.Input[Optional[_builtins.int]] = None):
        """
        Input properties used for looking up and filtering Status resources.

        :param pulumi.Input[_builtins.str] category: What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`. Changing it replaces the status, as a status can't move between categories. Set `rank` if you want the replacement to keep its place.
        :param pulumi.Input[_builtins.str] description: Rich text description of the incident status
        :param pulumi.Input[_builtins.str] name: Unique name of this status
        :param pulumi.Input[_builtins.int] rank: Where this status sits within its category, lowest rank first. Set it on every status in a category to fix their order: without it each status lands after the last one created, which for a single apply means whichever order Terraform happened to create them in. No two statuses in a category can share a rank, and ranks needn't be consecutive — leaving gaps (10, 20, 30) means you can insert a status between two others without renumbering them. Because a rank belongs to one status at a time, swapping a pair of them in a single apply fails: move a status to a rank nothing holds instead.
        """
        if category is not None:
            pulumi.set(__self__, "category", category)
        if description is not None:
            pulumi.set(__self__, "description", description)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if rank is not None:
            pulumi.set(__self__, "rank", rank)

    @_builtins.property
    @pulumi.getter
    def category(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`. Changing it replaces the status, as a status can't move between categories. Set `rank` if you want the replacement to keep its place.
        """
        return pulumi.get(self, "category")

    @category.setter
    def category(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "category", value)

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Rich text description of the incident status
        """
        return pulumi.get(self, "description")

    @description.setter
    def description(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "description", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Unique name of this status
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def rank(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        Where this status sits within its category, lowest rank first. Set it on every status in a category to fix their order: without it each status lands after the last one created, which for a single apply means whichever order Terraform happened to create them in. No two statuses in a category can share a rank, and ranks needn't be consecutive — leaving gaps (10, 20, 30) means you can insert a status between two others without renumbering them. Because a rank belongs to one status at a time, swapping a pair of them in a single apply fails: move a status to a rank nothing holds instead.
        """
        return pulumi.get(self, "rank")

    @rank.setter
    def rank(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "rank", value)


@pulumi.type_token("incident:index/status:Status")
class Status(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 category: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 rank: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        """
        Manage incident statuses.

        Each incident has a status, picked from one of the statuses configured in your
        organisations settings.

        Statuses help communicate where an incident is in its lifecycle. You can use
        statuses when filtering incidents in the dashboard, and in workflows and announcement
        rules.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident

        # Statuses are ordered by rank within their category, lowest first. Give every status
        # in a category a rank and the order is whatever the config says, rather than the order
        # Terraform happened to create them in.
        #
        # Leaving gaps is worth doing: a status can then be slotted between two others without
        # renumbering them.
        investigating = incident.Status("investigating",
            name="Investigating",
            description="We've spotted that something is wrong, but we're not sure why yet.",
            category="live",
            rank=10)
        clean_up = incident.Status("clean_up",
            name="Clean-up",
            description="Not yet fully finished, but isn't a live incident anymore.",
            category="live",
            rank=20)
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] category: What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`. Changing it replaces the status, as a status can't move between categories. Set `rank` if you want the replacement to keep its place.
        :param pulumi.Input[_builtins.str] description: Rich text description of the incident status
        :param pulumi.Input[_builtins.str] name: Unique name of this status
        :param pulumi.Input[_builtins.int] rank: Where this status sits within its category, lowest rank first. Set it on every status in a category to fix their order: without it each status lands after the last one created, which for a single apply means whichever order Terraform happened to create them in. No two statuses in a category can share a rank, and ranks needn't be consecutive — leaving gaps (10, 20, 30) means you can insert a status between two others without renumbering them. Because a rank belongs to one status at a time, swapping a pair of them in a single apply fails: move a status to a rank nothing holds instead.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: StatusArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manage incident statuses.

        Each incident has a status, picked from one of the statuses configured in your
        organisations settings.

        Statuses help communicate where an incident is in its lifecycle. You can use
        statuses when filtering incidents in the dashboard, and in workflows and announcement
        rules.

        ## Example Usage

        ```python
        import pulumi
        import pulumi_incident as incident

        # Statuses are ordered by rank within their category, lowest first. Give every status
        # in a category a rank and the order is whatever the config says, rather than the order
        # Terraform happened to create them in.
        #
        # Leaving gaps is worth doing: a status can then be slotted between two others without
        # renumbering them.
        investigating = incident.Status("investigating",
            name="Investigating",
            description="We've spotted that something is wrong, but we're not sure why yet.",
            category="live",
            rank=10)
        clean_up = incident.Status("clean_up",
            name="Clean-up",
            description="Not yet fully finished, but isn't a live incident anymore.",
            category="live",
            rank=20)
        ```


        :param str resource_name: The name of the resource.
        :param StatusArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(StatusArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 category: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 rank: pulumi.Input[Optional[_builtins.int]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = StatusArgs.__new__(StatusArgs)

            if category is None and not opts.urn:
                raise TypeError("Missing required property 'category'")
            __props__.__dict__["category"] = category
            if description is None and not opts.urn:
                raise TypeError("Missing required property 'description'")
            __props__.__dict__["description"] = description
            __props__.__dict__["name"] = name
            __props__.__dict__["rank"] = rank
        super(Status, __self__).__init__(
            'incident:index/status:Status',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            category: pulumi.Input[Optional[_builtins.str]] = None,
            description: pulumi.Input[Optional[_builtins.str]] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            rank: pulumi.Input[Optional[_builtins.int]] = None) -> 'Status':
        """
        Get an existing Status resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] category: What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`. Changing it replaces the status, as a status can't move between categories. Set `rank` if you want the replacement to keep its place.
        :param pulumi.Input[_builtins.str] description: Rich text description of the incident status
        :param pulumi.Input[_builtins.str] name: Unique name of this status
        :param pulumi.Input[_builtins.int] rank: Where this status sits within its category, lowest rank first. Set it on every status in a category to fix their order: without it each status lands after the last one created, which for a single apply means whichever order Terraform happened to create them in. No two statuses in a category can share a rank, and ranks needn't be consecutive — leaving gaps (10, 20, 30) means you can insert a status between two others without renumbering them. Because a rank belongs to one status at a time, swapping a pair of them in a single apply fails: move a status to a rank nothing holds instead.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _StatusState.__new__(_StatusState)

        __props__.__dict__["category"] = category
        __props__.__dict__["description"] = description
        __props__.__dict__["name"] = name
        __props__.__dict__["rank"] = rank
        return Status(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter
    def category(self) -> pulumi.Output[_builtins.str]:
        """
        What category of status it is. All statuses apart from live (renamed in the app to Active) and learning (renamed in the app to Post-incident) are managed by incident.io and cannot be configured. Possible values are: `triage`, `declined`, `merged`, `canceled`, `live`, `learning`, `closed`, `paused`. Changing it replaces the status, as a status can't move between categories. Set `rank` if you want the replacement to keep its place.
        """
        return pulumi.get(self, "category")

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Output[_builtins.str]:
        """
        Rich text description of the incident status
        """
        return pulumi.get(self, "description")

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        Unique name of this status
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def rank(self) -> pulumi.Output[Optional[_builtins.int]]:
        """
        Where this status sits within its category, lowest rank first. Set it on every status in a category to fix their order: without it each status lands after the last one created, which for a single apply means whichever order Terraform happened to create them in. No two statuses in a category can share a rank, and ranks needn't be consecutive — leaving gaps (10, 20, 30) means you can insert a status between two others without renumbering them. Because a rank belongs to one status at a time, swapping a pair of them in a single apply fails: move a status to a rank nothing holds instead.
        """
        return pulumi.get(self, "rank")

