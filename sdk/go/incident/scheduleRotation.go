// Code generated by pulumi-language-go DO NOT EDIT.
// *** WARNING: Do not edit by hand unless you're certain you know what you are doing! ***

package incident

import (
	"context"
	"reflect"

	"errors"
	"github.com/incident-io/pulumi-incident/sdk/go/incident/internal"
	"github.com/pulumi/pulumi/sdk/v3/go/pulumi"
)

// Manage a rotation on an on-call schedule.
//
// A rotation is a group of people who take turns being on call, and the cadence they
// hand over on. A schedule can hold several, and each is managed independently — so
// changing one never rewrites the others.
//
// Editing a rotation takes effect straight away, unless `rollout` says otherwise.
//
// ## Changing when shifts hand over
//
// `firstIntervalStartsAt` is the point intervals are counted from. With
// `handovers` it fixes the time of day and day of week that shifts change hands —
// the dashboard calls it the handover time.
//
// It can't change in the same apply as `rollout`. Whenever `rollout` is set the
// rotation keeps the interval start it already runs: an immediate rollout copies that
// across so the cadence doesn't move, and a phased one works backwards from the next
// handover so the new line-up slots into the rhythm people are already working. Either
// way a new `firstIntervalStartsAt` is discarded, so the provider stops the apply
// rather than storing a value that won't be kept.
//
// To change both, take it in two applies:
//
//  1. Change `firstIntervalStartsAt` on its own, with `rollout` unset. The new
//     cadence takes effect straight away.
//  2. Then make the line-up change, with `rollout` set.
//
// ## Choosing a scheduling mode
//
// `schedulingMode` decides who takes the next shift.
//
// `fair` shares time on call out evenly, tracking how much each person has already
// done and giving the next shift to whoever is behind. Someone newly added has done
// none, so `fair` tends to bring them on call sooner rather than adding them to
// the back of the queue.
//
// `sequential` goes around the list in order, so the next person on call is always
// the one after the last.
//
// For an even rotation — everyone on for the same length of time, no working hours,
// nobody joining or leaving — the two behave identically. They only diverge once
// shifts differ in length, `workingIntervals` is set, or the list of users
// changes. Reach for `sequential` when it matters that the running order is
// obvious to the people in it.
//
// ## Upgrading from v6
//
// Before v7, a rotation was declared inline on `Schedule`, in its
// `rotations` attribute. It is now its own resource. See `Schedule`
// for what that changes and how to import the rotations you already have.
//
// ## Example Usage
//
// ```go
// package main
//
// import (
//
//	"github.com/incident-io/pulumi-incident/sdk/go/incident"
//	"github.com/pulumi/pulumi/sdk/v3/go/pulumi"
//
// )
//
//	func main() {
//		pulumi.Run(func(ctx *pulumi.Context) error {
//			// A rotation is a group of people who take turns being on call. Each one is managed
//			// on its own, so editing this rotation doesn't touch the others on the schedule.
//			_, err := incident.NewScheduleRotation(ctx, "primary", &incident.ScheduleRotationArgs{
//				ScheduleId: pulumi.Any(platform.Id),
//				Name:       pulumi.String("Primary"),
//				Users: pulumi.StringArray{
//					alice.Id,
//					bob.Id,
//				},
//				FirstIntervalStartsAt: pulumi.String("2024-01-08T09:00:00Z"),
//				Handovers: incident.ScheduleRotationHandoverArray{
//					&incident.ScheduleRotationHandoverArgs{
//						Interval:     pulumi.Int(1),
//						IntervalType: pulumi.String("weekly"),
//					},
//				},
//			})
//			if err != nil {
//				return err
//			}
//			// A rotation that only covers weekday working hours, with two people on call at
//			// once and a place in the schedule's running order.
//			_, err = incident.NewScheduleRotation(ctx, "business_hours", &incident.ScheduleRotationArgs{
//				ScheduleId: pulumi.Any(platform.Id),
//				Name:       pulumi.String("Business hours"),
//				Rank:       pulumi.Int(2),
//				Users: pulumi.StringArray{
//					alice.Id,
//					bob.Id,
//					carol.Id,
//				},
//				FirstIntervalStartsAt: pulumi.String("2024-01-08T09:00:00Z"),
//				Handovers: incident.ScheduleRotationHandoverArray{
//					&incident.ScheduleRotationHandoverArgs{
//						Interval:     pulumi.Int(1),
//						IntervalType: pulumi.String("daily"),
//					},
//				},
//				ConcurrentShifts: pulumi.Int(2),
//				WorkingIntervals: incident.ScheduleRotationWorkingIntervalArray{
//					&incident.ScheduleRotationWorkingIntervalArgs{
//						Weekday:   pulumi.String("monday"),
//						StartTime: pulumi.String("09:00"),
//						EndTime:   pulumi.String("17:00"),
//					},
//					&incident.ScheduleRotationWorkingIntervalArgs{
//						Weekday:   pulumi.String("tuesday"),
//						StartTime: pulumi.String("09:00"),
//						EndTime:   pulumi.String("17:00"),
//					},
//					&incident.ScheduleRotationWorkingIntervalArgs{
//						Weekday:   pulumi.String("wednesday"),
//						StartTime: pulumi.String("09:00"),
//						EndTime:   pulumi.String("17:00"),
//					},
//					&incident.ScheduleRotationWorkingIntervalArgs{
//						Weekday:   pulumi.String("thursday"),
//						StartTime: pulumi.String("09:00"),
//						EndTime:   pulumi.String("17:00"),
//					},
//					&incident.ScheduleRotationWorkingIntervalArgs{
//						Weekday:   pulumi.String("friday"),
//						StartTime: pulumi.String("09:00"),
//						EndTime:   pulumi.String("17:00"),
//					},
//				},
//			})
//			if err != nil {
//				return err
//			}
//			return nil
//		})
//	}
//
// ```
type ScheduleRotation struct {
	pulumi.CustomResourceState

	// How many shifts run at the same time, which is how many people are on call at once. Defaults to 1, one person on call at a time. Reducing this stops scheduling the last of them, and stops any overrides on those shifts from applying.
	ConcurrentShifts pulumi.IntOutput `pulumi:"concurrentShifts"`
	// When this shape of the rotation took over. Unset when the rotation has only ever had one.
	EffectiveFrom pulumi.StringOutput `pulumi:"effectiveFrom"`
	// When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.. Can't be changed in the same apply as `rollout` — see the resource description for why, and what to do instead.
	FirstIntervalStartsAt pulumi.StringOutput `pulumi:"firstIntervalStartsAt"`
	// The cadence shifts hand over on, applied in turn
	Handovers ScheduleRotationHandoverArrayOutput `pulumi:"handovers"`
	// Human readable name for the rotation, unique within the schedule
	Name pulumi.StringOutput `pulumi:"name"`
	// Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
	Rank pulumi.IntPtrOutput `pulumi:"rank"`
	// How a change to this rotation is introduced: `immediate` replaces the line-up now and can change who is on call this minute, `afterCurrentShift` lets the shift on call finish first, and `afterFullRotation` waits for everyone to have taken a turn. Leave it out to replace the line-up straight away. Creating a rotation ignores it, as there's no shift to protect yet.
	Rollout pulumi.StringPtrOutput `pulumi:"rollout"`
	// ID of the schedule this rotation belongs to. Changing this replaces the rotation, as a rotation can't move between schedules.
	ScheduleId pulumi.StringOutput `pulumi:"scheduleId"`
	// How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`. Omit it to leave us to pick. For an even rotation the two behave identically — see the resource description for when they diverge.
	SchedulingMode pulumi.StringPtrOutput `pulumi:"schedulingMode"`
	// IDs of the people in the rotation, in the order they take shifts. Use the special ID `NOBODY` for a slot with nobody in it, which schedules the shift without putting anyone on call until an override covers it.
	Users pulumi.StringArrayOutput `pulumi:"users"`
	// If set, restricts on-call to these weekday intervals. Omit it to keep the rotation on call around the clock. An empty list is not valid, and is rejected at plan time — it would leave a rotation nobody is ever on call for.
	WorkingIntervals ScheduleRotationWorkingIntervalArrayOutput `pulumi:"workingIntervals"`
}

// NewScheduleRotation registers a new resource with the given unique name, arguments, and options.
func NewScheduleRotation(ctx *pulumi.Context,
	name string, args *ScheduleRotationArgs, opts ...pulumi.ResourceOption) (*ScheduleRotation, error) {
	if args == nil {
		return nil, errors.New("missing one or more required arguments")
	}

	if args.FirstIntervalStartsAt == nil {
		return nil, errors.New("invalid value for required argument 'FirstIntervalStartsAt'")
	}
	if args.Handovers == nil {
		return nil, errors.New("invalid value for required argument 'Handovers'")
	}
	if args.ScheduleId == nil {
		return nil, errors.New("invalid value for required argument 'ScheduleId'")
	}
	if args.Users == nil {
		return nil, errors.New("invalid value for required argument 'Users'")
	}
	opts = internal.PkgResourceDefaultOpts(opts)
	var resource ScheduleRotation
	err := ctx.RegisterResource("incident:index/scheduleRotation:ScheduleRotation", name, args, &resource, opts...)
	if err != nil {
		return nil, err
	}
	return &resource, nil
}

// GetScheduleRotation gets an existing ScheduleRotation resource's state with the given name, ID, and optional
// state properties that are used to uniquely qualify the lookup (nil if not required).
func GetScheduleRotation(ctx *pulumi.Context,
	name string, id pulumi.IDInput, state *ScheduleRotationState, opts ...pulumi.ResourceOption) (*ScheduleRotation, error) {
	var resource ScheduleRotation
	err := ctx.ReadResource("incident:index/scheduleRotation:ScheduleRotation", name, id, state, &resource, opts...)
	if err != nil {
		return nil, err
	}
	return &resource, nil
}

// Input properties used for looking up and filtering ScheduleRotation resources.
type scheduleRotationState struct {
	// How many shifts run at the same time, which is how many people are on call at once. Defaults to 1, one person on call at a time. Reducing this stops scheduling the last of them, and stops any overrides on those shifts from applying.
	ConcurrentShifts *int `pulumi:"concurrentShifts"`
	// When this shape of the rotation took over. Unset when the rotation has only ever had one.
	EffectiveFrom *string `pulumi:"effectiveFrom"`
	// When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.. Can't be changed in the same apply as `rollout` — see the resource description for why, and what to do instead.
	FirstIntervalStartsAt *string `pulumi:"firstIntervalStartsAt"`
	// The cadence shifts hand over on, applied in turn
	Handovers []ScheduleRotationHandover `pulumi:"handovers"`
	// Human readable name for the rotation, unique within the schedule
	Name *string `pulumi:"name"`
	// Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
	Rank *int `pulumi:"rank"`
	// How a change to this rotation is introduced: `immediate` replaces the line-up now and can change who is on call this minute, `afterCurrentShift` lets the shift on call finish first, and `afterFullRotation` waits for everyone to have taken a turn. Leave it out to replace the line-up straight away. Creating a rotation ignores it, as there's no shift to protect yet.
	Rollout *string `pulumi:"rollout"`
	// ID of the schedule this rotation belongs to. Changing this replaces the rotation, as a rotation can't move between schedules.
	ScheduleId *string `pulumi:"scheduleId"`
	// How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`. Omit it to leave us to pick. For an even rotation the two behave identically — see the resource description for when they diverge.
	SchedulingMode *string `pulumi:"schedulingMode"`
	// IDs of the people in the rotation, in the order they take shifts. Use the special ID `NOBODY` for a slot with nobody in it, which schedules the shift without putting anyone on call until an override covers it.
	Users []string `pulumi:"users"`
	// If set, restricts on-call to these weekday intervals. Omit it to keep the rotation on call around the clock. An empty list is not valid, and is rejected at plan time — it would leave a rotation nobody is ever on call for.
	WorkingIntervals []ScheduleRotationWorkingInterval `pulumi:"workingIntervals"`
}

type ScheduleRotationState struct {
	// How many shifts run at the same time, which is how many people are on call at once. Defaults to 1, one person on call at a time. Reducing this stops scheduling the last of them, and stops any overrides on those shifts from applying.
	ConcurrentShifts pulumi.IntPtrInput
	// When this shape of the rotation took over. Unset when the rotation has only ever had one.
	EffectiveFrom pulumi.StringPtrInput
	// When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.. Can't be changed in the same apply as `rollout` — see the resource description for why, and what to do instead.
	FirstIntervalStartsAt pulumi.StringPtrInput
	// The cadence shifts hand over on, applied in turn
	Handovers ScheduleRotationHandoverArrayInput
	// Human readable name for the rotation, unique within the schedule
	Name pulumi.StringPtrInput
	// Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
	Rank pulumi.IntPtrInput
	// How a change to this rotation is introduced: `immediate` replaces the line-up now and can change who is on call this minute, `afterCurrentShift` lets the shift on call finish first, and `afterFullRotation` waits for everyone to have taken a turn. Leave it out to replace the line-up straight away. Creating a rotation ignores it, as there's no shift to protect yet.
	Rollout pulumi.StringPtrInput
	// ID of the schedule this rotation belongs to. Changing this replaces the rotation, as a rotation can't move between schedules.
	ScheduleId pulumi.StringPtrInput
	// How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`. Omit it to leave us to pick. For an even rotation the two behave identically — see the resource description for when they diverge.
	SchedulingMode pulumi.StringPtrInput
	// IDs of the people in the rotation, in the order they take shifts. Use the special ID `NOBODY` for a slot with nobody in it, which schedules the shift without putting anyone on call until an override covers it.
	Users pulumi.StringArrayInput
	// If set, restricts on-call to these weekday intervals. Omit it to keep the rotation on call around the clock. An empty list is not valid, and is rejected at plan time — it would leave a rotation nobody is ever on call for.
	WorkingIntervals ScheduleRotationWorkingIntervalArrayInput
}

func (ScheduleRotationState) ElementType() reflect.Type {
	return reflect.TypeOf((*scheduleRotationState)(nil)).Elem()
}

type scheduleRotationArgs struct {
	// How many shifts run at the same time, which is how many people are on call at once. Defaults to 1, one person on call at a time. Reducing this stops scheduling the last of them, and stops any overrides on those shifts from applying.
	ConcurrentShifts *int `pulumi:"concurrentShifts"`
	// When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.. Can't be changed in the same apply as `rollout` — see the resource description for why, and what to do instead.
	FirstIntervalStartsAt string `pulumi:"firstIntervalStartsAt"`
	// The cadence shifts hand over on, applied in turn
	Handovers []ScheduleRotationHandover `pulumi:"handovers"`
	// Human readable name for the rotation, unique within the schedule
	Name *string `pulumi:"name"`
	// Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
	Rank *int `pulumi:"rank"`
	// How a change to this rotation is introduced: `immediate` replaces the line-up now and can change who is on call this minute, `afterCurrentShift` lets the shift on call finish first, and `afterFullRotation` waits for everyone to have taken a turn. Leave it out to replace the line-up straight away. Creating a rotation ignores it, as there's no shift to protect yet.
	Rollout *string `pulumi:"rollout"`
	// ID of the schedule this rotation belongs to. Changing this replaces the rotation, as a rotation can't move between schedules.
	ScheduleId string `pulumi:"scheduleId"`
	// How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`. Omit it to leave us to pick. For an even rotation the two behave identically — see the resource description for when they diverge.
	SchedulingMode *string `pulumi:"schedulingMode"`
	// IDs of the people in the rotation, in the order they take shifts. Use the special ID `NOBODY` for a slot with nobody in it, which schedules the shift without putting anyone on call until an override covers it.
	Users []string `pulumi:"users"`
	// If set, restricts on-call to these weekday intervals. Omit it to keep the rotation on call around the clock. An empty list is not valid, and is rejected at plan time — it would leave a rotation nobody is ever on call for.
	WorkingIntervals []ScheduleRotationWorkingInterval `pulumi:"workingIntervals"`
}

// The set of arguments for constructing a ScheduleRotation resource.
type ScheduleRotationArgs struct {
	// How many shifts run at the same time, which is how many people are on call at once. Defaults to 1, one person on call at a time. Reducing this stops scheduling the last of them, and stops any overrides on those shifts from applying.
	ConcurrentShifts pulumi.IntPtrInput
	// When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.. Can't be changed in the same apply as `rollout` — see the resource description for why, and what to do instead.
	FirstIntervalStartsAt pulumi.StringInput
	// The cadence shifts hand over on, applied in turn
	Handovers ScheduleRotationHandoverArrayInput
	// Human readable name for the rotation, unique within the schedule
	Name pulumi.StringPtrInput
	// Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
	Rank pulumi.IntPtrInput
	// How a change to this rotation is introduced: `immediate` replaces the line-up now and can change who is on call this minute, `afterCurrentShift` lets the shift on call finish first, and `afterFullRotation` waits for everyone to have taken a turn. Leave it out to replace the line-up straight away. Creating a rotation ignores it, as there's no shift to protect yet.
	Rollout pulumi.StringPtrInput
	// ID of the schedule this rotation belongs to. Changing this replaces the rotation, as a rotation can't move between schedules.
	ScheduleId pulumi.StringInput
	// How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`. Omit it to leave us to pick. For an even rotation the two behave identically — see the resource description for when they diverge.
	SchedulingMode pulumi.StringPtrInput
	// IDs of the people in the rotation, in the order they take shifts. Use the special ID `NOBODY` for a slot with nobody in it, which schedules the shift without putting anyone on call until an override covers it.
	Users pulumi.StringArrayInput
	// If set, restricts on-call to these weekday intervals. Omit it to keep the rotation on call around the clock. An empty list is not valid, and is rejected at plan time — it would leave a rotation nobody is ever on call for.
	WorkingIntervals ScheduleRotationWorkingIntervalArrayInput
}

func (ScheduleRotationArgs) ElementType() reflect.Type {
	return reflect.TypeOf((*scheduleRotationArgs)(nil)).Elem()
}

type ScheduleRotationInput interface {
	pulumi.Input

	ToScheduleRotationOutput() ScheduleRotationOutput
	ToScheduleRotationOutputWithContext(ctx context.Context) ScheduleRotationOutput
}

func (*ScheduleRotation) ElementType() reflect.Type {
	return reflect.TypeOf((**ScheduleRotation)(nil)).Elem()
}

func (i *ScheduleRotation) ToScheduleRotationOutput() ScheduleRotationOutput {
	return i.ToScheduleRotationOutputWithContext(context.Background())
}

func (i *ScheduleRotation) ToScheduleRotationOutputWithContext(ctx context.Context) ScheduleRotationOutput {
	return pulumi.ToOutputWithContext(ctx, i).(ScheduleRotationOutput)
}

// ScheduleRotationArrayInput is an input type that accepts ScheduleRotationArray and ScheduleRotationArrayOutput values.
// You can construct a concrete instance of `ScheduleRotationArrayInput` via:
//
//	ScheduleRotationArray{ ScheduleRotationArgs{...} }
type ScheduleRotationArrayInput interface {
	pulumi.Input

	ToScheduleRotationArrayOutput() ScheduleRotationArrayOutput
	ToScheduleRotationArrayOutputWithContext(context.Context) ScheduleRotationArrayOutput
}

type ScheduleRotationArray []ScheduleRotationInput

func (ScheduleRotationArray) ElementType() reflect.Type {
	return reflect.TypeOf((*[]*ScheduleRotation)(nil)).Elem()
}

func (i ScheduleRotationArray) ToScheduleRotationArrayOutput() ScheduleRotationArrayOutput {
	return i.ToScheduleRotationArrayOutputWithContext(context.Background())
}

func (i ScheduleRotationArray) ToScheduleRotationArrayOutputWithContext(ctx context.Context) ScheduleRotationArrayOutput {
	return pulumi.ToOutputWithContext(ctx, i).(ScheduleRotationArrayOutput)
}

// ScheduleRotationMapInput is an input type that accepts ScheduleRotationMap and ScheduleRotationMapOutput values.
// You can construct a concrete instance of `ScheduleRotationMapInput` via:
//
//	ScheduleRotationMap{ "key": ScheduleRotationArgs{...} }
type ScheduleRotationMapInput interface {
	pulumi.Input

	ToScheduleRotationMapOutput() ScheduleRotationMapOutput
	ToScheduleRotationMapOutputWithContext(context.Context) ScheduleRotationMapOutput
}

type ScheduleRotationMap map[string]ScheduleRotationInput

func (ScheduleRotationMap) ElementType() reflect.Type {
	return reflect.TypeOf((*map[string]*ScheduleRotation)(nil)).Elem()
}

func (i ScheduleRotationMap) ToScheduleRotationMapOutput() ScheduleRotationMapOutput {
	return i.ToScheduleRotationMapOutputWithContext(context.Background())
}

func (i ScheduleRotationMap) ToScheduleRotationMapOutputWithContext(ctx context.Context) ScheduleRotationMapOutput {
	return pulumi.ToOutputWithContext(ctx, i).(ScheduleRotationMapOutput)
}

type ScheduleRotationOutput struct{ *pulumi.OutputState }

func (ScheduleRotationOutput) ElementType() reflect.Type {
	return reflect.TypeOf((**ScheduleRotation)(nil)).Elem()
}

func (o ScheduleRotationOutput) ToScheduleRotationOutput() ScheduleRotationOutput {
	return o
}

func (o ScheduleRotationOutput) ToScheduleRotationOutputWithContext(ctx context.Context) ScheduleRotationOutput {
	return o
}

// How many shifts run at the same time, which is how many people are on call at once. Defaults to 1, one person on call at a time. Reducing this stops scheduling the last of them, and stops any overrides on those shifts from applying.
func (o ScheduleRotationOutput) ConcurrentShifts() pulumi.IntOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.IntOutput { return v.ConcurrentShifts }).(pulumi.IntOutput)
}

// When this shape of the rotation took over. Unset when the rotation has only ever had one.
func (o ScheduleRotationOutput) EffectiveFrom() pulumi.StringOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.StringOutput { return v.EffectiveFrom }).(pulumi.StringOutput)
}

// When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.. Can't be changed in the same apply as `rollout` — see the resource description for why, and what to do instead.
func (o ScheduleRotationOutput) FirstIntervalStartsAt() pulumi.StringOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.StringOutput { return v.FirstIntervalStartsAt }).(pulumi.StringOutput)
}

// The cadence shifts hand over on, applied in turn
func (o ScheduleRotationOutput) Handovers() ScheduleRotationHandoverArrayOutput {
	return o.ApplyT(func(v *ScheduleRotation) ScheduleRotationHandoverArrayOutput { return v.Handovers }).(ScheduleRotationHandoverArrayOutput)
}

// Human readable name for the rotation, unique within the schedule
func (o ScheduleRotationOutput) Name() pulumi.StringOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.StringOutput { return v.Name }).(pulumi.StringOutput)
}

// Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
func (o ScheduleRotationOutput) Rank() pulumi.IntPtrOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.IntPtrOutput { return v.Rank }).(pulumi.IntPtrOutput)
}

// How a change to this rotation is introduced: `immediate` replaces the line-up now and can change who is on call this minute, `afterCurrentShift` lets the shift on call finish first, and `afterFullRotation` waits for everyone to have taken a turn. Leave it out to replace the line-up straight away. Creating a rotation ignores it, as there's no shift to protect yet.
func (o ScheduleRotationOutput) Rollout() pulumi.StringPtrOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.StringPtrOutput { return v.Rollout }).(pulumi.StringPtrOutput)
}

// ID of the schedule this rotation belongs to. Changing this replaces the rotation, as a rotation can't move between schedules.
func (o ScheduleRotationOutput) ScheduleId() pulumi.StringOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.StringOutput { return v.ScheduleId }).(pulumi.StringOutput)
}

// How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`. Omit it to leave us to pick. For an even rotation the two behave identically — see the resource description for when they diverge.
func (o ScheduleRotationOutput) SchedulingMode() pulumi.StringPtrOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.StringPtrOutput { return v.SchedulingMode }).(pulumi.StringPtrOutput)
}

// IDs of the people in the rotation, in the order they take shifts. Use the special ID `NOBODY` for a slot with nobody in it, which schedules the shift without putting anyone on call until an override covers it.
func (o ScheduleRotationOutput) Users() pulumi.StringArrayOutput {
	return o.ApplyT(func(v *ScheduleRotation) pulumi.StringArrayOutput { return v.Users }).(pulumi.StringArrayOutput)
}

// If set, restricts on-call to these weekday intervals. Omit it to keep the rotation on call around the clock. An empty list is not valid, and is rejected at plan time — it would leave a rotation nobody is ever on call for.
func (o ScheduleRotationOutput) WorkingIntervals() ScheduleRotationWorkingIntervalArrayOutput {
	return o.ApplyT(func(v *ScheduleRotation) ScheduleRotationWorkingIntervalArrayOutput { return v.WorkingIntervals }).(ScheduleRotationWorkingIntervalArrayOutput)
}

type ScheduleRotationArrayOutput struct{ *pulumi.OutputState }

func (ScheduleRotationArrayOutput) ElementType() reflect.Type {
	return reflect.TypeOf((*[]*ScheduleRotation)(nil)).Elem()
}

func (o ScheduleRotationArrayOutput) ToScheduleRotationArrayOutput() ScheduleRotationArrayOutput {
	return o
}

func (o ScheduleRotationArrayOutput) ToScheduleRotationArrayOutputWithContext(ctx context.Context) ScheduleRotationArrayOutput {
	return o
}

func (o ScheduleRotationArrayOutput) Index(i pulumi.IntInput) ScheduleRotationOutput {
	return pulumi.All(o, i).ApplyT(func(vs []interface{}) *ScheduleRotation {
		return vs[0].([]*ScheduleRotation)[vs[1].(int)]
	}).(ScheduleRotationOutput)
}

type ScheduleRotationMapOutput struct{ *pulumi.OutputState }

func (ScheduleRotationMapOutput) ElementType() reflect.Type {
	return reflect.TypeOf((*map[string]*ScheduleRotation)(nil)).Elem()
}

func (o ScheduleRotationMapOutput) ToScheduleRotationMapOutput() ScheduleRotationMapOutput {
	return o
}

func (o ScheduleRotationMapOutput) ToScheduleRotationMapOutputWithContext(ctx context.Context) ScheduleRotationMapOutput {
	return o
}

func (o ScheduleRotationMapOutput) MapIndex(k pulumi.StringInput) ScheduleRotationOutput {
	return pulumi.All(o, k).ApplyT(func(vs []interface{}) *ScheduleRotation {
		return vs[0].(map[string]*ScheduleRotation)[vs[1].(string)]
	}).(ScheduleRotationOutput)
}

func init() {
	pulumi.RegisterInputType(reflect.TypeOf((*ScheduleRotationInput)(nil)).Elem(), &ScheduleRotation{})
	pulumi.RegisterInputType(reflect.TypeOf((*ScheduleRotationArrayInput)(nil)).Elem(), ScheduleRotationArray{})
	pulumi.RegisterInputType(reflect.TypeOf((*ScheduleRotationMapInput)(nil)).Elem(), ScheduleRotationMap{})
	pulumi.RegisterOutputType(ScheduleRotationOutput{})
	pulumi.RegisterOutputType(ScheduleRotationArrayOutput{})
	pulumi.RegisterOutputType(ScheduleRotationMapOutput{})
}
