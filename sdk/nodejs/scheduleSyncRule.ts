// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manage schedule sync rules that link schedules to sync targets (Slack user groups).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Create a sync target (Slack user group)
 * const platformOncall = new incident.ScheduleSyncTarget("platform_oncall", {
 *     addBotToGroup: true,
 *     newSlackUserGroup: {
 *         name: "Platform On-Call",
 *         handle: "platform-oncall",
 *         description: "Current on-call engineers for the Platform team",
 *     },
 * });
 * // Link a schedule to the sync target
 * // Only the current on-call engineer(s) will be synced to the user group
 * const platformOncallScheduleSyncRule = new incident.ScheduleSyncRule("platform_oncall", {
 *     scheduleId: platform.id,
 *     scheduleSyncTargetId: platformOncall.id,
 *     syncType: "on_call",
 * });
 * // Alternatively, sync all users in the rotation (not just who's on call)
 * const platformAllUsers = new incident.ScheduleSyncRule("platform_all_users", {
 *     scheduleId: platform.id,
 *     scheduleSyncTargetId: platformTeam.id,
 *     syncType: "all_users",
 * });
 * // Sync the people on the next upcoming shift, rather than whoever is on call now
 * const platformNextOncall = new incident.ScheduleSyncRule("platform_next_oncall", {
 *     scheduleId: platform.id,
 *     scheduleSyncTargetId: platformNextOncallIncidentScheduleSyncTarget.id,
 *     syncType: "next_on_call",
 * });
 * // Scope a rule to a single rotation. Without rotation_id a rule covers every
 * // rotation on the schedule; set it to sync only one. To feed a group from
 * // several rotations, create one rule per rotation pointing at the same target.
 * const platformEuOncall = new incident.ScheduleSyncRule("platform_eu_oncall", {
 *     scheduleId: platform.id,
 *     scheduleSyncTargetId: platformEuOncallIncidentScheduleSyncTarget.id,
 *     syncType: "on_call",
 *     rotationId: "eu",
 * });
 * // Keep a manager in the Slack user group regardless of who is on call.
 * // Omit permanent_member_user_ids to leave existing members unchanged on update;
 * // set it to [] to clear them.
 * const platformOncallWithManager = new incident.ScheduleSyncRule("platform_oncall_with_manager", {
 *     scheduleId: platform.id,
 *     scheduleSyncTargetId: platformOncall.id,
 *     syncType: "on_call",
 *     permanentMemberUserIds: [platformManager.id],
 * });
 * ```
 */
export class ScheduleSyncRule extends pulumi.CustomResource {
    /**
     * Get an existing ScheduleSyncRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScheduleSyncRuleState, opts?: pulumi.CustomResourceOptions): ScheduleSyncRule {
        return new ScheduleSyncRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'incident:index/scheduleSyncRule:ScheduleSyncRule';

    /**
     * Returns true if the given object is an instance of ScheduleSyncRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ScheduleSyncRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ScheduleSyncRule.__pulumiType;
    }

    /**
     * IDs of users always kept in the Slack user group, regardless of who is on call. Useful for keeping e.g. a manager in the group so they see mentions without being paged. Scoped to this rule: when several rules feed the same group, we sync the union of their permanent members.
     */
    declare public readonly permanentMemberUserIds: pulumi.Output<string[] | undefined>;
    /**
     * If set, only members of this rotation sync to the user group. When unset, all rotations on the schedule are synced.
     */
    declare public readonly rotationId: pulumi.Output<string | undefined>;
    /**
     * The schedule this rule belongs to
     */
    declare public readonly scheduleId: pulumi.Output<string>;
    /**
     * The sync target ID this rule links to
     */
    declare public readonly scheduleSyncTargetId: pulumi.Output<string>;
    /**
     * Which schedule members sync to the user group. Possible values are: `onCall`, `allUsers`, `nextOnCall`.
     */
    declare public readonly syncType: pulumi.Output<string>;

    /**
     * Create a ScheduleSyncRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ScheduleSyncRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScheduleSyncRuleArgs | ScheduleSyncRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScheduleSyncRuleState | undefined;
            resourceInputs["permanentMemberUserIds"] = state?.permanentMemberUserIds;
            resourceInputs["rotationId"] = state?.rotationId;
            resourceInputs["scheduleId"] = state?.scheduleId;
            resourceInputs["scheduleSyncTargetId"] = state?.scheduleSyncTargetId;
            resourceInputs["syncType"] = state?.syncType;
        } else {
            const args = argsOrState as ScheduleSyncRuleArgs | undefined;
            if (args?.scheduleId === undefined && !opts.urn) {
                throw new Error("Missing required property 'scheduleId'");
            }
            if (args?.scheduleSyncTargetId === undefined && !opts.urn) {
                throw new Error("Missing required property 'scheduleSyncTargetId'");
            }
            if (args?.syncType === undefined && !opts.urn) {
                throw new Error("Missing required property 'syncType'");
            }
            resourceInputs["permanentMemberUserIds"] = args?.permanentMemberUserIds;
            resourceInputs["rotationId"] = args?.rotationId;
            resourceInputs["scheduleId"] = args?.scheduleId;
            resourceInputs["scheduleSyncTargetId"] = args?.scheduleSyncTargetId;
            resourceInputs["syncType"] = args?.syncType;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ScheduleSyncRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ScheduleSyncRule resources.
 */
export interface ScheduleSyncRuleState {
    /**
     * IDs of users always kept in the Slack user group, regardless of who is on call. Useful for keeping e.g. a manager in the group so they see mentions without being paged. Scoped to this rule: when several rules feed the same group, we sync the union of their permanent members.
     */
    permanentMemberUserIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * If set, only members of this rotation sync to the user group. When unset, all rotations on the schedule are synced.
     */
    rotationId?: pulumi.Input<string | undefined>;
    /**
     * The schedule this rule belongs to
     */
    scheduleId?: pulumi.Input<string | undefined>;
    /**
     * The sync target ID this rule links to
     */
    scheduleSyncTargetId?: pulumi.Input<string | undefined>;
    /**
     * Which schedule members sync to the user group. Possible values are: `onCall`, `allUsers`, `nextOnCall`.
     */
    syncType?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ScheduleSyncRule resource.
 */
export interface ScheduleSyncRuleArgs {
    /**
     * IDs of users always kept in the Slack user group, regardless of who is on call. Useful for keeping e.g. a manager in the group so they see mentions without being paged. Scoped to this rule: when several rules feed the same group, we sync the union of their permanent members.
     */
    permanentMemberUserIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * If set, only members of this rotation sync to the user group. When unset, all rotations on the schedule are synced.
     */
    rotationId?: pulumi.Input<string | undefined>;
    /**
     * The schedule this rule belongs to
     */
    scheduleId: pulumi.Input<string>;
    /**
     * The sync target ID this rule links to
     */
    scheduleSyncTargetId: pulumi.Input<string>;
    /**
     * Which schedule members sync to the user group. Possible values are: `onCall`, `allUsers`, `nextOnCall`.
     */
    syncType: pulumi.Input<string>;
}
