// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manage schedule sync targets (Slack user groups that schedules can sync to).
 *
 * Use this data source to look up an existing schedule sync target, either by `id` or by `slackUserGroupId`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up a schedule sync target by ID.
 * const byId = incident.getScheduleSyncTarget({
 *     id: "01ABC123DEF456GHI789JKL",
 * });
 * // ...or by the Slack user group it keeps in sync. The group's Slack-assigned
 * // ID starts with 'S'; this is not the @-handle.
 * const bySlackGroup = incident.getScheduleSyncTarget({
 *     slackUserGroupId: "S06MNNU5BMK",
 * });
 * ```
 */
export function getScheduleSyncTarget(args?: GetScheduleSyncTargetArgs, opts?: pulumi.InvokeOptions): Promise<GetScheduleSyncTargetResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getScheduleSyncTarget:getScheduleSyncTarget", {
        "id": args.id,
        "slackUserGroupId": args.slackUserGroupId,
    }, opts);
}

/**
 * A collection of arguments for invoking getScheduleSyncTarget.
 */
export interface GetScheduleSyncTargetArgs {
    /**
     * Unique identifier of the sync target
     */
    id?: string;
    /**
     * Slack ID of the user group whose membership is kept in sync. This is the Slack-assigned group ID (starting with 'S'), not the @-handle.
     */
    slackUserGroupId?: string;
}

/**
 * A collection of values returned by getScheduleSyncTarget.
 */
export interface GetScheduleSyncTargetResult {
    /**
     * Whether the incident.io bot should be added to the group as a member. This is needed for some Slack configurations to let us manage the group's membership.
     */
    readonly addBotToGroup: boolean;
    /**
     * Unique identifier of the sync target
     */
    readonly id: string;
    /**
     * Schedules with an active sync rule pointing at this target
     */
    readonly linkedSchedules: outputs.GetScheduleSyncTargetLinkedSchedule[];
    /**
     * Slack team (workspace) ID the user group lives in. On Enterprise Grid this identifies which workspace within the org the group belongs to.
     */
    readonly slackTeamId: string;
    /**
     * Slack ID of the user group whose membership is kept in sync. This is the Slack-assigned group ID (starting with 'S'), not the @-handle.
     */
    readonly slackUserGroupId: string;
}
/**
 * Manage schedule sync targets (Slack user groups that schedules can sync to).
 *
 * Use this data source to look up an existing schedule sync target, either by `id` or by `slackUserGroupId`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up a schedule sync target by ID.
 * const byId = incident.getScheduleSyncTarget({
 *     id: "01ABC123DEF456GHI789JKL",
 * });
 * // ...or by the Slack user group it keeps in sync. The group's Slack-assigned
 * // ID starts with 'S'; this is not the @-handle.
 * const bySlackGroup = incident.getScheduleSyncTarget({
 *     slackUserGroupId: "S06MNNU5BMK",
 * });
 * ```
 */
export function getScheduleSyncTargetOutput(args?: GetScheduleSyncTargetOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetScheduleSyncTargetResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getScheduleSyncTarget:getScheduleSyncTarget", {
        "id": args.id,
        "slackUserGroupId": args.slackUserGroupId,
    }, opts);
}

/**
 * A collection of arguments for invoking getScheduleSyncTarget.
 */
export interface GetScheduleSyncTargetOutputArgs {
    /**
     * Unique identifier of the sync target
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * Slack ID of the user group whose membership is kept in sync. This is the Slack-assigned group ID (starting with 'S'), not the @-handle.
     */
    slackUserGroupId?: pulumi.Input<string | undefined>;
}
