// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";

export interface AlertRouteAlertSource {
    /**
     * The alert source ID that will match for the route
     */
    alertSourceId: string;
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertRouteAlertSourceConditionGroup[];
}

export interface AlertRouteAlertSourceConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertRouteAlertSourceConditionGroupCondition[];
}

export interface AlertRouteAlertSourceConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertRouteAlertSourceConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertRouteAlertSourceConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteAlertSourceConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteAlertSourceConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteAlertSourceConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteAlertSourceConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteChannelConfig {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertRouteChannelConfigConditionGroup[];
    msTeamsTargets?: outputs.AlertRouteChannelConfigMsTeamsTargets;
    slackTargets?: outputs.AlertRouteChannelConfigSlackTargets;
}

export interface AlertRouteChannelConfigConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertRouteChannelConfigConditionGroupCondition[];
}

export interface AlertRouteChannelConfigConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertRouteChannelConfigConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertRouteChannelConfigConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteChannelConfigConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteChannelConfigConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteChannelConfigConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteChannelConfigConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteChannelConfigMsTeamsTargets {
    binding: outputs.AlertRouteChannelConfigMsTeamsTargetsBinding;
    /**
     * The visibility of the channel
     */
    channelVisibility: string;
}

export interface AlertRouteChannelConfigMsTeamsTargetsBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteChannelConfigMsTeamsTargetsBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteChannelConfigMsTeamsTargetsBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteChannelConfigMsTeamsTargetsBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteChannelConfigMsTeamsTargetsBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteChannelConfigSlackTargets {
    binding: outputs.AlertRouteChannelConfigSlackTargetsBinding;
    /**
     * The visibility of the channel
     */
    channelVisibility: string;
}

export interface AlertRouteChannelConfigSlackTargetsBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteChannelConfigSlackTargetsBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteChannelConfigSlackTargetsBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteChannelConfigSlackTargetsBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteChannelConfigSlackTargetsBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertRouteConditionGroupCondition[];
}

export interface AlertRouteConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertRouteConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertRouteConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteEscalationConfig {
    /**
     * Should we auto cancel escalations when all alerts are resolved?
     */
    autoCancelEscalations: boolean;
    /**
     * Targets for escalation
     */
    escalationTargets: outputs.AlertRouteEscalationConfigEscalationTarget[];
    whenAlertJoinsGroup: outputs.AlertRouteEscalationConfigWhenAlertJoinsGroup;
}

export interface AlertRouteEscalationConfigEscalationTarget {
    escalationPaths?: outputs.AlertRouteEscalationConfigEscalationTargetEscalationPaths;
    users?: outputs.AlertRouteEscalationConfigEscalationTargetUsers;
}

export interface AlertRouteEscalationConfigEscalationTargetEscalationPaths {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteEscalationConfigEscalationTargetEscalationPathsArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteEscalationConfigEscalationTargetEscalationPathsValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteEscalationConfigEscalationTargetEscalationPathsArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteEscalationConfigEscalationTargetEscalationPathsValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteEscalationConfigEscalationTargetUsers {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteEscalationConfigEscalationTargetUsersArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteEscalationConfigEscalationTargetUsersValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteEscalationConfigEscalationTargetUsersArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteEscalationConfigEscalationTargetUsersValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteEscalationConfigWhenAlertJoinsGroup {
    /**
     * How long to wait before escalating once an alert joins the group, in seconds. Only applies when mode is 'on*each*new_alert'.
     */
    gracePeriodSeconds?: number;
    /**
     * When a subsequent alert joins an existing group, when should we escalate again? Possible values are: `onPriorityIncrease`, `onEachNewAlert`.
     */
    mode: string;
}

export interface AlertRouteExpression {
    /**
     * The else branch to resort to if all operations fail
     */
    elseBranch?: outputs.AlertRouteExpressionElseBranch;
    /**
     * The human readable label of the expression
     */
    label: string;
    /**
     * The operations to execute in sequence for this expression
     */
    operations: outputs.AlertRouteExpressionOperation[];
    /**
     * A short ID that can be used to reference the expression
     */
    reference: string;
    /**
     * The root reference for this expression (i.e. where the expression starts)
     */
    rootReference: string;
}

export interface AlertRouteExpressionElseBranch {
    /**
     * The result assumed if the else branch is reached
     */
    result: outputs.AlertRouteExpressionElseBranchResult;
}

export interface AlertRouteExpressionElseBranchResult {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteExpressionElseBranchResultArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteExpressionElseBranchResultValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteExpressionElseBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionElseBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionOperation {
    /**
     * An operation type that allows for a value to be set conditionally by a series of logical branches
     */
    branches?: outputs.AlertRouteExpressionOperationBranches;
    /**
     * An operation type that converts a value into another type. Only valid on values that can be represented as text. The returned `array` follows the value being cast, so it must match the cardinality of the previous operation
     */
    cast?: outputs.AlertRouteExpressionOperationCast;
    /**
     * An operation type that adds the values behind another reference to the current value, keeping each value once. There is no delimiter, despite the name
     */
    concatenate?: outputs.AlertRouteExpressionOperationConcatenate;
    /**
     * An operation type that allows values to be filtered out by conditions
     */
    filter?: outputs.AlertRouteExpressionOperationFilter;
    /**
     * An operation type that allows attributes of a type to be accessed by reference
     */
    navigate?: outputs.AlertRouteExpressionOperationNavigate;
    /**
     * Indicates which operation type to execute. Possible values are: `navigate`, `filter`, `concatenate`, `count`, `min`, `max`, `sum`, `random`, `first`, `parse`, `branches`, `cast`.
     */
    operationType: string;
    /**
     * An operation type that allows a value to parsed from within a JSON object
     */
    parse?: outputs.AlertRouteExpressionOperationParse;
}

export interface AlertRouteExpressionOperationBranches {
    /**
     * The branches to apply for this operation
     */
    branches: outputs.AlertRouteExpressionOperationBranchesBranch[];
    /**
     * The return type of an operation
     */
    returns: outputs.AlertRouteExpressionOperationBranchesReturns;
}

export interface AlertRouteExpressionOperationBranchesBranch {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertRouteExpressionOperationBranchesBranchConditionGroup[];
    /**
     * The result assumed if the condition groups are satisfied
     */
    result: outputs.AlertRouteExpressionOperationBranchesBranchResult;
}

export interface AlertRouteExpressionOperationBranchesBranchConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertRouteExpressionOperationBranchesBranchConditionGroupCondition[];
}

export interface AlertRouteExpressionOperationBranchesBranchConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertRouteExpressionOperationBranchesBranchConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertRouteExpressionOperationBranchesBranchConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionOperationBranchesBranchResult {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteExpressionOperationBranchesBranchResultArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteExpressionOperationBranchesBranchResultValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteExpressionOperationBranchesBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionOperationBranchesBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionOperationBranchesReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface AlertRouteExpressionOperationCast {
    /**
     * The return type of an operation
     */
    returns: outputs.AlertRouteExpressionOperationCastReturns;
}

export interface AlertRouteExpressionOperationCastReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface AlertRouteExpressionOperationConcatenate {
    /**
     * The reference within the scope to concatenate with
     */
    reference: string;
}

export interface AlertRouteExpressionOperationFilter {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertRouteExpressionOperationFilterConditionGroup[];
}

export interface AlertRouteExpressionOperationFilterConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertRouteExpressionOperationFilterConditionGroupCondition[];
}

export interface AlertRouteExpressionOperationFilterConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertRouteExpressionOperationFilterConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertRouteExpressionOperationFilterConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteExpressionOperationFilterConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteExpressionOperationFilterConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteExpressionOperationFilterConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionOperationFilterConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteExpressionOperationNavigate {
    reference: string;
}

export interface AlertRouteExpressionOperationParse {
    /**
     * The return type of an operation
     */
    returns: outputs.AlertRouteExpressionOperationParseReturns;
    /**
     * The ES5 Javascript expression to execute
     */
    source: string;
}

export interface AlertRouteExpressionOperationParseReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface AlertRouteGroupingConfig {
    default: outputs.AlertRouteGroupingConfigDefault;
}

export interface AlertRouteGroupingConfigDefault {
    /**
     * Whether grouping is enabled
     */
    enabled: boolean;
    /**
     * Which attributes should this alert route use to group alerts? Only set when grouping is enabled.
     */
    groupingKeys?: outputs.AlertRouteGroupingConfigDefaultGroupingKey[];
    /**
     * How long the grouping window is, in seconds. Must be between 60 (1 minute) and 172800 (48 hours). Only set when grouping is enabled.
     */
    windowSeconds?: number;
    /**
     * Controls how the grouping window behaves. 'rolling' keeps the window open for window*seconds after the most recent alert, so the group stays open as long as alerts keep arriving. 'fixed' opens the window when the first alert arrives and always closes window*seconds later, regardless of any subsequent alerts. Only set when grouping is enabled. Possible values are: `rolling`, `fixed`.
     */
    windowType?: string;
}

export interface AlertRouteGroupingConfigDefaultGroupingKey {
    /**
     * A reference to a property of the alert to group on
     */
    reference: string;
}

export interface AlertRouteIncidentConfig {
    /**
     * Should triage incidents be declined when alerts are resolved?
     */
    autoDeclineEnabled?: boolean;
    /**
     * Should grouped alerts automatically be related to active incidents without confirmation?
     *
     * @deprecated Deprecated: use `escalation_config.when_alert_joins_group.mode` instead. See v5.41.0 in the CHANGELOG for migration guidance: https://github.com/incident-io/terraform-provider-incident/blob/master/CHANGELOG.md
     */
    autoRelateGroupedAlerts: boolean;
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertRouteIncidentConfigConditionGroup[];
    /**
     * How long should the escalation defer time be?
     *
     * Deprecated: set the escalation grace period via `escalation_config.when_alert_joins_group.grace_period_seconds`. See v5.41.0 in the CHANGELOG for migration guidance: https://github.com/incident-io/terraform-provider-incident/blob/master/CHANGELOG.md
     *
     * @deprecated Deprecated: set the escalation grace period via `escalation_config.when_alert_joins_group.grace_period_seconds`. See v5.41.0 in the CHANGELOG for migration guidance: https://github.com/incident-io/terraform-provider-incident/blob/master/CHANGELOG.md
     */
    deferTimeSeconds?: number;
    /**
     * Whether incident creation is enabled for this alert route
     */
    enabled: boolean;
    /**
     * Which attributes should this alert route use to group alerts?
     *
     * Deprecated: set grouping keys via `grouping_config.default.grouping_keys` instead. See v5.41.0 in the CHANGELOG for migration guidance: https://github.com/incident-io/terraform-provider-incident/blob/master/CHANGELOG.md
     *
     * @deprecated Deprecated: set grouping keys via `grouping_config.default.grouping_keys` instead. See v5.41.0 in the CHANGELOG for migration guidance: https://github.com/incident-io/terraform-provider-incident/blob/master/CHANGELOG.md
     */
    groupingKeys?: outputs.AlertRouteIncidentConfigGroupingKey[];
    /**
     * How large should the grouping window be?
     *
     * Deprecated: set the grouping window via `grouping_config.default.window_seconds` instead. See v5.41.0 in the CHANGELOG for migration guidance: https://github.com/incident-io/terraform-provider-incident/blob/master/CHANGELOG.md
     *
     * @deprecated Deprecated: set the grouping window via `grouping_config.default.window_seconds` instead. See v5.41.0 in the CHANGELOG for migration guidance: https://github.com/incident-io/terraform-provider-incident/blob/master/CHANGELOG.md
     */
    groupingWindowSeconds?: number;
    /**
     * The template an alert route applies to the incidents it creates. Disabling incident creation clears it. Only used with `groupingConfig`.
     */
    template?: outputs.AlertRouteIncidentConfigTemplate;
}

export interface AlertRouteIncidentConfigConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertRouteIncidentConfigConditionGroupCondition[];
}

export interface AlertRouteIncidentConfigConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertRouteIncidentConfigConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertRouteIncidentConfigConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentConfigConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigGroupingKey {
    /**
     * The alert attribute ID to use as a grouping key
     */
    reference: string;
}

export interface AlertRouteIncidentConfigTemplate {
    /**
     * Custom fields configuration
     */
    customFields?: outputs.AlertRouteIncidentConfigTemplateCustomField[];
    incidentMode?: outputs.AlertRouteIncidentConfigTemplateIncidentMode;
    incidentType?: outputs.AlertRouteIncidentConfigTemplateIncidentType;
    name: outputs.AlertRouteIncidentConfigTemplateName;
    severity: outputs.AlertRouteIncidentConfigTemplateSeverity;
    startInTriage?: outputs.AlertRouteIncidentConfigTemplateStartInTriage;
    summary: outputs.AlertRouteIncidentConfigTemplateSummary;
}

export interface AlertRouteIncidentConfigTemplateCustomField {
    /**
     * Binding for the custom field
     */
    binding: outputs.AlertRouteIncidentConfigTemplateCustomFieldBinding;
    /**
     * ID of the custom field
     */
    customFieldId: string;
    /**
     * The strategy to use when multiple alerts match this route. Possible values are: `first-wins`, `last-wins`, `append`.
     */
    mergeStrategy: string;
}

export interface AlertRouteIncidentConfigTemplateCustomFieldBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigTemplateCustomFieldBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigTemplateCustomFieldBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentConfigTemplateCustomFieldBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateCustomFieldBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateIncidentMode {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigTemplateIncidentModeArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigTemplateIncidentModeValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentConfigTemplateIncidentModeArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateIncidentModeValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateIncidentType {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigTemplateIncidentTypeArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigTemplateIncidentTypeValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentConfigTemplateIncidentTypeArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateIncidentTypeValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateName {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigTemplateNameArrayValue[];
    /**
     * Whether this attribute should be autogenerated using AI
     */
    autogenerated: boolean;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigTemplateNameValue;
}

export interface AlertRouteIncidentConfigTemplateNameArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateNameValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateSeverity {
    binding?: outputs.AlertRouteIncidentConfigTemplateSeverityBinding;
    /**
     * Strategy for merging severity when multiple alerts create/update the same incident. Possible values are: `first-wins`, `max`.
     */
    mergeStrategy: string;
}

export interface AlertRouteIncidentConfigTemplateSeverityBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigTemplateSeverityBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigTemplateSeverityBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentConfigTemplateSeverityBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateSeverityBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateStartInTriage {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigTemplateStartInTriageArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigTemplateStartInTriageValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentConfigTemplateStartInTriageArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateStartInTriageValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateSummary {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentConfigTemplateSummaryArrayValue[];
    /**
     * Whether this attribute should be autogenerated using AI
     */
    autogenerated: boolean;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentConfigTemplateSummaryValue;
}

export interface AlertRouteIncidentConfigTemplateSummaryArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentConfigTemplateSummaryValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplate {
    /**
     * Custom fields configuration
     */
    customFields?: outputs.AlertRouteIncidentTemplateCustomField[];
    incidentMode?: outputs.AlertRouteIncidentTemplateIncidentMode;
    incidentType?: outputs.AlertRouteIncidentTemplateIncidentType;
    name: outputs.AlertRouteIncidentTemplateName;
    severity: outputs.AlertRouteIncidentTemplateSeverity;
    startInTriage?: outputs.AlertRouteIncidentTemplateStartInTriage;
    summary: outputs.AlertRouteIncidentTemplateSummary;
    workspace?: outputs.AlertRouteIncidentTemplateWorkspace;
}

export interface AlertRouteIncidentTemplateCustomField {
    /**
     * Binding for the custom field
     */
    binding: outputs.AlertRouteIncidentTemplateCustomFieldBinding;
    /**
     * ID of the custom field
     */
    customFieldId: string;
    /**
     * The strategy to use when multiple alerts match this route. Possible values are: `first-wins`, `last-wins`, `append`.
     */
    mergeStrategy: string;
}

export interface AlertRouteIncidentTemplateCustomFieldBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateCustomFieldBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateCustomFieldBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentTemplateCustomFieldBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateCustomFieldBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateIncidentMode {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateIncidentModeArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateIncidentModeValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentTemplateIncidentModeArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateIncidentModeValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateIncidentType {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateIncidentTypeArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateIncidentTypeValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentTemplateIncidentTypeArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateIncidentTypeValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateName {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateNameArrayValue[];
    /**
     * Whether this attribute should be autogenerated using AI
     */
    autogenerated: boolean;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateNameValue;
}

export interface AlertRouteIncidentTemplateNameArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateNameValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateSeverity {
    binding?: outputs.AlertRouteIncidentTemplateSeverityBinding;
    /**
     * Strategy for merging severity when multiple alerts create/update the same incident. Possible values are: `first-wins`, `max`.
     */
    mergeStrategy: string;
}

export interface AlertRouteIncidentTemplateSeverityBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateSeverityBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateSeverityBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentTemplateSeverityBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateSeverityBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateStartInTriage {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateStartInTriageArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateStartInTriageValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentTemplateStartInTriageArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateStartInTriageValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateSummary {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateSummaryArrayValue[];
    /**
     * Whether this attribute should be autogenerated using AI
     */
    autogenerated: boolean;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateSummaryValue;
}

export interface AlertRouteIncidentTemplateSummaryArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateSummaryValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateWorkspace {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteIncidentTemplateWorkspaceArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteIncidentTemplateWorkspaceValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteIncidentTemplateWorkspaceArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteIncidentTemplateWorkspaceValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfig {
    /**
     * The destinations (Slack/Teams channels) alert messages are sent to
     */
    destinations?: outputs.AlertRouteMessageConfigDestination[];
    template?: outputs.AlertRouteMessageConfigTemplate;
}

export interface AlertRouteMessageConfigDestination {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertRouteMessageConfigDestinationConditionGroup[];
    msTeamsTargets?: outputs.AlertRouteMessageConfigDestinationMsTeamsTargets;
    slackTargets?: outputs.AlertRouteMessageConfigDestinationSlackTargets;
}

export interface AlertRouteMessageConfigDestinationConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertRouteMessageConfigDestinationConditionGroupCondition[];
}

export interface AlertRouteMessageConfigDestinationConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertRouteMessageConfigDestinationConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertRouteMessageConfigDestinationConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteMessageConfigDestinationConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteMessageConfigDestinationConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteMessageConfigDestinationConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfigDestinationConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfigDestinationMsTeamsTargets {
    binding: outputs.AlertRouteMessageConfigDestinationMsTeamsTargetsBinding;
    /**
     * The visibility of the channel
     */
    channelVisibility: string;
    /**
     * Whether grouped alerts should render as a single group-summary message per channel
     */
    groupAlertsSummary: boolean;
}

export interface AlertRouteMessageConfigDestinationMsTeamsTargetsBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteMessageConfigDestinationMsTeamsTargetsBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteMessageConfigDestinationMsTeamsTargetsBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteMessageConfigDestinationMsTeamsTargetsBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfigDestinationMsTeamsTargetsBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfigDestinationSlackTargets {
    binding: outputs.AlertRouteMessageConfigDestinationSlackTargetsBinding;
    /**
     * The visibility of the channel
     */
    channelVisibility: string;
    /**
     * Whether grouped alerts should render as a single group-summary message per channel
     */
    groupAlertsSummary: boolean;
}

export interface AlertRouteMessageConfigDestinationSlackTargetsBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteMessageConfigDestinationSlackTargetsBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteMessageConfigDestinationSlackTargetsBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteMessageConfigDestinationSlackTargetsBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfigDestinationSlackTargetsBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfigTemplate {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteMessageConfigTemplateArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteMessageConfigTemplateValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteMessageConfigTemplateArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageConfigTemplateValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageTemplate {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertRouteMessageTemplateArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertRouteMessageTemplateValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertRouteMessageTemplateArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertRouteMessageTemplateValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpression {
    /**
     * What this expression produces when nothing else matched.
     */
    fallback?: outputs.AlertSourceAttributeBetaExpressionFallback;
    /**
     * An ordered pipeline. Each operation feeds the next.
     */
    operations?: outputs.AlertSourceAttributeBetaExpressionOperation[];
    /**
     * Where the expression starts: "payload", "alert", "." for a branches-only expression, or a scope path. Required when the block is present.
     */
    startFrom?: string;
}

export interface AlertSourceAttributeBetaExpressionFallback {
    /**
     * The unconditional default for the shorthand above.
     */
    else?: outputs.AlertSourceAttributeBetaExpressionFallbackElse;
    /**
     * Tried in order, after if.
     */
    elseIfs?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIf[];
    /**
     * The name of a namedExpression in this resource.
     */
    expressionRef?: string;
    /**
     * Shorthand for a branching fallback.
     */
    if?: outputs.AlertSourceAttributeBetaExpressionFallbackIf;
    /**
     * A flat, unconditional value.
     */
    result?: outputs.AlertSourceAttributeBetaExpressionFallbackResult;
}

export interface AlertSourceAttributeBetaExpressionFallbackElse {
    /**
     * The value to fall back to. Required when the block is present.
     */
    result?: outputs.AlertSourceAttributeBetaExpressionFallbackElseResult;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfResult;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackElseIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackElseResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackElseResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackElseResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackElseResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaExpressionFallbackIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaExpressionFallbackIfResult;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionFallbackResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionFallbackResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionFallbackResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionFallbackResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperation {
    /**
     * A lookup table, evaluated in order until one matches. Must be the only operation in its expression, with startFrom = ".".
     */
    branches?: outputs.AlertSourceAttributeBetaExpressionOperationBranches;
    /**
     * Converts the current value to another type.
     */
    cast?: outputs.AlertSourceAttributeBetaExpressionOperationCast;
    /**
     * Adds the values behind another reference to the current value, keeping each value once. There is no delimiter, despite the name.
     */
    concatenate?: outputs.AlertSourceAttributeBetaExpressionOperationConcatenate;
    /**
     * Counts the values.
     */
    count?: outputs.AlertSourceAttributeBetaExpressionOperationCount;
    /**
     * Keeps the values matching these conditions. Inside a filter the value under test is bound as `input`.
     */
    filter?: outputs.AlertSourceAttributeBetaExpressionOperationFilter;
    /**
     * Takes the first value.
     */
    first?: outputs.AlertSourceAttributeBetaExpressionOperationFirst;
    /**
     * Takes the largest value.
     */
    max?: outputs.AlertSourceAttributeBetaExpressionOperationMax;
    /**
     * Takes the smallest value.
     */
    min?: outputs.AlertSourceAttributeBetaExpressionOperationMin;
    /**
     * Follows an attribute of the current value.
     */
    navigate?: outputs.AlertSourceAttributeBetaExpressionOperationNavigate;
    /**
     * Evaluates a function against the current value.
     */
    parse?: outputs.AlertSourceAttributeBetaExpressionOperationParse;
    /**
     * Takes one value at random.
     */
    random?: outputs.AlertSourceAttributeBetaExpressionOperationRandom;
    /**
     * Adds the values together.
     */
    sum?: outputs.AlertSourceAttributeBetaExpressionOperationSum;
}

export interface AlertSourceAttributeBetaExpressionOperationBranches {
    /**
     * Whether each branch returns several values rather than one.
     */
    array?: boolean;
    /**
     * The type every branch result returns. Required when the block is present.
     */
    as?: string;
    /**
     * Tried in order, after if.
     */
    elseIfs?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIf[];
    /**
     * The first branch to try.
     */
    if?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIf;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfResult;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesElseIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesElseIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfResult;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationBranchesIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationBranchesIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationCast {
    /**
     * The type to convert to. Take it from the resource that defines the type rather than writing it out.
     */
    as: string;
}

export interface AlertSourceAttributeBetaExpressionOperationConcatenate {
    /**
     * The reference whose values are added.
     */
    with: string;
}

export interface AlertSourceAttributeBetaExpressionOperationCount {
}

export interface AlertSourceAttributeBetaExpressionOperationFilter {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaExpressionOperationFilterCondition[];
}

export interface AlertSourceAttributeBetaExpressionOperationFilterCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaExpressionOperationFilterConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationFilterConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaExpressionOperationFirst {
}

export interface AlertSourceAttributeBetaExpressionOperationMax {
}

export interface AlertSourceAttributeBetaExpressionOperationMin {
}

export interface AlertSourceAttributeBetaExpressionOperationNavigate {
    /**
     * The catalog attribute to follow.
     */
    to: string;
}

export interface AlertSourceAttributeBetaExpressionOperationParse {
    /**
     * Whether this returns several values rather than one.
     */
    array?: boolean;
    /**
     * The type this returns. Take it from the resource that defines the type rather than writing it out, e.g. `incident_catalog_type.service.attribute_type`.
     */
    as: string;
    /**
     * JavaScript evaluated against the current value, bound to `$`. 5 KiB limit.
     */
    function: string;
}

export interface AlertSourceAttributeBetaExpressionOperationRandom {
}

export interface AlertSourceAttributeBetaExpressionOperationSum {
}

export interface AlertSourceAttributeBetaNamedExpression {
    /**
     * What this expression produces when nothing else matched.
     */
    fallback?: outputs.AlertSourceAttributeBetaNamedExpressionFallback;
    /**
     * What the dashboard shows for this expression. Defaults to the name. Set it when importing a source whose expressions were labelled in the dashboard, so those labels survive.
     */
    label?: string;
    /**
     * A name for this expression, unique within this resource, referenced by expression_ref.
     */
    name: string;
    /**
     * An ordered pipeline. Each operation feeds the next.
     */
    operations?: outputs.AlertSourceAttributeBetaNamedExpressionOperation[];
    /**
     * Where the expression starts: "payload", "alert", "." for a branches-only expression, or a scope path. Required when the block is present.
     */
    startFrom?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallback {
    /**
     * The unconditional default for the shorthand above.
     */
    else?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElse;
    /**
     * Tried in order, after if.
     */
    elseIfs?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIf[];
    /**
     * The name of a namedExpression in this resource.
     */
    expressionRef?: string;
    /**
     * Shorthand for a branching fallback.
     */
    if?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIf;
    /**
     * A flat, unconditional value.
     */
    result?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackResult;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElse {
    /**
     * The value to fall back to. Required when the block is present.
     */
    result?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseResult;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfResult;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackElseResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackElseResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfResult;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionFallbackResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionFallbackResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperation {
    /**
     * A lookup table, evaluated in order until one matches. Must be the only operation in its expression, with start*from = ".".
     */
    branches?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranches;
    /**
     * Converts the current value to another type.
     */
    cast?: outputs.AlertSourceAttributeBetaNamedExpressionOperationCast;
    /**
     * Adds the values behind another reference to the current value, keeping each value once. There is no delimiter, despite the name.
     */
    concatenate?: outputs.AlertSourceAttributeBetaNamedExpressionOperationConcatenate;
    /**
     * Counts the values.
     */
    count?: outputs.AlertSourceAttributeBetaNamedExpressionOperationCount;
    /**
     * Keeps the values matching these conditions. Inside a filter the value under test is bound as `input`.
     */
    filter?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilter;
    /**
     * Takes the first value.
     */
    first?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFirst;
    /**
     * Takes the largest value.
     */
    max?: outputs.AlertSourceAttributeBetaNamedExpressionOperationMax;
    /**
     * Takes the smallest value.
     */
    min?: outputs.AlertSourceAttributeBetaNamedExpressionOperationMin;
    /**
     * Follows an attribute of the current value.
     */
    navigate?: outputs.AlertSourceAttributeBetaNamedExpressionOperationNavigate;
    /**
     * Evaluates a function against the current value.
     */
    parse?: outputs.AlertSourceAttributeBetaNamedExpressionOperationParse;
    /**
     * Takes one value at random.
     */
    random?: outputs.AlertSourceAttributeBetaNamedExpressionOperationRandom;
    /**
     * Adds the values together.
     */
    sum?: outputs.AlertSourceAttributeBetaNamedExpressionOperationSum;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranches {
    /**
     * Whether each branch returns several values rather than one.
     */
    array?: boolean;
    /**
     * The type every branch result returns. Required when the block is present.
     */
    as?: string;
    /**
     * Tried in order, after if.
     */
    elseIfs?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIf[];
    /**
     * The first branch to try.
     */
    if?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIf;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfResult;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesElseIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfResult;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationBranchesIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationBranchesIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationCast {
    /**
     * The type to convert to. Take it from the resource that defines the type rather than writing it out.
     */
    as: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationConcatenate {
    /**
     * The reference whose values are added.
     */
    with: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationCount {
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilter {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterCondition[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupCondition[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceAttributeBetaNamedExpressionOperationFilterConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFilterConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationFirst {
}

export interface AlertSourceAttributeBetaNamedExpressionOperationMax {
}

export interface AlertSourceAttributeBetaNamedExpressionOperationMin {
}

export interface AlertSourceAttributeBetaNamedExpressionOperationNavigate {
    /**
     * The catalog attribute to follow.
     */
    to: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationParse {
    /**
     * Whether this returns several values rather than one.
     */
    array?: boolean;
    /**
     * The type this returns. Take it from the resource that defines the type rather than writing it out, e.g. `incident_catalog_type.service.attribute_type`.
     */
    as: string;
    /**
     * JavaScript evaluated against the current value, bound to `$`. 5 KiB limit.
     */
    function: string;
}

export interface AlertSourceAttributeBetaNamedExpressionOperationRandom {
}

export interface AlertSourceAttributeBetaNamedExpressionOperationSum {
}

export interface AlertSourceAttributeBetaValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaDescription {
    /**
     * Fixed content, which may interpolate the scope with `{{ variable }}`. Filters `truncate: N` and `omitIfUnset` are supported. For content needing formatting a template can't express, pass a document from `data.incident_rich_text` with `featureSet = "rich"`.
     */
    literal?: string;
    /**
     * A reference into the scope whose value becomes the content, such as `payload.summary`. Prefer a literal interpolating `{{ payload.summary }}`, which can also carry surrounding text.
     */
    reference?: string;
}

export interface AlertSourceBetaEmailOptions {
    /**
     * Which PII types to automatically redact from incoming email content before storage. Possible values are: `creditCardNumbers`, `usSocialSecurityNumbers`, `phoneNumbers`.
     */
    redactions: string[];
    /**
     * JavaScript expression to transform email fields into structured alert fields
     */
    transformExpression?: string;
}

export interface AlertSourceBetaHeartbeatOptions {
    /**
     * Number of consecutive missed pings before an alert fires.
     */
    failureThreshold: number;
    /**
     * How long after a missed ping before the heartbeat is considered late, in seconds. If zero, it transitions directly to failing.
     */
    gracePeriodSeconds: number;
    /**
     * How often a ping is expected, in seconds.
     */
    intervalSeconds: number;
    /**
     * The URL to POST to in order to send a heartbeat ping.
     */
    pingUrl: string;
}

export interface AlertSourceBetaHttpCustomOptions {
    /**
     * JSON path to extract the deduplication key from the payload
     */
    deduplicationKeyPath: string;
    /**
     * JavaScript expression that returns an object with all alert fields
     */
    transformExpression: string;
}

export interface AlertSourceBetaJiraOptions {
    /**
     * Which projects in Jira should this alert source watch for new issues? IDs can either be IDs of the projects in Jira, or ID of catalog entries in the 'Jira Project' catalog type.
     */
    projectIds: string[];
}

export interface AlertSourceBetaNamedExpression {
    /**
     * What this expression produces when nothing else matched.
     */
    fallback?: outputs.AlertSourceBetaNamedExpressionFallback;
    /**
     * What the dashboard shows for this expression. Defaults to the name. Set it when importing a source whose expressions were labelled in the dashboard, so those labels survive.
     */
    label?: string;
    /**
     * A name for this expression, unique within this resource, referenced by expression_ref.
     */
    name: string;
    /**
     * An ordered pipeline. Each operation feeds the next.
     */
    operations?: outputs.AlertSourceBetaNamedExpressionOperation[];
    /**
     * Where the expression starts: "payload", "alert", "." for a branches-only expression, or a scope path. Required when the block is present.
     */
    startFrom?: string;
}

export interface AlertSourceBetaNamedExpressionFallback {
    /**
     * The unconditional default for the shorthand above.
     */
    else?: outputs.AlertSourceBetaNamedExpressionFallbackElse;
    /**
     * Tried in order, after if.
     */
    elseIfs?: outputs.AlertSourceBetaNamedExpressionFallbackElseIf[];
    /**
     * The name of a namedExpression in this resource.
     */
    expressionRef?: string;
    /**
     * Shorthand for a branching fallback.
     */
    if?: outputs.AlertSourceBetaNamedExpressionFallbackIf;
    /**
     * A flat, unconditional value.
     */
    result?: outputs.AlertSourceBetaNamedExpressionFallbackResult;
}

export interface AlertSourceBetaNamedExpressionFallbackElse {
    /**
     * The value to fall back to. Required when the block is present.
     */
    result?: outputs.AlertSourceBetaNamedExpressionFallbackElseResult;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfResult;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupCondition[];
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackElseIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackElseResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackElseResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackElseResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackElseResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceBetaNamedExpressionFallbackIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceBetaNamedExpressionFallbackIfResult;
}

export interface AlertSourceBetaNamedExpressionFallbackIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionGroupCondition[];
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionFallbackResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionFallbackResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionFallbackResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionFallbackResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperation {
    /**
     * A lookup table, evaluated in order until one matches. Must be the only operation in its expression, with start*from = ".".
     */
    branches?: outputs.AlertSourceBetaNamedExpressionOperationBranches;
    /**
     * Converts the current value to another type.
     */
    cast?: outputs.AlertSourceBetaNamedExpressionOperationCast;
    /**
     * Adds the values behind another reference to the current value, keeping each value once. There is no delimiter, despite the name.
     */
    concatenate?: outputs.AlertSourceBetaNamedExpressionOperationConcatenate;
    /**
     * Counts the values.
     */
    count?: outputs.AlertSourceBetaNamedExpressionOperationCount;
    /**
     * Keeps the values matching these conditions. Inside a filter the value under test is bound as `input`.
     */
    filter?: outputs.AlertSourceBetaNamedExpressionOperationFilter;
    /**
     * Takes the first value.
     */
    first?: outputs.AlertSourceBetaNamedExpressionOperationFirst;
    /**
     * Takes the largest value.
     */
    max?: outputs.AlertSourceBetaNamedExpressionOperationMax;
    /**
     * Takes the smallest value.
     */
    min?: outputs.AlertSourceBetaNamedExpressionOperationMin;
    /**
     * Follows an attribute of the current value.
     */
    navigate?: outputs.AlertSourceBetaNamedExpressionOperationNavigate;
    /**
     * Evaluates a function against the current value.
     */
    parse?: outputs.AlertSourceBetaNamedExpressionOperationParse;
    /**
     * Takes one value at random.
     */
    random?: outputs.AlertSourceBetaNamedExpressionOperationRandom;
    /**
     * Adds the values together.
     */
    sum?: outputs.AlertSourceBetaNamedExpressionOperationSum;
}

export interface AlertSourceBetaNamedExpressionOperationBranches {
    /**
     * Whether each branch returns several values rather than one.
     */
    array?: boolean;
    /**
     * The type every branch result returns. Required when the block is present.
     */
    as?: string;
    /**
     * Tried in order, after if.
     */
    elseIfs?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIf[];
    /**
     * The first branch to try.
     */
    if?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIf;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfResult;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupCondition[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationBranchesElseIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesElseIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIf {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfCondition[];
    /**
     * The value this branch produces. Required when the branch is present.
     */
    result?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfResult;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupCondition[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfResult {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfResultArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationBranchesIfResultValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfResultArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationBranchesIfResultValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationCast {
    /**
     * The type to convert to. Take it from the resource that defines the type rather than writing it out.
     */
    as: string;
}

export interface AlertSourceBetaNamedExpressionOperationConcatenate {
    /**
     * The reference whose values are added.
     */
    with: string;
}

export interface AlertSourceBetaNamedExpressionOperationCount {
}

export interface AlertSourceBetaNamedExpressionOperationFilter {
    /**
     * Groups are OR'd; conditions within a group are AND'd.
     */
    conditionGroups?: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionGroup[];
    /**
     * All of these must hold. Sugar for a single condition group.
     */
    conditions?: outputs.AlertSourceBetaNamedExpressionOperationFilterCondition[];
}

export interface AlertSourceBetaNamedExpressionOperationFilterCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionGroup {
    /**
     * All of these must hold for the group to hold.
     */
    conditions: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionGroupCondition[];
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionGroupCondition {
    /**
     * How the subject is tested. The available operations depend on the subject's type.
     */
    operation: string;
    /**
     * Positional parameters for the operation.
     */
    params?: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionGroupConditionParam[];
    /**
     * The reference this condition tests.
     */
    subject: string;
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionGroupConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionGroupConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionGroupConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionGroupConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionGroupConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionParam {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionParamArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaNamedExpressionOperationFilterConditionParamValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionParamArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationFilterConditionParamValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaNamedExpressionOperationFirst {
}

export interface AlertSourceBetaNamedExpressionOperationMax {
}

export interface AlertSourceBetaNamedExpressionOperationMin {
}

export interface AlertSourceBetaNamedExpressionOperationNavigate {
    /**
     * The catalog attribute to follow.
     */
    to: string;
}

export interface AlertSourceBetaNamedExpressionOperationParse {
    /**
     * Whether this returns several values rather than one.
     */
    array?: boolean;
    /**
     * The type this returns. Take it from the resource that defines the type rather than writing it out, e.g. `incident_catalog_type.service.attribute_type`.
     */
    as: string;
    /**
     * JavaScript evaluated against the current value, bound to `$`. 5 KiB limit.
     */
    function: string;
}

export interface AlertSourceBetaNamedExpressionOperationRandom {
}

export interface AlertSourceBetaNamedExpressionOperationSum {
}

export interface AlertSourceBetaPriority {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaPriorityArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaPriorityValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaPriorityArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaPriorityValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaRateLimitSharding {
    /**
     * JSON path to a value that splits this source's rate limit into per-value buckets.
     */
    rateLimitShardKeyPath: string;
}

export interface AlertSourceBetaTitle {
    /**
     * Fixed content, which may interpolate the scope with `{{ variable }}`. Filters `truncate: N` and `omitIfUnset` are supported. For content needing formatting a template can't express, pass a document from `data.incident_rich_text` with `featureSet = "plainSingleLine"`.
     */
    literal?: string;
    /**
     * A reference into the scope whose value becomes the content, such as `payload.summary`. Prefer a literal interpolating `{{ payload.summary }}`, which can also carry surrounding text.
     */
    reference?: string;
}

export interface AlertSourceBetaVisibleToTeams {
    /**
     * Several values, spelled out. Needed when they mix fixed values and references.
     */
    arrayValues?: outputs.AlertSourceBetaVisibleToTeamsArrayValue[];
    /**
     * The name of a namedExpression in this resource, whose result becomes the value.
     */
    expressionRef?: string;
    /**
     * One value, spelled out. `valueLiteral` and `valueReference` are shorthand for this.
     */
    value?: outputs.AlertSourceBetaVisibleToTeamsValue;
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    valueReference?: string;
    /**
     * Several fixed values. For a mix of fixed values and references, use array_value.
     */
    values?: string[];
}

export interface AlertSourceBetaVisibleToTeamsArrayValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceBetaVisibleToTeamsValue {
    /**
     * A fixed value. A catalog entry ID is a literal, not a reference.
     */
    literal?: string;
    /**
     * A reference into the scope, such as `payload.team`.
     */
    reference?: string;
}

export interface AlertSourceEmailOptions {
    /**
     * Which PII types to automatically redact from incoming email content before storage. Possible values are: `creditCardNumbers`, `usSocialSecurityNumbers`, `phoneNumbers`.
     */
    redactions: string[];
    /**
     * JavaScript expression to transform email fields into structured alert fields
     */
    transformExpression?: string;
}

export interface AlertSourceHeartbeatOptions {
    /**
     * Number of consecutive missed pings before an alert fires.
     */
    failureThreshold: number;
    /**
     * How long after a missed ping before the heartbeat is considered late, in seconds. If zero, it transitions directly to failing.
     */
    gracePeriodSeconds: number;
    /**
     * How often a ping is expected, in seconds.
     */
    intervalSeconds: number;
    /**
     * The URL to POST to in order to send a heartbeat ping.
     */
    pingUrl: string;
}

export interface AlertSourceHttpCustomOptions {
    /**
     * JSON path to extract the deduplication key from the payload
     */
    deduplicationKeyPath: string;
    /**
     * JavaScript expression that returns an object with all alert fields
     */
    transformExpression: string;
}

export interface AlertSourceJiraOptions {
    /**
     * Which projects in Jira should this alert source watch for new issues? IDs can either be IDs of the projects in Jira, or ID of catalog entries in the 'Jira Project' catalog type.
     */
    projectIds?: string[];
}

export interface AlertSourceRateLimitSharding {
    /**
     * JSON path to a value that splits this source's rate limit into per-value buckets.
     */
    rateLimitShardKeyPath: string;
}

export interface AlertSourceTemplate {
    /**
     * Attributes to set on alerts coming from this source, with a binding describing how to set them.
     */
    attributes: outputs.AlertSourceTemplateAttribute[];
    description: outputs.AlertSourceTemplateDescription;
    /**
     * The expressions to be prepared for use by steps and conditions
     */
    expressions: outputs.AlertSourceTemplateExpression[];
    /**
     * Whether or not alerts produced by this source should be private
     */
    isPrivate: boolean;
    title: outputs.AlertSourceTemplateTitle;
    visibleToTeams?: outputs.AlertSourceTemplateVisibleToTeams;
}

export interface AlertSourceTemplateAttribute {
    /**
     * ID of the alert attribute to set with this binding
     */
    alertAttributeId: string;
    binding: outputs.AlertSourceTemplateAttributeBinding;
}

export interface AlertSourceTemplateAttributeBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertSourceTemplateAttributeBindingArrayValue[];
    /**
     * Merge strategy for this attribute when alert updates. Possible values are: `firstWins`, `lastWins`, `append`, `max`, `min`.
     */
    mergeStrategy: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertSourceTemplateAttributeBindingValue;
}

export interface AlertSourceTemplateAttributeBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateAttributeBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateDescription {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpression {
    /**
     * The else branch to resort to if all operations fail
     */
    elseBranch?: outputs.AlertSourceTemplateExpressionElseBranch;
    /**
     * The human readable label of the expression
     */
    label: string;
    /**
     * The operations to execute in sequence for this expression
     */
    operations: outputs.AlertSourceTemplateExpressionOperation[];
    /**
     * A short ID that can be used to reference the expression
     */
    reference: string;
    /**
     * The root reference for this expression (i.e. where the expression starts)
     */
    rootReference: string;
}

export interface AlertSourceTemplateExpressionElseBranch {
    /**
     * The result assumed if the else branch is reached
     */
    result: outputs.AlertSourceTemplateExpressionElseBranchResult;
}

export interface AlertSourceTemplateExpressionElseBranchResult {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertSourceTemplateExpressionElseBranchResultArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertSourceTemplateExpressionElseBranchResultValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertSourceTemplateExpressionElseBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionElseBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionOperation {
    /**
     * An operation type that allows for a value to be set conditionally by a series of logical branches
     */
    branches?: outputs.AlertSourceTemplateExpressionOperationBranches;
    /**
     * An operation type that converts a value into another type. Only valid on values that can be represented as text. The returned `array` follows the value being cast, so it must match the cardinality of the previous operation
     */
    cast?: outputs.AlertSourceTemplateExpressionOperationCast;
    /**
     * An operation type that adds the values behind another reference to the current value, keeping each value once. There is no delimiter, despite the name
     */
    concatenate?: outputs.AlertSourceTemplateExpressionOperationConcatenate;
    /**
     * An operation type that allows values to be filtered out by conditions
     */
    filter?: outputs.AlertSourceTemplateExpressionOperationFilter;
    /**
     * An operation type that allows attributes of a type to be accessed by reference
     */
    navigate?: outputs.AlertSourceTemplateExpressionOperationNavigate;
    /**
     * Indicates which operation type to execute. Possible values are: `navigate`, `filter`, `concatenate`, `count`, `min`, `max`, `sum`, `random`, `first`, `parse`, `branches`, `cast`.
     */
    operationType: string;
    /**
     * An operation type that allows a value to parsed from within a JSON object
     */
    parse?: outputs.AlertSourceTemplateExpressionOperationParse;
}

export interface AlertSourceTemplateExpressionOperationBranches {
    /**
     * The branches to apply for this operation
     */
    branches: outputs.AlertSourceTemplateExpressionOperationBranchesBranch[];
    /**
     * The return type of an operation
     */
    returns: outputs.AlertSourceTemplateExpressionOperationBranchesReturns;
}

export interface AlertSourceTemplateExpressionOperationBranchesBranch {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertSourceTemplateExpressionOperationBranchesBranchConditionGroup[];
    /**
     * The result assumed if the condition groups are satisfied
     */
    result: outputs.AlertSourceTemplateExpressionOperationBranchesBranchResult;
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupCondition[];
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchResult {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertSourceTemplateExpressionOperationBranchesBranchResultArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertSourceTemplateExpressionOperationBranchesBranchResultValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionOperationBranchesBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionOperationBranchesReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface AlertSourceTemplateExpressionOperationCast {
    /**
     * The return type of an operation
     */
    returns: outputs.AlertSourceTemplateExpressionOperationCastReturns;
}

export interface AlertSourceTemplateExpressionOperationCastReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface AlertSourceTemplateExpressionOperationConcatenate {
    /**
     * The reference within the scope to concatenate with
     */
    reference: string;
}

export interface AlertSourceTemplateExpressionOperationFilter {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.AlertSourceTemplateExpressionOperationFilterConditionGroup[];
}

export interface AlertSourceTemplateExpressionOperationFilterConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.AlertSourceTemplateExpressionOperationFilterConditionGroupCondition[];
}

export interface AlertSourceTemplateExpressionOperationFilterConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.AlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface AlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateExpressionOperationNavigate {
    reference: string;
}

export interface AlertSourceTemplateExpressionOperationParse {
    /**
     * The return type of an operation
     */
    returns: outputs.AlertSourceTemplateExpressionOperationParseReturns;
    /**
     * The ES5 Javascript expression to execute
     */
    source: string;
}

export interface AlertSourceTemplateExpressionOperationParseReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface AlertSourceTemplateTitle {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateVisibleToTeams {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.AlertSourceTemplateVisibleToTeamsArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.AlertSourceTemplateVisibleToTeamsValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface AlertSourceTemplateVisibleToTeamsArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface AlertSourceTemplateVisibleToTeamsValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface CatalogEntriesEntries {
    /**
     * Optional aliases that can be used to reference this entry
     */
    aliases: string[];
    /**
     * Map of attribute ID to attribute values. (Keys are the system-generated IDs.)
     */
    attributeValues: {[key: string]: outputs.CatalogEntriesEntriesAttributeValues};
    /**
     * ID of this catalog entry
     */
    id: string;
    /**
     * Name is the human readable name of this entry
     */
    name: string;
    /**
     * When catalog type is ranked, this is used to help order things
     */
    rank: number;
}

export interface CatalogEntriesEntriesAttributeValues {
    /**
     * The value of this element of the array, in a format suitable for this attribute type.
     */
    arrayValues?: string[];
    /**
     * The value of this attribute, in a format suitable for this attribute type.
     */
    value?: string;
}

export interface CatalogEntryAttributeValue {
    /**
     * The value of this element of the array, in a format suitable for this attribute type.
     */
    arrayValues?: string[];
    /**
     * The ID of this attribute, usually loaded from the incident*catalog*type_attribute resource.
     */
    attribute: string;
    /**
     * The value of this attribute, in a format suitable for this attribute type.
     */
    value?: string;
}

export interface CustomFieldFilterBy {
    /**
     * This must be an attribute of the catalog type of this custom field. It must be an attribute that points to another catalog type (so not a plain string, number, or boolean attribute).
     */
    catalogAttributeId: string;
    /**
     * This must be the ID of a custom field, which must have values of the same type as the attribute you are filtering by.
     */
    customFieldId: string;
}

export interface EscalationPathBetaRepeatConfig {
    /**
     * When true, incident activity resets the repeat timer.
     */
    delayRepeatOnActivity: boolean;
    /**
     * Number of seconds we'll wait before repeating an escalation.
     */
    repeatAfterSeconds: number;
}

export interface EscalationPathBetaSequences {
    /**
     * The nodes in this sequence, in the order they run.
     */
    nodes: outputs.EscalationPathBetaSequencesNode[];
}

export interface EscalationPathBetaSequencesNode {
    /**
     * Send the escalation down one of two sequences, depending on what `if` tests. A branch must be the last node in its sequence.
     */
    branch?: outputs.EscalationPathBetaSequencesNodeBranch;
    delay?: outputs.EscalationPathBetaSequencesNodeDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathBetaSequencesNodeEscalationPath;
    /**
     * An id for this node, unique within the escalation path, so a `loop` can name it. Leave it unset unless something loops back here: we derive one from the node's position, which keeps it stable across applies.
     */
    id?: string;
    level?: outputs.EscalationPathBetaSequencesNodeLevel;
    /**
     * Go back to an earlier node and run from there again.
     */
    loop?: outputs.EscalationPathBetaSequencesNodeLoop;
    notifyChannel?: outputs.EscalationPathBetaSequencesNodeNotifyChannel;
}

export interface EscalationPathBetaSequencesNodeBranch {
    /**
     * The key of the sequence to continue down when the condition is not met. Leave unset to end the escalation path instead.
     */
    else?: string;
    /**
     * What the branch tests. Set exactly one of these: a branch tests one thing, so combining them means nesting a second branch inside the first.
     */
    if: outputs.EscalationPathBetaSequencesNodeBranchIf;
    /**
     * The key of the sequence to continue down when the condition is met.
     */
    then: string;
}

export interface EscalationPathBetaSequencesNodeBranchIf {
    /**
     * Alert priority ids, met when the escalation came in at one of them.
     */
    priorityOneOfs?: string[];
    /**
     * The `id` of one of this escalation path's `workingHours`, met while those hours are active.
     */
    workingHoursActive?: string;
}

export interface EscalationPathBetaSequencesNodeDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathBetaSequencesNodeEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathBetaSequencesNodeLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathBetaSequencesNodeLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathBetaSequencesNodeLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathBetaSequencesNodeLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathBetaSequencesNodeLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathBetaSequencesNodeLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathBetaSequencesNodeLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathBetaSequencesNodeLoop {
    /**
     * The `id` of the node to repeat from.
     */
    backTo: string;
    /**
     * How many times to repeat these nodes
     */
    times: number;
}

export interface EscalationPathBetaSequencesNodeNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathBetaSequencesNodeNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathBetaSequencesNodeNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathBetaWorkingHour {
    /**
     * The unique identifier for this set of working intervals
     */
    id: string;
    /**
     * A human readable label for this set of working intervals
     */
    name: string;
    /**
     * How to interpret all the intervals
     */
    timezone: string;
    weekdayIntervals: outputs.EscalationPathBetaWorkingHourWeekdayInterval[];
}

export interface EscalationPathBetaWorkingHourWeekdayInterval {
    /**
     * End time of the interval, in 24hr format
     */
    endTime: string;
    /**
     * Start time of the interval, in 24hr format
     */
    startTime: string;
    /**
     * Weekdays for use within a schedule or escalation path. Possible values are: `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
     */
    weekday: string;
}

export interface EscalationPathPath {
    delay?: outputs.EscalationPathPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElse;
    level?: outputs.EscalationPathPathLevel;
    notifyChannel?: outputs.EscalationPathPathNotifyChannel;
    repeat?: outputs.EscalationPathPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    ifElse?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     *
     * This allows you to reference the node in other nodes, such as when configuring a 'repeat' node.
     */
    id: string;
    level?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition?: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds?: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig?: outputs.EscalationPathPathLevelRetryConfig;
    roundRobinConfig?: outputs.EscalationPathPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.EscalationPathPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface EscalationPathPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds?: number;
}

export interface EscalationPathPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.EscalationPathPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition?: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds?: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId?: string;
}

export interface EscalationPathPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currentlyOnCall to notify whoever is on call right now across the schedule, allUsers to notify every user attached to the schedule, or allUsersForRota / currentlyOnCallForRota / nextOnCallForRota to scope to a specific rota (in which case selectedRotaId is required). nextOnCall notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the scheduleMode applies to. Required when scheduleMode is all_users_for_rota, currently_on_call_for_rota, or next_on_call_for_rota; must be omitted for other scheduleMode values.
     */
    selectedRotaId?: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface EscalationPathPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface EscalationPathRepeatConfig {
    /**
     * When true, incident activity resets the repeat timer.
     */
    delayRepeatOnActivity: boolean;
    /**
     * Number of seconds we'll wait before repeating an escalation.
     */
    repeatAfterSeconds: number;
}

export interface EscalationPathWorkingHour {
    /**
     * The unique identifier for this set of working intervals
     */
    id: string;
    /**
     * A human readable label for this set of working intervals
     */
    name: string;
    /**
     * How to interpret all the intervals
     */
    timezone: string;
    weekdayIntervals: outputs.EscalationPathWorkingHourWeekdayInterval[];
}

export interface EscalationPathWorkingHourWeekdayInterval {
    /**
     * End time of the interval, in 24hr format
     */
    endTime: string;
    /**
     * Start time of the interval, in 24hr format
     */
    startTime: string;
    /**
     * Weekdays for use within a schedule or escalation path. Possible values are: `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
     */
    weekday: string;
}

export interface GetAlertSourcesAlertSource {
    /**
     * URL that can be used to send alert events to this source. This is only set for sources that accept webhook/HTTP events; email sources use the emailAddress field, and integration-based sources (like Jira) receive events through their native integrations.
     */
    alertEventsUrl: string;
    /**
     * Email address this alert source receives alerts to
     */
    emailAddress: string;
    /**
     * The ID of this alert source
     */
    id: string;
    jiraOptions: outputs.GetAlertSourcesAlertSourceJiraOptions;
    /**
     * Unique name of the alert source
     */
    name: string;
    /**
     * Secret token used to authenticate this source, if applicable. If applicable, this is the token that must be included in either the query string or the 'Authorization' header when sending events to this alert source.
     */
    secretToken: string;
    /**
     * Type of alert source. Possible values are: `alertmanager`, `appOptics`, `azureMonitor`, `azureDevops`, `bigPanda`, `bugsnag`, `checkly`, `chronosphere`, `cloudwatch`, `cloudflare`, `coralogix`, `cronitor`, `crowdstrikeFalcon`, `dash0`, `datadog`, `dynatrace`, `elasticsearch`, `email`, `expel`, `githubIssue`, `googleCloud`, `grafana`, `heartbeat`, `http`, `httpCustom`, `honeycomb`, `icinga2`, `incomingCalls`, `jira`, `jsm`, `monteCarlo`, `nagios`, `newRelic`, `opsgenie`, `prtg`, `pagerDuty`, `panther`, `pingdom`, `runscope`, `sns`, `sentry`, `sentryMetric`, `serviceNow`, `splunk`, `statusCake`, `statusPageViews`, `sumoLogic`, `uptime`, `vercel`, `wiz`, `zendesk`.
     */
    sourceType: string;
    template: outputs.GetAlertSourcesAlertSourceTemplate;
}

export interface GetAlertSourcesAlertSourceJiraOptions {
    /**
     * Which projects in Jira should this alert source watch for new issues? IDs can either be IDs of the projects in Jira, or ID of catalog entries in the 'Jira Project' catalog type.
     */
    projectIds: string[];
}

export interface GetAlertSourcesAlertSourceTemplate {
    /**
     * Attributes to set on alerts coming from this source, with a binding describing how to set them.
     */
    attributes: outputs.GetAlertSourcesAlertSourceTemplateAttribute[];
    description: outputs.GetAlertSourcesAlertSourceTemplateDescription;
    /**
     * Expressions that make variables available in the scope
     */
    expressions: outputs.GetAlertSourcesAlertSourceTemplateExpression[];
    /**
     * Whether or not alerts produced by this source should be private
     */
    isPrivate: boolean;
    title: outputs.GetAlertSourcesAlertSourceTemplateTitle;
    visibleToTeams: outputs.GetAlertSourcesAlertSourceTemplateVisibleToTeams;
}

export interface GetAlertSourcesAlertSourceTemplateAttribute {
    /**
     * ID of the alert attribute to set with this binding
     */
    alertAttributeId: string;
    binding: outputs.GetAlertSourcesAlertSourceTemplateAttributeBinding;
}

export interface GetAlertSourcesAlertSourceTemplateAttributeBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues: outputs.GetAlertSourcesAlertSourceTemplateAttributeBindingArrayValue[];
    /**
     * Merge strategy for this attribute when alert updates. Possible values are: `firstWins`, `lastWins`, `append`, `max`, `min`.
     */
    mergeStrategy: string;
    /**
     * The literal or reference parameter value
     */
    value: outputs.GetAlertSourcesAlertSourceTemplateAttributeBindingValue;
}

export interface GetAlertSourcesAlertSourceTemplateAttributeBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateAttributeBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateDescription {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpression {
    elseBranch: outputs.GetAlertSourcesAlertSourceTemplateExpressionElseBranch;
    /**
     * The human readable label of the expression
     */
    label: string;
    operations: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperation[];
    /**
     * A short ID that can be used to reference the expression
     */
    reference: string;
    /**
     * The root reference for this expression (i.e. where the expression starts)
     */
    rootReference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionElseBranch {
    result: outputs.GetAlertSourcesAlertSourceTemplateExpressionElseBranchResult;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionElseBranchResult {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetAlertSourcesAlertSourceTemplateExpressionElseBranchResultArrayValue[];
    expressionRef: string;
    value: outputs.GetAlertSourcesAlertSourceTemplateExpressionElseBranchResultValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetAlertSourcesAlertSourceTemplateExpressionElseBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionElseBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperation {
    branches: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranches;
    /**
     * The cast target of this operation, set when the operation type is `cast`
     */
    cast: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationCast;
    concatenate: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationConcatenate;
    filter: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationFilter;
    navigate: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationNavigate;
    /**
     * The type of the operation. Possible values are: `navigate`, `filter`, `concatenate`, `count`, `min`, `max`, `sum`, `random`, `first`, `parse`, `branches`, `cast`.
     */
    operationType: string;
    parse: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationParse;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranches {
    /**
     * The branches to apply for this operation
     */
    branches: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranch[];
    returns: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesReturns;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranch {
    /**
     * The condition groups to apply in this filter. Only one group needs to be satisfied for the filter to pass.
     */
    conditionGroups: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroup[];
    result: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchResult;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroup {
    /**
     * All conditions in this list must be satisfied for the group to be satisfied
     */
    conditions: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupCondition[];
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupCondition {
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBinding[];
    subject: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchResult {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchResultArrayValue[];
    expressionRef: string;
    value: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchResultValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationBranchesReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationCast {
    returns: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationCastReturns;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationCastReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationConcatenate {
    /**
     * The reference within the scope to concatenate with
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationFilter {
    /**
     * The condition groups to apply in this filter. Only one group needs to be satisfied for the filter to pass.
     */
    conditionGroups: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroup[];
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroup {
    /**
     * All conditions in this list must be satisfied for the group to be satisfied
     */
    conditions: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupCondition[];
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupCondition {
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBinding[];
    subject: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationFilterConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationNavigate {
    /**
     * The reference within the scope to navigate to
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationParse {
    returns: outputs.GetAlertSourcesAlertSourceTemplateExpressionOperationParseReturns;
    /**
     * Source expression that is evaluated to a result
     */
    source: string;
}

export interface GetAlertSourcesAlertSourceTemplateExpressionOperationParseReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface GetAlertSourcesAlertSourceTemplateTitle {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateVisibleToTeams {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetAlertSourcesAlertSourceTemplateVisibleToTeamsArrayValue[];
    expressionRef: string;
    value: outputs.GetAlertSourcesAlertSourceTemplateVisibleToTeamsValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetAlertSourcesAlertSourceTemplateVisibleToTeamsArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetAlertSourcesAlertSourceTemplateVisibleToTeamsValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetCatalogEntriesCatalogEntry {
    /**
     * Optional aliases that can be used to reference this entry
     */
    aliases: string[];
    attributeValues: outputs.GetCatalogEntriesCatalogEntryAttributeValue[];
    /**
     * ID of this catalog type
     */
    catalogTypeId: string;
    /**
     * An optional alternative ID for this entry, which is ensured to be unique for the type
     */
    externalId: string;
    /**
     * ID of this catalog entry
     */
    id: string;
    /**
     * Name is the human readable name of this entry
     */
    name: string;
    /**
     * When catalog type is ranked, this is used to help order things
     */
    rank: number;
}

export interface GetCatalogEntriesCatalogEntryAttributeValue {
    /**
     * The value of this element of the array, in a format suitable for this attribute type.
     */
    arrayValues: string[];
    /**
     * The ID of this attribute.
     */
    attribute: string;
    /**
     * The value of this attribute, in a format suitable for this attribute type.
     */
    value: string;
}

export interface GetCatalogEntryAttributeValue {
    /**
     * The value of this element of the array, in a format suitable for this attribute type.
     */
    arrayValues: string[];
    /**
     * The ID of this attribute.
     */
    attribute: string;
    /**
     * The value of this attribute, in a format suitable for this attribute type.
     */
    value: string;
}

export interface GetCustomFieldFilterBy {
    /**
     * This must be an attribute of the catalog type of this custom field. It must be an attribute that points to another catalog type (so not a plain string, number, or boolean attribute).
     */
    catalogAttributeId: string;
    /**
     * This must be the ID of a custom field, which must have values of the same type as the attribute you are filtering by.
     */
    customFieldId: string;
}

export interface GetEscalationPathPath {
    delay: outputs.GetEscalationPathPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElse;
    level: outputs.GetEscalationPathPathLevel;
    notifyChannel: outputs.GetEscalationPathPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    ifElse: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElse {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition[];
    /**
     * The nodes that form the levels if our condition is not met
     */
    elsePaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath[];
    /**
     * Then path nodes
     */
    thenPaths: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseElsePathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPath {
    delay: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay;
    /**
     * Reassign the escalation to another escalation path, continuing from that path's first node.
     */
    escalationPath: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath;
    /**
     * An ID for this node, unique within the escalation path.
     */
    id: string;
    level: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel;
    notifyChannel: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel;
    repeat: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat;
    /**
     * The type of this node. Available types are:
     * * level: A set of targets (users or schedules) that should be paged, either all at once, or with a round-robin configuration.
     * * notify_channel: Send the escalation to a Slack channel, where it can be acked by anyone in the channel.
     * * if_else: Branch the escalation based on a set of conditions.
     * * repeat: Go back to a previous node and repeat the logic from there.
     * * delay: Pause the escalation for a configured duration before advancing to the next node.
     * * escalation_path: Reassign the escalation to another escalation path, continuing from that path's first node.
     * * voicemail: Send an inbound caller to voicemail. Only valid inside a call route's path.
     *
     * Possible values are: `ifElse`, `repeat`, `level`, `notifyChannel`, `delay`, `voicemail`, `escalationPath`.
     */
    type: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathDelay {
    /**
     * If the delay is relative to a time window, this defines whether we advance when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    delayIntervalCondition: string;
    /**
     * How long to delay before advancing to the next node in the path, in seconds
     */
    delaySeconds: number;
    /**
     * If the delay is relative to a time window, this identifies which window it is relative to
     */
    delayWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathEscalationPath {
    /**
     * The ID of the escalation path to reassign to
     */
    escalationPathId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathIfElseThenPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathIfElseThenPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathIfElseThenPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathIfElseThenPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathIfElseThenPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathIfElseThenPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathIfElseThenPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathPathLevel {
    /**
     * Controls the behaviour of acknowledgements for this level, with 'first' cancelling all other escalations on the same level when someone acks. Possible values are: `all`, `first`.
     */
    ackMode: string;
    retryConfig: outputs.GetEscalationPathPathLevelRetryConfig;
    roundRobinConfig: outputs.GetEscalationPathPathLevelRoundRobinConfig;
    /**
     * The targets (users or schedules) for this level
     */
    targets: outputs.GetEscalationPathPathLevelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before proceeding to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathLevelRetryConfig {
    /**
     * The total number of times we page this level, counting the initial page. For example, 3 means three notifications in total. Must be between 2 and 10.
     */
    attempts: number;
    /**
     * How long we wait between attempts at this level, in seconds. Must be a whole number of minutes (divisible by 60).
     */
    intervalSeconds: number;
}

export interface GetEscalationPathPathLevelRoundRobinConfig {
    /**
     * Whether round robin is enabled for this level
     */
    enabled: boolean;
    /**
     * How long should we wait before rotating to the next target in a round robin, if not set will stick with a single target per level.
     */
    rotateAfterSeconds: number;
}

export interface GetEscalationPathPathLevelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathNotifyChannel {
    /**
     * The targets (Slack channels) for this level
     */
    targets: outputs.GetEscalationPathPathNotifyChannelTarget[];
    /**
     * If the time to ack is relative to a time window, this defines whether we move when the window is active or inactive. Possible values are: `active`, `inactive`.
     */
    timeToAckIntervalCondition: string;
    /**
     * How long should we wait for this level to acknowledge before moving on to the next node in the path?
     */
    timeToAckSeconds: number;
    /**
     * If the time to ack is relative to a time window, this identifies which window it is relative to
     */
    timeToAckWeekdayIntervalConfigId: string;
}

export interface GetEscalationPathPathNotifyChannelTarget {
    /**
     * Uniquely identifies an entity of this type
     */
    id: string;
    /**
     * Only set for schedule targets, this specifies which users to fetch from the schedule. Use currently*on*call to notify whoever is on call right now across the schedule, all*users to notify every user attached to the schedule, or all*users*for*rota / currently*on*call*for*rota / next*on*call*for*rota to scope to a specific rota (in which case selected*rota*id is required). next*on*call notifies whoever is next on call across the schedule. Possible values are: `currentlyOnCall`, `allUsersForRota`, `allUsers`, `currentlyOnCallForRota`, `nextOnCallForRota`, `nextOnCall`.
     */
    scheduleMode: string;
    /**
     * For schedule targets, identifies which rota on the schedule the schedule*mode applies to. Required when schedule*mode is all*users*for*rota, currently*on*call*for*rota, or next*on*call*for*rota; must be omitted for other schedule*mode values.
     */
    selectedRotaId: string;
    /**
     * Controls what type of entity this target identifies, such as EscalationPolicy or User. Possible values are: `schedule`, `user`, `slackChannel`, `msteamsChannel`.
     */
    type: string;
    /**
     * The urgency of this escalation path target. Possible values are: `high`, `low`.
     */
    urgency: string;
}

export interface GetEscalationPathPathRepeat {
    /**
     * How many times to repeat these nodes
     */
    repeatTimes: number;
    /**
     * Which node ID we begin repeating from.
     */
    toNode: string;
}

export interface GetEscalationPathRepeatConfig {
    /**
     * When true, incident activity resets the repeat timer.
     */
    delayRepeatOnActivity: boolean;
    /**
     * Number of seconds we'll wait before repeating an escalation.
     */
    repeatAfterSeconds: number;
}

export interface GetEscalationPathWorkingHour {
    /**
     * The unique identifier for this set of working intervals
     */
    id: string;
    /**
     * A human readable label for this set of working intervals
     */
    name: string;
    /**
     * How to interpret all the intervals
     */
    timezone: string;
    weekdayIntervals: outputs.GetEscalationPathWorkingHourWeekdayInterval[];
}

export interface GetEscalationPathWorkingHourWeekdayInterval {
    /**
     * End time of the interval, in 24hr format
     */
    endTime: string;
    /**
     * Start time of the interval, in 24hr format
     */
    startTime: string;
    /**
     * Weekdays for use within a schedule or escalation path. Possible values are: `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
     */
    weekday: string;
}

export interface GetIncidentTypesIncidentType {
    /**
     * Whether incidents of this must always, or can optionally, be created in triage. Possible values are: `always`, `optional`.
     */
    createInTriage: string;
    /**
     * What is this incident type for?
     */
    description: string;
    /**
     * Unique identifier for this Incident Type
     */
    id: string;
    /**
     * The default Incident Type is used when no other type is explicitly specified
     */
    isDefault: boolean;
    /**
     * The name of this Incident Type
     */
    name: string;
    /**
     * Should all incidents created with this Incident Type be private?
     */
    privateIncidentsOnly: boolean;
}

export interface GetScheduleRotationBetaHandover {
    /**
     * How many of the interval type to wait between handovers.
     */
    interval: number;
    /**
     * How often a handover occurs. Possible values are: `hourly`, `daily`, `weekly`.
     */
    intervalType: string;
}

export interface GetScheduleRotationBetaWorkingInterval {
    /**
     * Time of day this window closes, as HH:MM.
     */
    endTime: string;
    /**
     * Time of day this window opens, as HH:MM.
     */
    startTime: string;
    /**
     * Day of the week, lowercase. Possible values are: `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
     */
    weekday: string;
}

export interface GetWorkflowConditionGroup {
    /**
     * All conditions in this list must be satisfied for the group to be satisfied
     */
    conditions: outputs.GetWorkflowConditionGroupCondition[];
}

export interface GetWorkflowConditionGroupCondition {
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetWorkflowConditionGroupConditionParamBinding[];
    subject: string;
}

export interface GetWorkflowConditionGroupConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetWorkflowConditionGroupConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetWorkflowConditionGroupConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetWorkflowConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowDelay {
    /**
     * If this workflow is delayed, whether the conditions should be rechecked between trigger firing and execution
     */
    conditionsApplyOverDelay: boolean;
    /**
     * Delay in seconds between trigger firing and running the workflow
     */
    forSeconds: number;
}

export interface GetWorkflowExpression {
    elseBranch: outputs.GetWorkflowExpressionElseBranch;
    /**
     * The human readable label of the expression
     */
    label: string;
    operations: outputs.GetWorkflowExpressionOperation[];
    /**
     * A short ID that can be used to reference the expression
     */
    reference: string;
    /**
     * The root reference for this expression (i.e. where the expression starts)
     */
    rootReference: string;
}

export interface GetWorkflowExpressionElseBranch {
    result: outputs.GetWorkflowExpressionElseBranchResult;
}

export interface GetWorkflowExpressionElseBranchResult {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetWorkflowExpressionElseBranchResultArrayValue[];
    expressionRef: string;
    value: outputs.GetWorkflowExpressionElseBranchResultValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetWorkflowExpressionElseBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionElseBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionOperation {
    branches: outputs.GetWorkflowExpressionOperationBranches;
    /**
     * The cast target of this operation, set when the operation type is `cast`
     */
    cast: outputs.GetWorkflowExpressionOperationCast;
    concatenate: outputs.GetWorkflowExpressionOperationConcatenate;
    filter: outputs.GetWorkflowExpressionOperationFilter;
    navigate: outputs.GetWorkflowExpressionOperationNavigate;
    /**
     * The type of the operation. Possible values are: `navigate`, `filter`, `concatenate`, `count`, `min`, `max`, `sum`, `random`, `first`, `parse`, `branches`, `cast`.
     */
    operationType: string;
    parse: outputs.GetWorkflowExpressionOperationParse;
}

export interface GetWorkflowExpressionOperationBranches {
    /**
     * The branches to apply for this operation
     */
    branches: outputs.GetWorkflowExpressionOperationBranchesBranch[];
    returns: outputs.GetWorkflowExpressionOperationBranchesReturns;
}

export interface GetWorkflowExpressionOperationBranchesBranch {
    /**
     * The condition groups to apply in this filter. Only one group needs to be satisfied for the filter to pass.
     */
    conditionGroups: outputs.GetWorkflowExpressionOperationBranchesBranchConditionGroup[];
    result: outputs.GetWorkflowExpressionOperationBranchesBranchResult;
}

export interface GetWorkflowExpressionOperationBranchesBranchConditionGroup {
    /**
     * All conditions in this list must be satisfied for the group to be satisfied
     */
    conditions: outputs.GetWorkflowExpressionOperationBranchesBranchConditionGroupCondition[];
}

export interface GetWorkflowExpressionOperationBranchesBranchConditionGroupCondition {
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetWorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBinding[];
    subject: string;
}

export interface GetWorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetWorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetWorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetWorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationBranchesBranchResult {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetWorkflowExpressionOperationBranchesBranchResultArrayValue[];
    expressionRef: string;
    value: outputs.GetWorkflowExpressionOperationBranchesBranchResultValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetWorkflowExpressionOperationBranchesBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationBranchesBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationBranchesReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface GetWorkflowExpressionOperationCast {
    returns: outputs.GetWorkflowExpressionOperationCastReturns;
}

export interface GetWorkflowExpressionOperationCastReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface GetWorkflowExpressionOperationConcatenate {
    /**
     * The reference within the scope to concatenate with
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationFilter {
    /**
     * The condition groups to apply in this filter. Only one group needs to be satisfied for the filter to pass.
     */
    conditionGroups: outputs.GetWorkflowExpressionOperationFilterConditionGroup[];
}

export interface GetWorkflowExpressionOperationFilterConditionGroup {
    /**
     * All conditions in this list must be satisfied for the group to be satisfied
     */
    conditions: outputs.GetWorkflowExpressionOperationFilterConditionGroupCondition[];
}

export interface GetWorkflowExpressionOperationFilterConditionGroupCondition {
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetWorkflowExpressionOperationFilterConditionGroupConditionParamBinding[];
    subject: string;
}

export interface GetWorkflowExpressionOperationFilterConditionGroupConditionParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetWorkflowExpressionOperationFilterConditionGroupConditionParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetWorkflowExpressionOperationFilterConditionGroupConditionParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetWorkflowExpressionOperationFilterConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationFilterConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationNavigate {
    /**
     * The reference within the scope to navigate to
     */
    reference: string;
}

export interface GetWorkflowExpressionOperationParse {
    returns: outputs.GetWorkflowExpressionOperationParseReturns;
    /**
     * Source expression that is evaluated to a result
     */
    source: string;
}

export interface GetWorkflowExpressionOperationParseReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface GetWorkflowFormField {
    /**
     * Whether this field holds a list of values rather than a single value
     */
    array: boolean;
    /**
     * Optional help text shown beneath the field
     */
    description: string;
    /**
     * Stable identifier for this form field, preserved across versions
     */
    id: string;
    /**
     * The key used to reference this field in the workflow scope
     */
    key: string;
    /**
     * Whether this field must be filled in when running the workflow
     */
    required: boolean;
    /**
     * Human readable title shown in the form
     */
    title: string;
    /**
     * The engine resource type of this field
     */
    type: string;
}

export interface GetWorkflowStep {
    forEach: string;
    id: string;
    name: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.GetWorkflowStepParamBinding[];
}

export interface GetWorkflowStepParamBinding {
    /**
     * If array*value is set, this helps render the values
     */
    arrayValues: outputs.GetWorkflowStepParamBindingArrayValue[];
    expressionRef: string;
    value: outputs.GetWorkflowStepParamBindingValue;
    valueLiteral: string;
    valueReference: string;
    values: string[];
}

export interface GetWorkflowStepParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface GetWorkflowStepParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference: string;
}

export interface MaintenanceWindowAlertConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.MaintenanceWindowAlertConditionGroupCondition[];
}

export interface MaintenanceWindowAlertConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.MaintenanceWindowAlertConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface MaintenanceWindowAlertConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.MaintenanceWindowAlertConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.MaintenanceWindowAlertConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface MaintenanceWindowAlertConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface MaintenanceWindowAlertConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface MaintenanceWindowEscalationTarget {
    /**
     * Escalation paths to route alerts to
     */
    escalationPaths?: outputs.MaintenanceWindowEscalationTargetEscalationPaths;
    /**
     * Users to notify directly
     */
    users?: outputs.MaintenanceWindowEscalationTargetUsers;
}

export interface MaintenanceWindowEscalationTargetEscalationPaths {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.MaintenanceWindowEscalationTargetEscalationPathsArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.MaintenanceWindowEscalationTargetEscalationPathsValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface MaintenanceWindowEscalationTargetEscalationPathsArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface MaintenanceWindowEscalationTargetEscalationPathsValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface MaintenanceWindowEscalationTargetUsers {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.MaintenanceWindowEscalationTargetUsersArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.MaintenanceWindowEscalationTargetUsersValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface MaintenanceWindowEscalationTargetUsersArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface MaintenanceWindowEscalationTargetUsersValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface MaintenanceWindowNotifyChannel {
    /**
     * The external provider channel ID (e.g. Slack channel ID)
     */
    channelId: string;
    /**
     * Human readable name of the channel
     */
    channelName?: string;
    /**
     * The type of channel (e.g. public, private)
     */
    channelType: string;
}

export interface ScheduleBetaHolidaysPublicConfig {
    /**
     * ISO 3166-1 alpha-2 country codes for the countries that this schedule is configured to view holidays for
     */
    countryCodes: string[];
}

export interface ScheduleHolidaysPublicConfig {
    /**
     * ISO 3166-1 alpha-2 country codes for the countries that this schedule is configured to view holidays for
     */
    countryCodes: string[];
}

export interface ScheduleRotation {
    /**
     * Unique internal ID of the rotation
     */
    id: string;
    /**
     * Human readable name synced from external provider
     */
    name: string;
    versions: outputs.ScheduleRotationVersion[];
}

export interface ScheduleRotationBetaHandover {
    /**
     * How many of the interval type to wait between handovers.
     */
    interval: number;
    /**
     * How often a handover occurs. Possible values are: `hourly`, `daily`, `weekly`.
     */
    intervalType: string;
}

export interface ScheduleRotationBetaWorkingInterval {
    /**
     * Time of day this window closes, as HH:MM.
     */
    endTime: string;
    /**
     * Time of day this window opens, as HH:MM.
     */
    startTime: string;
    /**
     * Day of the week, lowercase. Possible values are: `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
     */
    weekday: string;
}

export interface ScheduleRotationVersion {
    /**
     * When this version of the rotation takes effect. A rotation can appear multiple times in `rotations` with the same `id`, scheduling changes ahead of time: each version applies from its `effectiveFrom` until the next version's. A rotation's first version has no `effectiveFrom`.
     */
    effectiveFrom?: string;
    /**
     * Determines when shifts change hands and who takes them: the first user in `users` comes on shift at this time, handing over to the next user after each `handovers` interval, cycling through the list — for example, weekly handovers from a Monday 09:00 give week-long shifts that change hands on Mondays at 09:00.
     */
    handoverStartAt: string;
    /**
     * The cadence shifts hand over on. With more than one entry, the intervals apply in turn — for example, one day then three days produces alternating one-day and three-day shifts.
     */
    handovers: outputs.ScheduleRotationVersionHandover[];
    /**
     * Controls how many people are on-call concurrently
     */
    layers: outputs.ScheduleRotationVersionLayer[];
    /**
     * The incident.io ID of a user
     */
    users: string[];
    /**
     * Optional restrictions that define when to schedule people for this rota
     */
    workingIntervals?: outputs.ScheduleRotationVersionWorkingInterval[];
}

export interface ScheduleRotationVersionHandover {
    interval: number;
    /**
     * How often a handover occurs. Possible values are: `hourly`, `daily`, `weekly`.
     */
    intervalType: string;
}

export interface ScheduleRotationVersionLayer {
    id: string;
    name: string;
}

export interface ScheduleRotationVersionWorkingInterval {
    endTime: string;
    startTime: string;
    /**
     * Weekdays for use with a schedule. Possible values are: `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday`, `sunday`.
     */
    weekday: string;
}

export interface ScheduleSyncTargetNewSlackUserGroup {
    /**
     * Description of the user group
     */
    description: string;
    /**
     * Handle of the user group
     */
    handle: string;
    /**
     * Name of the user group
     */
    name: string;
    /**
     * Slack workspace ID where the user group should be created. Required for Enterprise Grid organizations with multiple workspaces.
     */
    slackTeamId?: string;
}

export interface WorkflowConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.WorkflowConditionGroupCondition[];
}

export interface WorkflowConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.WorkflowConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface WorkflowConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.WorkflowConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.WorkflowConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface WorkflowConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowDelay {
    /**
     * If this workflow is delayed, whether the conditions should be rechecked between trigger firing and execution
     */
    conditionsApplyOverDelay: boolean;
    /**
     * Delay in seconds between trigger firing and running the workflow
     */
    forSeconds: number;
}

export interface WorkflowExpression {
    /**
     * The else branch to resort to if all operations fail
     */
    elseBranch?: outputs.WorkflowExpressionElseBranch;
    /**
     * The human readable label of the expression
     */
    label: string;
    /**
     * The operations to execute in sequence for this expression
     */
    operations: outputs.WorkflowExpressionOperation[];
    /**
     * A short ID that can be used to reference the expression
     */
    reference: string;
    /**
     * The root reference for this expression (i.e. where the expression starts)
     */
    rootReference: string;
}

export interface WorkflowExpressionElseBranch {
    /**
     * The result assumed if the else branch is reached
     */
    result: outputs.WorkflowExpressionElseBranchResult;
}

export interface WorkflowExpressionElseBranchResult {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.WorkflowExpressionElseBranchResultArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.WorkflowExpressionElseBranchResultValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface WorkflowExpressionElseBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionElseBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionOperation {
    /**
     * An operation type that allows for a value to be set conditionally by a series of logical branches
     */
    branches?: outputs.WorkflowExpressionOperationBranches;
    /**
     * An operation type that converts a value into another type. Only valid on values that can be represented as text. The returned `array` follows the value being cast, so it must match the cardinality of the previous operation
     */
    cast?: outputs.WorkflowExpressionOperationCast;
    /**
     * An operation type that adds the values behind another reference to the current value, keeping each value once. There is no delimiter, despite the name
     */
    concatenate?: outputs.WorkflowExpressionOperationConcatenate;
    /**
     * An operation type that allows values to be filtered out by conditions
     */
    filter?: outputs.WorkflowExpressionOperationFilter;
    /**
     * An operation type that allows attributes of a type to be accessed by reference
     */
    navigate?: outputs.WorkflowExpressionOperationNavigate;
    /**
     * Indicates which operation type to execute. Possible values are: `navigate`, `filter`, `concatenate`, `count`, `min`, `max`, `sum`, `random`, `first`, `parse`, `branches`, `cast`.
     */
    operationType: string;
    /**
     * An operation type that allows a value to parsed from within a JSON object
     */
    parse?: outputs.WorkflowExpressionOperationParse;
}

export interface WorkflowExpressionOperationBranches {
    /**
     * The branches to apply for this operation
     */
    branches: outputs.WorkflowExpressionOperationBranchesBranch[];
    /**
     * The return type of an operation
     */
    returns: outputs.WorkflowExpressionOperationBranchesReturns;
}

export interface WorkflowExpressionOperationBranchesBranch {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.WorkflowExpressionOperationBranchesBranchConditionGroup[];
    /**
     * The result assumed if the condition groups are satisfied
     */
    result: outputs.WorkflowExpressionOperationBranchesBranchResult;
}

export interface WorkflowExpressionOperationBranchesBranchConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.WorkflowExpressionOperationBranchesBranchConditionGroupCondition[];
}

export interface WorkflowExpressionOperationBranchesBranchConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.WorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface WorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.WorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.WorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface WorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionOperationBranchesBranchConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionOperationBranchesBranchResult {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.WorkflowExpressionOperationBranchesBranchResultArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.WorkflowExpressionOperationBranchesBranchResultValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface WorkflowExpressionOperationBranchesBranchResultArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionOperationBranchesBranchResultValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionOperationBranchesReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface WorkflowExpressionOperationCast {
    /**
     * The return type of an operation
     */
    returns: outputs.WorkflowExpressionOperationCastReturns;
}

export interface WorkflowExpressionOperationCastReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface WorkflowExpressionOperationConcatenate {
    /**
     * The reference within the scope to concatenate with
     */
    reference: string;
}

export interface WorkflowExpressionOperationFilter {
    /**
     * Groups of prerequisite conditions. All conditions in at least one group must be satisfied
     */
    conditionGroups: outputs.WorkflowExpressionOperationFilterConditionGroup[];
}

export interface WorkflowExpressionOperationFilterConditionGroup {
    /**
     * The prerequisite conditions that must all be satisfied
     */
    conditions: outputs.WorkflowExpressionOperationFilterConditionGroupCondition[];
}

export interface WorkflowExpressionOperationFilterConditionGroupCondition {
    /**
     * The logical operation to be applied
     */
    operation: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.WorkflowExpressionOperationFilterConditionGroupConditionParamBinding[];
    /**
     * The subject of the condition, on which the operation is applied
     */
    subject: string;
}

export interface WorkflowExpressionOperationFilterConditionGroupConditionParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.WorkflowExpressionOperationFilterConditionGroupConditionParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.WorkflowExpressionOperationFilterConditionGroupConditionParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface WorkflowExpressionOperationFilterConditionGroupConditionParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionOperationFilterConditionGroupConditionParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowExpressionOperationNavigate {
    reference: string;
}

export interface WorkflowExpressionOperationParse {
    /**
     * The return type of an operation
     */
    returns: outputs.WorkflowExpressionOperationParseReturns;
    /**
     * The ES5 Javascript expression to execute
     */
    source: string;
}

export interface WorkflowExpressionOperationParseReturns {
    /**
     * Whether the return value should be single or multi-value
     */
    array: boolean;
    /**
     * Expected return type of this expression (what to try casting the result to)
     */
    type: string;
}

export interface WorkflowFormField {
    /**
     * Whether this field holds a list of values rather than a single value
     */
    array: boolean;
    /**
     * Optional help text shown beneath the field
     */
    description?: string;
    /**
     * Stable identifier for this form field, preserved across versions
     */
    id: string;
    /**
     * The key used to reference this field in the workflow scope
     */
    key: string;
    /**
     * Whether this field must be filled in when running the workflow
     */
    required: boolean;
    /**
     * Human readable title shown in the form
     */
    title: string;
    /**
     * The engine resource type of this field
     */
    type: string;
}

export interface WorkflowStep {
    forEach?: string;
    id: string;
    name: string;
    /**
     * Bindings for the operation parameters
     */
    paramBindings: outputs.WorkflowStepParamBinding[];
}

export interface WorkflowStepParamBinding {
    /**
     * The array of literal or reference parameter values
     */
    arrayValues?: outputs.WorkflowStepParamBindingArrayValue[];
    /**
     * The name of an expression on this resource, whose result becomes the value. Shorthand for referencing `expressions["name"]`.
     */
    expressionRef?: string;
    /**
     * The literal or reference parameter value
     */
    value?: outputs.WorkflowStepParamBindingValue;
    /**
     * A fixed value, shorthand for `value = { literal = ... }`. A catalog entry ID is a literal, not a reference.
     */
    valueLiteral?: string;
    /**
     * A reference into the scope, shorthand for `value = { reference = ... }`.
     */
    valueReference?: string;
    /**
     * Several fixed values, shorthand for an `arrayValue` of literals. For a mix of literals and references, use `arrayValue`.
     */
    values?: string[];
}

export interface WorkflowStepParamBindingArrayValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

export interface WorkflowStepParamBindingValue {
    /**
     * If set, this is the literal value of the step parameter
     */
    literal?: string;
    /**
     * If set, this is the reference into the trigger scope that is the value of this parameter
     */
    reference?: string;
}

