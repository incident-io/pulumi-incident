// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Build a rich text document from markdown, for fields that store a document rather than a string. Use it where formatting — bold, links, lists — can't be expressed by writing the template inline. One-way: markdown in, document out.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Build a rich text document from markdown, for a field that stores a document rather
 * // than a "{{ }}" template. feature_set must match the field it's going into.
 * const alertDescription = incident.getRichText({
 *     markdown: `**Alert**: {{payload.summary}}
 *
 * Runbook: {{payload.annotations.runbook_url}}`,
 *     featureSet: "rich",
 * });
 * const prometheus = new incident.AlertSourceBeta("prometheus", {
 *     name: "Prometheus",
 *     sourceType: "http",
 *     title: {
 *         literal: "{{payload.labels.alertname}}",
 *     },
 *     description: {
 *         literal: alertDescription.then(alertDescription => alertDescription.json),
 *     },
 * });
 * export const droppedContent = alertDescription.then(alertDescription => alertDescription.droppedContents);
 * ```
 */
export function getRichText(args: GetRichTextArgs, opts?: pulumi.InvokeOptions): Promise<GetRichTextResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getRichText:getRichText", {
        "featureSet": args.featureSet,
        "markdown": args.markdown,
    }, opts);
}

/**
 * A collection of arguments for invoking getRichText.
 */
export interface GetRichTextArgs {
    /**
     * The feature set of the field this document is destined for. Content the feature set doesn't permit is dropped, and reported in dropped_content. Possible values are: `plainSingleLine`, `plainSingleLineWithSecrets`, `plainMultiLine`, `mrkdwn`, `rich`, `richWithImages`, `slackRichText`, `slackRichTextWithHeadings`, `basic`, `basicNoLinks`, `jira`.
     */
    featureSet: string;
    /**
     * The markdown to parse. Template variables written as {{ name }} become variable placeholders, and support the omit*if*unset and truncate filters.
     */
    markdown: string;
}

/**
 * A collection of values returned by getRichText.
 */
export interface GetRichTextResult {
    /**
     * Node and mark types dropped because the feature set doesn't permit them. Empty when nothing was dropped.
     */
    readonly droppedContents: string[];
    /**
     * The feature set of the field this document is destined for. Content the feature set doesn't permit is dropped, and reported in dropped_content. Possible values are: `plainSingleLine`, `plainSingleLineWithSecrets`, `plainMultiLine`, `mrkdwn`, `rich`, `richWithImages`, `slackRichText`, `slackRichTextWithHeadings`, `basic`, `basicNoLinks`, `jira`.
     */
    readonly featureSet: string;
    /**
     * The document, encoded as JSON. Assign it to the rich text field you built it for, such as an alert source template's `title` or `description` literal.
     */
    readonly json: string;
    /**
     * The markdown to parse. Template variables written as {{ name }} become variable placeholders, and support the omit*if*unset and truncate filters.
     */
    readonly markdown: string;
}
/**
 * Build a rich text document from markdown, for fields that store a document rather than a string. Use it where formatting — bold, links, lists — can't be expressed by writing the template inline. One-way: markdown in, document out.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Build a rich text document from markdown, for a field that stores a document rather
 * // than a "{{ }}" template. feature_set must match the field it's going into.
 * const alertDescription = incident.getRichText({
 *     markdown: `**Alert**: {{payload.summary}}
 *
 * Runbook: {{payload.annotations.runbook_url}}`,
 *     featureSet: "rich",
 * });
 * const prometheus = new incident.AlertSourceBeta("prometheus", {
 *     name: "Prometheus",
 *     sourceType: "http",
 *     title: {
 *         literal: "{{payload.labels.alertname}}",
 *     },
 *     description: {
 *         literal: alertDescription.then(alertDescription => alertDescription.json),
 *     },
 * });
 * export const droppedContent = alertDescription.then(alertDescription => alertDescription.droppedContents);
 * ```
 */
export function getRichTextOutput(args: GetRichTextOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetRichTextResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getRichText:getRichText", {
        "featureSet": args.featureSet,
        "markdown": args.markdown,
    }, opts);
}

/**
 * A collection of arguments for invoking getRichText.
 */
export interface GetRichTextOutputArgs {
    /**
     * The feature set of the field this document is destined for. Content the feature set doesn't permit is dropped, and reported in dropped_content. Possible values are: `plainSingleLine`, `plainSingleLineWithSecrets`, `plainMultiLine`, `mrkdwn`, `rich`, `richWithImages`, `slackRichText`, `slackRichTextWithHeadings`, `basic`, `basicNoLinks`, `jira`.
     */
    featureSet: pulumi.Input<string>;
    /**
     * The markdown to parse. Template variables written as {{ name }} become variable placeholders, and support the omit*if*unset and truncate filters.
     */
    markdown: pulumi.Input<string>;
}
