// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * `incident.EscalationPathBeta` was renamed to `incident.EscalationPath` in v7.0.
 *
 * This name still works and still manages the same thing, so upgrading to v7 needs no change
 * to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
 * be removed in v8.0.
 *
 * To move onto the new name:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * Both names are the same resource, backed by the same schema and the same API, so the move
 * carries your state across and the plan after it is empty. See `incident.EscalationPath` for the
 * documentation.
 */
export function getEscalationPathBeta(args?: GetEscalationPathBetaArgs, opts?: pulumi.InvokeOptions): Promise<GetEscalationPathBetaResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getEscalationPathBeta:getEscalationPathBeta", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getEscalationPathBeta.
 */
export interface GetEscalationPathBetaArgs {
    /**
     * Unique identifier for this escalation path.
     */
    id?: string;
    /**
     * The name of this escalation path, for the user's reference.
     */
    name?: string;
}

/**
 * A collection of values returned by getEscalationPathBeta.
 */
export interface GetEscalationPathBetaResult {
    /**
     * Unique identifier for this escalation path.
     */
    readonly id: string;
    /**
     * The name of this escalation path, for the user's reference.
     */
    readonly name: string;
    /**
     * Controls if an escalation will repeat after acknowledgement, when the alert is unresolved. When configured, it will repeat after the specified delay.
     */
    readonly repeatConfig: outputs.GetEscalationPathBetaRepeatConfig;
    /**
     * Named sequences of nodes, keyed by a name you choose. Each sequence either ends with a `branch` node or runs off the end of the escalation path. Branches reference other sequences by key.
     */
    readonly sequences: {[key: string]: outputs.GetEscalationPathBetaSequences};
    /**
     * The key of the sequence this escalation path begins with.
     */
    readonly start: string;
    /**
     * IDs of the teams that own this escalation path. This will automatically sync escalation paths with the right teams in Catalog. If you have an escalation paths attribute on your Teams, this attribute is required.
     */
    readonly teamIds: string[];
    /**
     * The working hours for this escalation path.
     */
    readonly workingHours: outputs.GetEscalationPathBetaWorkingHour[];
}
/**
 * `incident.EscalationPathBeta` was renamed to `incident.EscalationPath` in v7.0.
 *
 * This name still works and still manages the same thing, so upgrading to v7 needs no change
 * to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
 * be removed in v8.0.
 *
 * To move onto the new name:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * Both names are the same resource, backed by the same schema and the same API, so the move
 * carries your state across and the plan after it is empty. See `incident.EscalationPath` for the
 * documentation.
 */
export function getEscalationPathBetaOutput(args?: GetEscalationPathBetaOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetEscalationPathBetaResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getEscalationPathBeta:getEscalationPathBeta", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getEscalationPathBeta.
 */
export interface GetEscalationPathBetaOutputArgs {
    /**
     * Unique identifier for this escalation path.
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * The name of this escalation path, for the user's reference.
     */
    name?: pulumi.Input<string | undefined>;
}
