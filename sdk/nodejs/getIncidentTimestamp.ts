// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * View incident timestamps.
 *
 * Each incident has a number of timestamps; some being defaults that we set on
 * each incident for you, and other being configured for your organisation within
 * settings.
 *
 * Timestamps help to communicate when a given action was taken for a specific
 * incident, for example when it was reported, closed or fixed.
 *
 * Use this data source to look up an existing incident timestamp, either by `id` or by `name`. Timestamps can't be created from Terraform, so this is how you get hold of the ID of one incident.io sets for you, such as Reported or Closed, or one configured in your settings.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up one of the timestamps incident.io sets on every incident, by name.
 * // This avoids pinning an ID that differs between workspaces.
 * const reported = incident.getIncidentTimestamp({
 *     name: "Reported",
 * });
 * // Or by ID, for example one you've copied out of your organisation's settings.
 * const impactStarted = incident.getIncidentTimestamp({
 *     id: "01FCNDV6P870EA6S7TK1DSYD5H",
 * });
 * export const reportedTimestampId = reported.then(reported => reported.id);
 * ```
 */
export function getIncidentTimestamp(args?: GetIncidentTimestampArgs, opts?: pulumi.InvokeOptions): Promise<GetIncidentTimestampResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getIncidentTimestamp:getIncidentTimestamp", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getIncidentTimestamp.
 */
export interface GetIncidentTimestampArgs {
    /**
     * Unique ID of this incident timestamp
     */
    id?: string;
    /**
     * Unique name of this timestamp
     */
    name?: string;
}

/**
 * A collection of values returned by getIncidentTimestamp.
 */
export interface GetIncidentTimestampResult {
    /**
     * Unique ID of this incident timestamp
     */
    readonly id: string;
    /**
     * Unique name of this timestamp
     */
    readonly name: string;
    /**
     * Order in which this timestamp should be shown
     */
    readonly rank: number;
}
/**
 * View incident timestamps.
 *
 * Each incident has a number of timestamps; some being defaults that we set on
 * each incident for you, and other being configured for your organisation within
 * settings.
 *
 * Timestamps help to communicate when a given action was taken for a specific
 * incident, for example when it was reported, closed or fixed.
 *
 * Use this data source to look up an existing incident timestamp, either by `id` or by `name`. Timestamps can't be created from Terraform, so this is how you get hold of the ID of one incident.io sets for you, such as Reported or Closed, or one configured in your settings.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up one of the timestamps incident.io sets on every incident, by name.
 * // This avoids pinning an ID that differs between workspaces.
 * const reported = incident.getIncidentTimestamp({
 *     name: "Reported",
 * });
 * // Or by ID, for example one you've copied out of your organisation's settings.
 * const impactStarted = incident.getIncidentTimestamp({
 *     id: "01FCNDV6P870EA6S7TK1DSYD5H",
 * });
 * export const reportedTimestampId = reported.then(reported => reported.id);
 * ```
 */
export function getIncidentTimestampOutput(args?: GetIncidentTimestampOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetIncidentTimestampResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getIncidentTimestamp:getIncidentTimestamp", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getIncidentTimestamp.
 */
export interface GetIncidentTimestampOutputArgs {
    /**
     * Unique ID of this incident timestamp
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * Unique name of this timestamp
     */
    name?: pulumi.Input<string | undefined>;
}
