// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Configure your alert sources in incident.io.
 *
 * Alert sources are the systems that send alerts to incident.io, which can then be routed to the right people and teams.
 *
 * We'd generally recommend building alert sources in our [web dashboard](https://app.incident.io/~/alerts/configuration), and using the 'Export' flow to generate your Terraform, as it's easier to see what you've configured. You can also make changes to an existing alert source and copy the resulting Terraform without persisting it.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 * import * as incident from "@incident-io/pulumi";
 *
 * //# Create a basic Alert Source that receives from an SNS Topic in AWS
 * const cloudwatch = new incident.AlertSource("cloudwatch", {
 *     name: "CloudWatch Alerts",
 *     sourceType: "cloudwatch",
 *     template: {
 *         title: {
 *             literal: JSON.stringify({
 *                 content: [{
 *                     content: [{
 *                         attrs: {
 *                             label: "Payload → Title",
 *                             missing: false,
 *                             name: "title",
 *                         },
 *                         type: "varSpec",
 *                     }],
 *                     type: "paragraph",
 *                 }],
 *                 type: "doc",
 *             }),
 *         },
 *         description: {
 *             literal: JSON.stringify({
 *                 content: [{
 *                     content: [{
 *                         attrs: {
 *                             label: "Payload → Description",
 *                             missing: false,
 *                             name: "description",
 *                         },
 *                         type: "varSpec",
 *                     }],
 *                     type: "paragraph",
 *                 }],
 *                 type: "doc",
 *             }),
 *         },
 *         attributes: [{
 *             alertAttributeId: team.id,
 *             binding: {
 *                 value: {
 *                     reference: "expressions[\"cloudwatch-team\"]",
 *                 },
 *                 mergeStrategy: "first_wins",
 *             },
 *         }],
 *         expressions: [{
 *             label: "Team",
 *             operations: [{
 *                 operationType: "parse",
 *                 parse: {
 *                     returns: {
 *                         array: false,
 *                         type: "CatalogEntry[\"CatalogEntryID\"]",
 *                     },
 *                     source: "$['query_params']['team']",
 *                 },
 *             }],
 *             reference: "cloudwatch-team",
 *             rootReference: "payload",
 *         }],
 *     },
 * });
 * //# The `team` Alert Attribute we've configured to label Alerts and route alerts to schedules
 * const squad = incident.getAlertAttribute({
 *     name: "Team",
 * });
 * //# AWS Resources
 * const alerts = new aws.index.SnsTopic("alerts", {name: "cloudwatch-alerts"});
 * //# SNS Topic Subscription that routes to the incident.io Alert Source created above
 * const incidentioAlertSource = new aws.index.SnsTopicSubscription("incidentio_alert_source", {
 *     endpoint: `https://api.incident.io/v2/alert_events/cloudwatch/${cloudwatch.id}?team=platform`,
 *     endpointAutoConfirms: true,
 *     protocol: "https",
 *     topicArn: alerts.arn,
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * !/bin/bash
 *
 * Import an alert source using its ID
 * Replace the ID with a real ID from your incident.io organization
 *
 * ```sh
 * $ pulumi import incident:index/alertSource:AlertSource example 01ABC123DEF456GHI789JKL
 * ```
 */
export class AlertSource extends pulumi.CustomResource {
    /**
     * Get an existing AlertSource resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AlertSourceState, opts?: pulumi.CustomResourceOptions): AlertSource {
        return new AlertSource(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'incident:index/alertSource:AlertSource';

    /**
     * Returns true if the given object is an instance of AlertSource.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AlertSource {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AlertSource.__pulumiType;
    }

    /**
     * URL that can be used to send alert events to this source. This is only set for sources that accept webhook/HTTP events; email sources use the emailAddress field, and integration-based sources (like Jira) receive events through their native integrations.
     */
    declare public /*out*/ readonly alertEventsUrl: pulumi.Output<string>;
    /**
     * Whether alerts from this source keep counting down to auto-resolve while attached to an incident. Defaults to true. Has no effect without auto*resolve*timeout_minutes.
     */
    declare public readonly autoResolveIncidentAlerts: pulumi.Output<boolean>;
    /**
     * When set, alerts from this source will automatically resolve after this many minutes.
     */
    declare public readonly autoResolveTimeoutMinutes: pulumi.Output<number | undefined>;
    /**
     * Email address this alert source receives alerts to
     */
    declare public readonly emailAddress: pulumi.Output<string>;
    declare public readonly emailOptions: pulumi.Output<outputs.AlertSourceEmailOptions | undefined>;
    declare public readonly heartbeatOptions: pulumi.Output<outputs.AlertSourceHeartbeatOptions | undefined>;
    declare public readonly httpCustomOptions: pulumi.Output<outputs.AlertSourceHttpCustomOptions | undefined>;
    declare public readonly jiraOptions: pulumi.Output<outputs.AlertSourceJiraOptions | undefined>;
    /**
     * Unique name of the alert source
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * IDs of teams that own this alert source
     */
    declare public readonly owningTeamIds: pulumi.Output<string[] | undefined>;
    /**
     * Controls how this source's ingest rate limit is split into buckets.
     */
    declare public readonly rateLimitSharding: pulumi.Output<outputs.AlertSourceRateLimitSharding | undefined>;
    /**
     * Secret token used to authenticate this source, if applicable. If applicable, this is the token that must be included in either the query string or the 'Authorization' header when sending events to this alert source.
     */
    declare public /*out*/ readonly secretToken: pulumi.Output<string>;
    /**
     * Type of alert source. Possible values are: `alertmanager`, `appOptics`, `azureMonitor`, `azureDevops`, `bigPanda`, `bugsnag`, `checkly`, `chronosphere`, `cloudwatch`, `cloudflare`, `coralogix`, `cronitor`, `crowdstrikeFalcon`, `dash0`, `datadog`, `dynatrace`, `elasticsearch`, `email`, `expel`, `githubIssue`, `googleCloud`, `grafana`, `heartbeat`, `http`, `httpCustom`, `honeycomb`, `icinga2`, `incomingCalls`, `jira`, `jsm`, `monteCarlo`, `nagios`, `newRelic`, `opsgenie`, `prtg`, `pagerDuty`, `panther`, `pingdom`, `runscope`, `sns`, `sentry`, `sentryMetric`, `serviceNow`, `splunk`, `statusCake`, `statusPageViews`, `sumoLogic`, `uptime`, `vercel`, `wiz`, `zendesk`.
     */
    declare public readonly sourceType: pulumi.Output<string>;
    declare public readonly template: pulumi.Output<outputs.AlertSourceTemplate>;

    /**
     * Create a AlertSource resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AlertSourceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AlertSourceArgs | AlertSourceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AlertSourceState | undefined;
            resourceInputs["alertEventsUrl"] = state?.alertEventsUrl;
            resourceInputs["autoResolveIncidentAlerts"] = state?.autoResolveIncidentAlerts;
            resourceInputs["autoResolveTimeoutMinutes"] = state?.autoResolveTimeoutMinutes;
            resourceInputs["emailAddress"] = state?.emailAddress;
            resourceInputs["emailOptions"] = state?.emailOptions;
            resourceInputs["heartbeatOptions"] = state?.heartbeatOptions;
            resourceInputs["httpCustomOptions"] = state?.httpCustomOptions;
            resourceInputs["jiraOptions"] = state?.jiraOptions;
            resourceInputs["name"] = state?.name;
            resourceInputs["owningTeamIds"] = state?.owningTeamIds;
            resourceInputs["rateLimitSharding"] = state?.rateLimitSharding;
            resourceInputs["secretToken"] = state?.secretToken;
            resourceInputs["sourceType"] = state?.sourceType;
            resourceInputs["template"] = state?.template;
        } else {
            const args = argsOrState as AlertSourceArgs | undefined;
            if (args?.sourceType === undefined && !opts.urn) {
                throw new Error("Missing required property 'sourceType'");
            }
            if (args?.template === undefined && !opts.urn) {
                throw new Error("Missing required property 'template'");
            }
            resourceInputs["autoResolveIncidentAlerts"] = args?.autoResolveIncidentAlerts;
            resourceInputs["autoResolveTimeoutMinutes"] = args?.autoResolveTimeoutMinutes;
            resourceInputs["emailAddress"] = args?.emailAddress;
            resourceInputs["emailOptions"] = args?.emailOptions;
            resourceInputs["heartbeatOptions"] = args?.heartbeatOptions;
            resourceInputs["httpCustomOptions"] = args?.httpCustomOptions;
            resourceInputs["jiraOptions"] = args?.jiraOptions;
            resourceInputs["name"] = args?.name;
            resourceInputs["owningTeamIds"] = args?.owningTeamIds;
            resourceInputs["rateLimitSharding"] = args?.rateLimitSharding;
            resourceInputs["sourceType"] = args?.sourceType;
            resourceInputs["template"] = args?.template;
            resourceInputs["alertEventsUrl"] = undefined /*out*/;
            resourceInputs["secretToken"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AlertSource.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AlertSource resources.
 */
export interface AlertSourceState {
    /**
     * URL that can be used to send alert events to this source. This is only set for sources that accept webhook/HTTP events; email sources use the emailAddress field, and integration-based sources (like Jira) receive events through their native integrations.
     */
    alertEventsUrl?: pulumi.Input<string | undefined>;
    /**
     * Whether alerts from this source keep counting down to auto-resolve while attached to an incident. Defaults to true. Has no effect without auto*resolve*timeout_minutes.
     */
    autoResolveIncidentAlerts?: pulumi.Input<boolean | undefined>;
    /**
     * When set, alerts from this source will automatically resolve after this many minutes.
     */
    autoResolveTimeoutMinutes?: pulumi.Input<number | undefined>;
    /**
     * Email address this alert source receives alerts to
     */
    emailAddress?: pulumi.Input<string | undefined>;
    emailOptions?: pulumi.Input<inputs.AlertSourceEmailOptions | undefined>;
    heartbeatOptions?: pulumi.Input<inputs.AlertSourceHeartbeatOptions | undefined>;
    httpCustomOptions?: pulumi.Input<inputs.AlertSourceHttpCustomOptions | undefined>;
    jiraOptions?: pulumi.Input<inputs.AlertSourceJiraOptions | undefined>;
    /**
     * Unique name of the alert source
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * IDs of teams that own this alert source
     */
    owningTeamIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Controls how this source's ingest rate limit is split into buckets.
     */
    rateLimitSharding?: pulumi.Input<inputs.AlertSourceRateLimitSharding | undefined>;
    /**
     * Secret token used to authenticate this source, if applicable. If applicable, this is the token that must be included in either the query string or the 'Authorization' header when sending events to this alert source.
     */
    secretToken?: pulumi.Input<string | undefined>;
    /**
     * Type of alert source. Possible values are: `alertmanager`, `appOptics`, `azureMonitor`, `azureDevops`, `bigPanda`, `bugsnag`, `checkly`, `chronosphere`, `cloudwatch`, `cloudflare`, `coralogix`, `cronitor`, `crowdstrikeFalcon`, `dash0`, `datadog`, `dynatrace`, `elasticsearch`, `email`, `expel`, `githubIssue`, `googleCloud`, `grafana`, `heartbeat`, `http`, `httpCustom`, `honeycomb`, `icinga2`, `incomingCalls`, `jira`, `jsm`, `monteCarlo`, `nagios`, `newRelic`, `opsgenie`, `prtg`, `pagerDuty`, `panther`, `pingdom`, `runscope`, `sns`, `sentry`, `sentryMetric`, `serviceNow`, `splunk`, `statusCake`, `statusPageViews`, `sumoLogic`, `uptime`, `vercel`, `wiz`, `zendesk`.
     */
    sourceType?: pulumi.Input<string | undefined>;
    template?: pulumi.Input<inputs.AlertSourceTemplate | undefined>;
}

/**
 * The set of arguments for constructing a AlertSource resource.
 */
export interface AlertSourceArgs {
    /**
     * Whether alerts from this source keep counting down to auto-resolve while attached to an incident. Defaults to true. Has no effect without auto*resolve*timeout_minutes.
     */
    autoResolveIncidentAlerts?: pulumi.Input<boolean | undefined>;
    /**
     * When set, alerts from this source will automatically resolve after this many minutes.
     */
    autoResolveTimeoutMinutes?: pulumi.Input<number | undefined>;
    /**
     * Email address this alert source receives alerts to
     */
    emailAddress?: pulumi.Input<string | undefined>;
    emailOptions?: pulumi.Input<inputs.AlertSourceEmailOptions | undefined>;
    heartbeatOptions?: pulumi.Input<inputs.AlertSourceHeartbeatOptions | undefined>;
    httpCustomOptions?: pulumi.Input<inputs.AlertSourceHttpCustomOptions | undefined>;
    jiraOptions?: pulumi.Input<inputs.AlertSourceJiraOptions | undefined>;
    /**
     * Unique name of the alert source
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * IDs of teams that own this alert source
     */
    owningTeamIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Controls how this source's ingest rate limit is split into buckets.
     */
    rateLimitSharding?: pulumi.Input<inputs.AlertSourceRateLimitSharding | undefined>;
    /**
     * Type of alert source. Possible values are: `alertmanager`, `appOptics`, `azureMonitor`, `azureDevops`, `bigPanda`, `bugsnag`, `checkly`, `chronosphere`, `cloudwatch`, `cloudflare`, `coralogix`, `cronitor`, `crowdstrikeFalcon`, `dash0`, `datadog`, `dynatrace`, `elasticsearch`, `email`, `expel`, `githubIssue`, `googleCloud`, `grafana`, `heartbeat`, `http`, `httpCustom`, `honeycomb`, `icinga2`, `incomingCalls`, `jira`, `jsm`, `monteCarlo`, `nagios`, `newRelic`, `opsgenie`, `prtg`, `pagerDuty`, `panther`, `pingdom`, `runscope`, `sns`, `sentry`, `sentryMetric`, `serviceNow`, `splunk`, `statusCake`, `statusPageViews`, `sumoLogic`, `uptime`, `vercel`, `wiz`, `zendesk`.
     */
    sourceType: pulumi.Input<string>;
    template: pulumi.Input<inputs.AlertSourceTemplate>;
}
