// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manage a schedule replica that mirrors an incident.io schedule into an external provider such as PagerDuty, Opsgenie, or Jira Service Management.
 *
 * The API does not support updating a replica in place: changing any configuration forces a new resource.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Mirror a schedule into a PagerDuty schedule. The replica_provider_id is the
 * // PagerDuty schedule ID; replica_fallback_user_id is a PagerDuty user used when
 * // nobody is on-call in incident.io.
 * const platformPagerduty = new incident.ScheduleReplica("platform_pagerduty", {
 *     scheduleId: platform.id,
 *     replicaProvider: "pagerduty",
 *     replicaProviderId: "PO8107X",
 *     replicaFallbackUserId: "PA7AXXN",
 *     mirrorWindowDays: 14,
 *     sources: [{
 *         rotationId: "primary",
 *         layerId: "primary",
 *     }],
 * });
 * ```
 */
export class ScheduleReplica extends pulumi.CustomResource {
    /**
     * Get an existing ScheduleReplica resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScheduleReplicaState, opts?: pulumi.CustomResourceOptions): ScheduleReplica {
        return new ScheduleReplica(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'incident:index/scheduleReplica:ScheduleReplica';

    /**
     * Returns true if the given object is an instance of ScheduleReplica.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ScheduleReplica {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ScheduleReplica.__pulumiType;
    }

    /**
     * When this schedule replica was first created
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * The most recent error encountered while syncing this replica to the external provider, if any. Common errors include unmapped users or connectivity issues with the external provider. Null if the last sync was successful.
     */
    declare public /*out*/ readonly lastSyncError: pulumi.Output<string>;
    /**
     * When the replica was last successfully synced to the external provider. Null if the replica has never been successfully synced.
     */
    declare public /*out*/ readonly lastSyncedAt: pulumi.Output<string>;
    /**
     * How many days ahead to mirror this schedule into the external provider. Defaults to 14 if not set; maximum 90.
     */
    declare public readonly mirrorWindowDays: pulumi.Output<number>;
    /**
     * The ID of a user in the external provider that will be assigned whenever nobody is on-call in the incident.io schedule. External providers typically require someone to always be on-call, so this user fills gaps where incident.io has no one scheduled.
     */
    declare public readonly replicaFallbackUserId: pulumi.Output<string>;
    /**
     * The external provider where this schedule is replicated to. Possible values are: `native`, `pagerduty`, `opsgenie`, `jsm`.
     */
    declare public readonly replicaProvider: pulumi.Output<string>;
    /**
     * The ID of the schedule in the external provider that this replica syncs to. For PagerDuty this is the schedule ID (e.g. PO8107X), for Opsgenie the schedule ID, and for Jira Service Management the schedule ID.
     */
    declare public readonly replicaProviderId: pulumi.Output<string>;
    /**
     * The ID of the incident.io schedule that this replica is syncing from
     */
    declare public readonly scheduleId: pulumi.Output<string>;
    /**
     * The specific rotation and layer combinations from the schedule that are being replicated. Each source identifies a single layer within a rotation to sync to the external provider.
     */
    declare public readonly sources: pulumi.Output<outputs.ScheduleReplicaSource[]>;
    /**
     * When this schedule replica was last updated
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<string>;
    /**
     * The mapping status of each incident.io user in the schedule to their corresponding user in the external provider. Users must be mapped for the replica to sync their on-call shifts correctly.
     */
    declare public /*out*/ readonly userStatuses: pulumi.Output<outputs.ScheduleReplicaUserStatus[]>;

    /**
     * Create a ScheduleReplica resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ScheduleReplicaArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScheduleReplicaArgs | ScheduleReplicaState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScheduleReplicaState | undefined;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["lastSyncError"] = state?.lastSyncError;
            resourceInputs["lastSyncedAt"] = state?.lastSyncedAt;
            resourceInputs["mirrorWindowDays"] = state?.mirrorWindowDays;
            resourceInputs["replicaFallbackUserId"] = state?.replicaFallbackUserId;
            resourceInputs["replicaProvider"] = state?.replicaProvider;
            resourceInputs["replicaProviderId"] = state?.replicaProviderId;
            resourceInputs["scheduleId"] = state?.scheduleId;
            resourceInputs["sources"] = state?.sources;
            resourceInputs["updatedAt"] = state?.updatedAt;
            resourceInputs["userStatuses"] = state?.userStatuses;
        } else {
            const args = argsOrState as ScheduleReplicaArgs | undefined;
            if (args?.replicaFallbackUserId === undefined && !opts.urn) {
                throw new Error("Missing required property 'replicaFallbackUserId'");
            }
            if (args?.replicaProvider === undefined && !opts.urn) {
                throw new Error("Missing required property 'replicaProvider'");
            }
            if (args?.replicaProviderId === undefined && !opts.urn) {
                throw new Error("Missing required property 'replicaProviderId'");
            }
            if (args?.scheduleId === undefined && !opts.urn) {
                throw new Error("Missing required property 'scheduleId'");
            }
            if (args?.sources === undefined && !opts.urn) {
                throw new Error("Missing required property 'sources'");
            }
            resourceInputs["mirrorWindowDays"] = args?.mirrorWindowDays;
            resourceInputs["replicaFallbackUserId"] = args?.replicaFallbackUserId;
            resourceInputs["replicaProvider"] = args?.replicaProvider;
            resourceInputs["replicaProviderId"] = args?.replicaProviderId;
            resourceInputs["scheduleId"] = args?.scheduleId;
            resourceInputs["sources"] = args?.sources;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["lastSyncError"] = undefined /*out*/;
            resourceInputs["lastSyncedAt"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
            resourceInputs["userStatuses"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ScheduleReplica.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ScheduleReplica resources.
 */
export interface ScheduleReplicaState {
    /**
     * When this schedule replica was first created
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * The most recent error encountered while syncing this replica to the external provider, if any. Common errors include unmapped users or connectivity issues with the external provider. Null if the last sync was successful.
     */
    lastSyncError?: pulumi.Input<string | undefined>;
    /**
     * When the replica was last successfully synced to the external provider. Null if the replica has never been successfully synced.
     */
    lastSyncedAt?: pulumi.Input<string | undefined>;
    /**
     * How many days ahead to mirror this schedule into the external provider. Defaults to 14 if not set; maximum 90.
     */
    mirrorWindowDays?: pulumi.Input<number | undefined>;
    /**
     * The ID of a user in the external provider that will be assigned whenever nobody is on-call in the incident.io schedule. External providers typically require someone to always be on-call, so this user fills gaps where incident.io has no one scheduled.
     */
    replicaFallbackUserId?: pulumi.Input<string | undefined>;
    /**
     * The external provider where this schedule is replicated to. Possible values are: `native`, `pagerduty`, `opsgenie`, `jsm`.
     */
    replicaProvider?: pulumi.Input<string | undefined>;
    /**
     * The ID of the schedule in the external provider that this replica syncs to. For PagerDuty this is the schedule ID (e.g. PO8107X), for Opsgenie the schedule ID, and for Jira Service Management the schedule ID.
     */
    replicaProviderId?: pulumi.Input<string | undefined>;
    /**
     * The ID of the incident.io schedule that this replica is syncing from
     */
    scheduleId?: pulumi.Input<string | undefined>;
    /**
     * The specific rotation and layer combinations from the schedule that are being replicated. Each source identifies a single layer within a rotation to sync to the external provider.
     */
    sources?: pulumi.Input<pulumi.Input<inputs.ScheduleReplicaSource>[] | undefined>;
    /**
     * When this schedule replica was last updated
     */
    updatedAt?: pulumi.Input<string | undefined>;
    /**
     * The mapping status of each incident.io user in the schedule to their corresponding user in the external provider. Users must be mapped for the replica to sync their on-call shifts correctly.
     */
    userStatuses?: pulumi.Input<pulumi.Input<inputs.ScheduleReplicaUserStatus>[] | undefined>;
}

/**
 * The set of arguments for constructing a ScheduleReplica resource.
 */
export interface ScheduleReplicaArgs {
    /**
     * How many days ahead to mirror this schedule into the external provider. Defaults to 14 if not set; maximum 90.
     */
    mirrorWindowDays?: pulumi.Input<number | undefined>;
    /**
     * The ID of a user in the external provider that will be assigned whenever nobody is on-call in the incident.io schedule. External providers typically require someone to always be on-call, so this user fills gaps where incident.io has no one scheduled.
     */
    replicaFallbackUserId: pulumi.Input<string>;
    /**
     * The external provider where this schedule is replicated to. Possible values are: `native`, `pagerduty`, `opsgenie`, `jsm`.
     */
    replicaProvider: pulumi.Input<string>;
    /**
     * The ID of the schedule in the external provider that this replica syncs to. For PagerDuty this is the schedule ID (e.g. PO8107X), for Opsgenie the schedule ID, and for Jira Service Management the schedule ID.
     */
    replicaProviderId: pulumi.Input<string>;
    /**
     * The ID of the incident.io schedule that this replica is syncing from
     */
    scheduleId: pulumi.Input<string>;
    /**
     * The specific rotation and layer combinations from the schedule that are being replicated. Each source identifies a single layer within a rotation to sync to the external provider.
     */
    sources: pulumi.Input<pulumi.Input<inputs.ScheduleReplicaSource>[]>;
}
