// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * `incident.ScheduleBeta` was renamed to `incident.Schedule` in v7.0.
 *
 * This name still works and still manages the same thing, so upgrading to v7 needs no change
 * to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
 * be removed in v8.0.
 *
 * To move onto the new name:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * Both names are the same resource, backed by the same schema and the same API, so the move
 * carries your state across and the plan after it is empty. See `incident.Schedule` for the
 * documentation.
 */
export class ScheduleBeta extends pulumi.CustomResource {
    /**
     * Get an existing ScheduleBeta resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScheduleBetaState, opts?: pulumi.CustomResourceOptions): ScheduleBeta {
        return new ScheduleBeta(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'incident:index/scheduleBeta:ScheduleBeta';

    /**
     * Returns true if the given object is an instance of ScheduleBeta.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ScheduleBeta {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ScheduleBeta.__pulumiType;
    }

    /**
     * Public holidays to show on this schedule. Omit the block entirely to show none.
     */
    declare public readonly holidaysPublicConfig: pulumi.Output<outputs.ScheduleBetaHolidaysPublicConfig | undefined>;
    /**
     * Human readable name for the schedule
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * IDs of teams that own this schedule
     */
    declare public readonly teamIds: pulumi.Output<string[] | undefined>;
    /**
     * Timezone the schedule's rotations are anchored to, as an IANA name. Changing this replaces the schedule: a timezone is what its rotations are anchored to, and we don't support moving an existing schedule to another one.
     */
    declare public readonly timezone: pulumi.Output<string>;

    /**
     * Create a ScheduleBeta resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ScheduleBetaArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScheduleBetaArgs | ScheduleBetaState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScheduleBetaState | undefined;
            resourceInputs["holidaysPublicConfig"] = state?.holidaysPublicConfig;
            resourceInputs["name"] = state?.name;
            resourceInputs["teamIds"] = state?.teamIds;
            resourceInputs["timezone"] = state?.timezone;
        } else {
            const args = argsOrState as ScheduleBetaArgs | undefined;
            if (args?.timezone === undefined && !opts.urn) {
                throw new Error("Missing required property 'timezone'");
            }
            resourceInputs["holidaysPublicConfig"] = args?.holidaysPublicConfig;
            resourceInputs["name"] = args?.name;
            resourceInputs["teamIds"] = args?.teamIds;
            resourceInputs["timezone"] = args?.timezone;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ScheduleBeta.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ScheduleBeta resources.
 */
export interface ScheduleBetaState {
    /**
     * Public holidays to show on this schedule. Omit the block entirely to show none.
     */
    holidaysPublicConfig?: pulumi.Input<inputs.ScheduleBetaHolidaysPublicConfig | undefined>;
    /**
     * Human readable name for the schedule
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * IDs of teams that own this schedule
     */
    teamIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Timezone the schedule's rotations are anchored to, as an IANA name. Changing this replaces the schedule: a timezone is what its rotations are anchored to, and we don't support moving an existing schedule to another one.
     */
    timezone?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ScheduleBeta resource.
 */
export interface ScheduleBetaArgs {
    /**
     * Public holidays to show on this schedule. Omit the block entirely to show none.
     */
    holidaysPublicConfig?: pulumi.Input<inputs.ScheduleBetaHolidaysPublicConfig | undefined>;
    /**
     * Human readable name for the schedule
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * IDs of teams that own this schedule
     */
    teamIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Timezone the schedule's rotations are anchored to, as an IANA name. Changing this replaces the schedule: a timezone is what its rotations are anchored to, and we don't support moving an existing schedule to another one.
     */
    timezone: pulumi.Input<string>;
}
