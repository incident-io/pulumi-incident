// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manage schedule sync targets (Slack user groups that schedules can sync to).
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Create a new Slack user group as a sync target
 * const platformOncall = new incident.ScheduleSyncTarget("platform_oncall", {
 *     addBotToGroup: true,
 *     newSlackUserGroup: {
 *         name: "Platform On-Call",
 *         handle: "platform-oncall",
 *         description: "Current on-call engineers for the Platform team",
 *     },
 * });
 * // Or use an existing Slack user group
 * const existingGroup = new incident.ScheduleSyncTarget("existing_group", {
 *     addBotToGroup: true,
 *     slackUserGroupId: "S0123456789",
 * });
 * ```
 */
export class ScheduleSyncTarget extends pulumi.CustomResource {
    /**
     * Get an existing ScheduleSyncTarget resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ScheduleSyncTargetState, opts?: pulumi.CustomResourceOptions): ScheduleSyncTarget {
        return new ScheduleSyncTarget(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'incident:index/scheduleSyncTarget:ScheduleSyncTarget';

    /**
     * Returns true if the given object is an instance of ScheduleSyncTarget.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ScheduleSyncTarget {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ScheduleSyncTarget.__pulumiType;
    }

    /**
     * Whether the incident.io bot should be added to the group as a member. This is needed for some Slack configurations to let us manage the group's membership.
     */
    declare public readonly addBotToGroup: pulumi.Output<boolean>;
    /**
     * Configuration for creating a new Slack user group. Mutually exclusive with `slackUserGroupId`.
     */
    declare public readonly newSlackUserGroup: pulumi.Output<outputs.ScheduleSyncTargetNewSlackUserGroup | undefined>;
    /**
     * Slack team (workspace) ID the user group lives in. On Enterprise Grid this identifies which workspace within the org the group belongs to.
     */
    declare public /*out*/ readonly slackTeamId: pulumi.Output<string>;
    /**
     * Slack ID of the user group whose membership is kept in sync. This is the Slack-assigned group ID (starting with 'S'), not the @-handle.
     */
    declare public readonly slackUserGroupId: pulumi.Output<string>;

    /**
     * Create a ScheduleSyncTarget resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ScheduleSyncTargetArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ScheduleSyncTargetArgs | ScheduleSyncTargetState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ScheduleSyncTargetState | undefined;
            resourceInputs["addBotToGroup"] = state?.addBotToGroup;
            resourceInputs["newSlackUserGroup"] = state?.newSlackUserGroup;
            resourceInputs["slackTeamId"] = state?.slackTeamId;
            resourceInputs["slackUserGroupId"] = state?.slackUserGroupId;
        } else {
            const args = argsOrState as ScheduleSyncTargetArgs | undefined;
            if (args?.addBotToGroup === undefined && !opts.urn) {
                throw new Error("Missing required property 'addBotToGroup'");
            }
            resourceInputs["addBotToGroup"] = args?.addBotToGroup;
            resourceInputs["newSlackUserGroup"] = args?.newSlackUserGroup;
            resourceInputs["slackUserGroupId"] = args?.slackUserGroupId;
            resourceInputs["slackTeamId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ScheduleSyncTarget.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ScheduleSyncTarget resources.
 */
export interface ScheduleSyncTargetState {
    /**
     * Whether the incident.io bot should be added to the group as a member. This is needed for some Slack configurations to let us manage the group's membership.
     */
    addBotToGroup?: pulumi.Input<boolean | undefined>;
    /**
     * Configuration for creating a new Slack user group. Mutually exclusive with `slackUserGroupId`.
     */
    newSlackUserGroup?: pulumi.Input<inputs.ScheduleSyncTargetNewSlackUserGroup | undefined>;
    /**
     * Slack team (workspace) ID the user group lives in. On Enterprise Grid this identifies which workspace within the org the group belongs to.
     */
    slackTeamId?: pulumi.Input<string | undefined>;
    /**
     * Slack ID of the user group whose membership is kept in sync. This is the Slack-assigned group ID (starting with 'S'), not the @-handle.
     */
    slackUserGroupId?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a ScheduleSyncTarget resource.
 */
export interface ScheduleSyncTargetArgs {
    /**
     * Whether the incident.io bot should be added to the group as a member. This is needed for some Slack configurations to let us manage the group's membership.
     */
    addBotToGroup: pulumi.Input<boolean>;
    /**
     * Configuration for creating a new Slack user group. Mutually exclusive with `slackUserGroupId`.
     */
    newSlackUserGroup?: pulumi.Input<inputs.ScheduleSyncTargetNewSlackUserGroup | undefined>;
    /**
     * Slack ID of the user group whose membership is kept in sync. This is the Slack-assigned group ID (starting with 'S'), not the @-handle.
     */
    slackUserGroupId?: pulumi.Input<string | undefined>;
}
