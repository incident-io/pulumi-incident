// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * This data source provides a list of catalog entries for a specific catalog type.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // List catalog entries for a specific catalog type
 * const services = incident.getCatalogEntries({
 *     catalogTypeId: "01FCNDV6P870EA6S7TK1DSYDG0",
 * });
 * export const serviceNames = services.then(services => .map(entry => (entry.name)));
 * export const servicesWithExternalId = services.then(services => .filter(entry => entry.externalId != "").map(entry => ({
 *     name: entry.name,
 *     externalId: entry.externalId,
 * })));
 * export const serviceAliases = services.then(services => .filter(entry => entry.aliases.length > 0).reduce((__obj, entry) => ({ ...__obj, [entry.name]: entry.aliases }), {}));
 * ```
 */
export function getCatalogEntries(args: GetCatalogEntriesArgs, opts?: pulumi.InvokeOptions): Promise<GetCatalogEntriesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getCatalogEntries:getCatalogEntries", {
        "catalogTypeId": args.catalogTypeId,
    }, opts);
}

/**
 * A collection of arguments for invoking getCatalogEntries.
 */
export interface GetCatalogEntriesArgs {
    /**
     * The catalog type ID to list entries for.
     */
    catalogTypeId: string;
}

/**
 * A collection of values returned by getCatalogEntries.
 */
export interface GetCatalogEntriesResult {
    /**
     * List of catalog entries for the specified catalog type.
     */
    readonly catalogEntries: outputs.GetCatalogEntriesCatalogEntry[];
    /**
     * The catalog type ID to list entries for.
     */
    readonly catalogTypeId: string;
}
/**
 * This data source provides a list of catalog entries for a specific catalog type.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // List catalog entries for a specific catalog type
 * const services = incident.getCatalogEntries({
 *     catalogTypeId: "01FCNDV6P870EA6S7TK1DSYDG0",
 * });
 * export const serviceNames = services.then(services => .map(entry => (entry.name)));
 * export const servicesWithExternalId = services.then(services => .filter(entry => entry.externalId != "").map(entry => ({
 *     name: entry.name,
 *     externalId: entry.externalId,
 * })));
 * export const serviceAliases = services.then(services => .filter(entry => entry.aliases.length > 0).reduce((__obj, entry) => ({ ...__obj, [entry.name]: entry.aliases }), {}));
 * ```
 */
export function getCatalogEntriesOutput(args: GetCatalogEntriesOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetCatalogEntriesResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getCatalogEntries:getCatalogEntries", {
        "catalogTypeId": args.catalogTypeId,
    }, opts);
}

/**
 * A collection of arguments for invoking getCatalogEntries.
 */
export interface GetCatalogEntriesOutputArgs {
    /**
     * The catalog type ID to list entries for.
     */
    catalogTypeId: pulumi.Input<string>;
}
