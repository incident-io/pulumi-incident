// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * `incident.ScheduleBeta` was renamed to `incident.Schedule` in v7.0.
 *
 * This name still works and still manages the same thing, so upgrading to v7 needs no change
 * to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
 * be removed in v8.0.
 *
 * To move onto the new name:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * Both names are the same resource, backed by the same schema and the same API, so the move
 * carries your state across and the plan after it is empty. See `incident.Schedule` for the
 * documentation.
 */
export function getScheduleBeta(args?: GetScheduleBetaArgs, opts?: pulumi.InvokeOptions): Promise<GetScheduleBetaResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getScheduleBeta:getScheduleBeta", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getScheduleBeta.
 */
export interface GetScheduleBetaArgs {
    /**
     * Look up the schedule by ID.
     */
    id?: string;
    /**
     * Look up the schedule by name. Names aren't unique, so this fails if more than one schedule matches.
     */
    name?: string;
}

/**
 * A collection of values returned by getScheduleBeta.
 */
export interface GetScheduleBetaResult {
    /**
     * Look up the schedule by ID.
     */
    readonly id: string;
    /**
     * Look up the schedule by name. Names aren't unique, so this fails if more than one schedule matches.
     */
    readonly name: string;
    /**
     * IDs of teams that own this schedule
     */
    readonly teamIds: string[];
    /**
     * Timezone the schedule's rotations are anchored to, as an IANA name
     */
    readonly timezone: string;
}
/**
 * `incident.ScheduleBeta` was renamed to `incident.Schedule` in v7.0.
 *
 * This name still works and still manages the same thing, so upgrading to v7 needs no change
 * to a configuration that uses it. It is deprecated: every plan naming it warns, and it will
 * be removed in v8.0.
 *
 * To move onto the new name:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * ```
 *
 * Both names are the same resource, backed by the same schema and the same API, so the move
 * carries your state across and the plan after it is empty. See `incident.Schedule` for the
 * documentation.
 */
export function getScheduleBetaOutput(args?: GetScheduleBetaOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetScheduleBetaResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getScheduleBeta:getScheduleBeta", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getScheduleBeta.
 */
export interface GetScheduleBetaOutputArgs {
    /**
     * Look up the schedule by ID.
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * Look up the schedule by name. Names aren't unique, so this fails if more than one schedule matches.
     */
    name?: pulumi.Input<string | undefined>;
}
