// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Create and manage escalation paths.
 *
 * We'd generally recommend building escalation paths in our [web dashboard](https://app.incident.io/~/on-call/escalation-paths), and using the 'Export' flow to generate your Terraform, as it's easier to see what you've configured. You can also make changes to an existing escalation path and copy the resulting Terraform without persisting it.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // This is the primary schedule that receives pages in working hours.
 * const primaryOnCall = new incident.Schedule("primary_on_call", {
 *     name: "Primary",
 *     timezone: "Europe/London",
 *     rotations: [{
 *         id: "primary",
 *         name: "Primary",
 *         versions: [{
 *             handoverStartAt: "2024-05-01T12:00:00Z",
 *             users: [],
 *             layers: [{
 *                 id: "primary",
 *                 name: "Primary",
 *             }],
 *             handovers: [{
 *                 intervalType: "daily",
 *                 interval: 1,
 *             }],
 *         }],
 *     }],
 * });
 * // If in working hours, send high-urgency alerts. Otherwise use low-urgency.
 * const urgentSupport = new incident.EscalationPath("urgent_support", {
 *     name: "Urgent support",
 *     paths: [{
 *         id: "start",
 *         type: "if_else",
 *         ifElse: {
 *             conditions: [{
 *                 operation: "is_active",
 *                 paramBindings: [],
 *                 subject: "escalation.working_hours[\"UK\"]",
 *             }],
 *             thenPath: [
 *                 {
 *                     type: "level",
 *                     level: {
 *                         targets: [{
 *                             type: "schedule",
 *                             id: primaryOnCall.id,
 *                             urgency: "high",
 *                         }],
 *                         timeToAckSeconds: 300,
 *                     },
 *                 },
 *                 {
 *                     type: "delay",
 *                     delay: {
 *                         delaySeconds: 120,
 *                     },
 *                 },
 *                 {
 *                     type: "repeat",
 *                     repeat: {
 *                         repeatTimes: 3,
 *                         toNode: "start",
 *                     },
 *                 },
 *             ],
 *             elsePath: [{
 *                 type: "level",
 *                 level: {
 *                     targets: [{
 *                         type: "schedule",
 *                         id: primaryOnCall.id,
 *                         urgency: "low",
 *                     }],
 *                     timeToAckSeconds: 300,
 *                 },
 *             }],
 *         },
 *     }],
 *     workingHours: [{
 *         id: "UK",
 *         name: "UK",
 *         timezone: "Europe/London",
 *         weekdayIntervals: [{
 *             weekday: "monday",
 *             startTime: "09:00",
 *             endTime: "17:00",
 *         }],
 *     }],
 *     teamIds: [
 *         "01FCNDV6P870EA6S7TK1DSYD00",
 *         "01FCNDV6P870EA6S7TK1DSYD01",
 *     ],
 * });
 * // A last resort path, which the path below hands over to.
 * const fallback = new incident.EscalationPath("fallback", {
 *     name: "Fallback",
 *     paths: [{
 *         type: "level",
 *         level: {
 *             targets: [{
 *                 type: "schedule",
 *                 id: primaryOnCall.id,
 *                 urgency: "high",
 *             }],
 *             timeToAckSeconds: 300,
 *         },
 *     }],
 * });
 * // If nobody acknowledges here, reassign the escalation to the fallback path. It
 * // continues from that path's first node rather than ending unacknowledged.
 * const withReassignment = new incident.EscalationPath("with_reassignment", {
 *     name: "Support, with a fallback",
 *     paths: [
 *         {
 *             type: "level",
 *             level: {
 *                 targets: [{
 *                     type: "schedule",
 *                     id: primaryOnCall.id,
 *                     urgency: "low",
 *                 }],
 *                 timeToAckSeconds: 300,
 *             },
 *         },
 *         {
 *             type: "escalation_path",
 *             escalationPath: {
 *                 escalationPathId: fallback.id,
 *             },
 *         },
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * !/bin/bash
 *
 * Import an escalation path using its ID
 * Replace the ID with a real ID from your incident.io organization
 *
 * ```sh
 * $ pulumi import incident:index/escalationPath:EscalationPath example 01ABC123DEF456GHI789JKL
 * ```
 */
export class EscalationPath extends pulumi.CustomResource {
    /**
     * Get an existing EscalationPath resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: EscalationPathState, opts?: pulumi.CustomResourceOptions): EscalationPath {
        return new EscalationPath(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'incident:index/escalationPath:EscalationPath';

    /**
     * Returns true if the given object is an instance of EscalationPath.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is EscalationPath {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === EscalationPath.__pulumiType;
    }

    /**
     * The name of this escalation path, for the user's reference.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The nodes that form the levels and branches of this escalation path.
     *
     * -->**Note** Although the `ifElse` block is recursive, currently a maximum of 5 levels are supported. Attempting to configure more than 5 levels of nesting will result in a validation error.
     */
    declare public readonly paths: pulumi.Output<outputs.EscalationPathPath[]>;
    /**
     * Controls if an escalation will repeat after acknowledgement, when the alert is unresolved. When configured, it will repeat after the specified delay.
     */
    declare public readonly repeatConfig: pulumi.Output<outputs.EscalationPathRepeatConfig | undefined>;
    /**
     * IDs of the teams that own this escalation path. This will automatically sync escalation paths with the right teams in Catalog. If you have an escalation paths attribute on your Teams, this attribute is required.
     */
    declare public readonly teamIds: pulumi.Output<string[] | undefined>;
    /**
     * The working hours for this escalation path.
     */
    declare public readonly workingHours: pulumi.Output<outputs.EscalationPathWorkingHour[] | undefined>;

    /**
     * Create a EscalationPath resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: EscalationPathArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: EscalationPathArgs | EscalationPathState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as EscalationPathState | undefined;
            resourceInputs["name"] = state?.name;
            resourceInputs["paths"] = state?.paths;
            resourceInputs["repeatConfig"] = state?.repeatConfig;
            resourceInputs["teamIds"] = state?.teamIds;
            resourceInputs["workingHours"] = state?.workingHours;
        } else {
            const args = argsOrState as EscalationPathArgs | undefined;
            if (args?.paths === undefined && !opts.urn) {
                throw new Error("Missing required property 'paths'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["paths"] = args?.paths;
            resourceInputs["repeatConfig"] = args?.repeatConfig;
            resourceInputs["teamIds"] = args?.teamIds;
            resourceInputs["workingHours"] = args?.workingHours;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(EscalationPath.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering EscalationPath resources.
 */
export interface EscalationPathState {
    /**
     * The name of this escalation path, for the user's reference.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The nodes that form the levels and branches of this escalation path.
     *
     * -->**Note** Although the `ifElse` block is recursive, currently a maximum of 5 levels are supported. Attempting to configure more than 5 levels of nesting will result in a validation error.
     */
    paths?: pulumi.Input<pulumi.Input<inputs.EscalationPathPath>[] | undefined>;
    /**
     * Controls if an escalation will repeat after acknowledgement, when the alert is unresolved. When configured, it will repeat after the specified delay.
     */
    repeatConfig?: pulumi.Input<inputs.EscalationPathRepeatConfig | undefined>;
    /**
     * IDs of the teams that own this escalation path. This will automatically sync escalation paths with the right teams in Catalog. If you have an escalation paths attribute on your Teams, this attribute is required.
     */
    teamIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The working hours for this escalation path.
     */
    workingHours?: pulumi.Input<pulumi.Input<inputs.EscalationPathWorkingHour>[] | undefined>;
}

/**
 * The set of arguments for constructing a EscalationPath resource.
 */
export interface EscalationPathArgs {
    /**
     * The name of this escalation path, for the user's reference.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The nodes that form the levels and branches of this escalation path.
     *
     * -->**Note** Although the `ifElse` block is recursive, currently a maximum of 5 levels are supported. Attempting to configure more than 5 levels of nesting will result in a validation error.
     */
    paths: pulumi.Input<pulumi.Input<inputs.EscalationPathPath>[]>;
    /**
     * Controls if an escalation will repeat after acknowledgement, when the alert is unresolved. When configured, it will repeat after the specified delay.
     */
    repeatConfig?: pulumi.Input<inputs.EscalationPathRepeatConfig | undefined>;
    /**
     * IDs of the teams that own this escalation path. This will automatically sync escalation paths with the right teams in Catalog. If you have an escalation paths attribute on your Teams, this attribute is required.
     */
    teamIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The working hours for this escalation path.
     */
    workingHours?: pulumi.Input<pulumi.Input<inputs.EscalationPathWorkingHour>[] | undefined>;
}
