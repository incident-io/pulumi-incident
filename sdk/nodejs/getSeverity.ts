// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manage incident severities.
 *
 * Each incident has a severity, picked from one of the severities configured in your
 * organisations settings.
 *
 * Severities help categorise incidents, and communicate urgency/impact. You can use
 * severities when filtering incidents in the dashboard, and in workflows and announcement
 * rules.
 *
 * Use this data source to look up an existing incident severity, either by `id` or by `name`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up an existing incident severity by name.
 * const minor = incident.getSeverity({
 *     name: "Minor",
 * });
 * // Create an incident severity.
 * const criticalSeverity = new incident.Severity("critical", {
 *     name: "Critical",
 *     description: "A critical incident that requires an immediate response.",
 * });
 * // Reference the incident severity by its ID.
 * const critical = incident.getSeverityOutput({
 *     id: criticalSeverity.id,
 * });
 * ```
 */
export function getSeverity(args?: GetSeverityArgs, opts?: pulumi.InvokeOptions): Promise<GetSeverityResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getSeverity:getSeverity", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getSeverity.
 */
export interface GetSeverityArgs {
    /**
     * Unique identifier of the severity
     */
    id?: string;
    /**
     * Human readable name of the severity
     */
    name?: string;
}

/**
 * A collection of values returned by getSeverity.
 */
export interface GetSeverityResult {
    /**
     * Description of the severity
     */
    readonly description: string;
    /**
     * Unique identifier of the severity
     */
    readonly id: string;
    /**
     * Human readable name of the severity
     */
    readonly name: string;
    /**
     * Rank to help sort severities (lower numbers are less severe)
     */
    readonly rank: number;
}
/**
 * Manage incident severities.
 *
 * Each incident has a severity, picked from one of the severities configured in your
 * organisations settings.
 *
 * Severities help categorise incidents, and communicate urgency/impact. You can use
 * severities when filtering incidents in the dashboard, and in workflows and announcement
 * rules.
 *
 * Use this data source to look up an existing incident severity, either by `id` or by `name`.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up an existing incident severity by name.
 * const minor = incident.getSeverity({
 *     name: "Minor",
 * });
 * // Create an incident severity.
 * const criticalSeverity = new incident.Severity("critical", {
 *     name: "Critical",
 *     description: "A critical incident that requires an immediate response.",
 * });
 * // Reference the incident severity by its ID.
 * const critical = incident.getSeverityOutput({
 *     id: criticalSeverity.id,
 * });
 * ```
 */
export function getSeverityOutput(args?: GetSeverityOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetSeverityResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getSeverity:getSeverity", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getSeverity.
 */
export interface GetSeverityOutputArgs {
    /**
     * Unique identifier of the severity
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * Human readable name of the severity
     */
    name?: pulumi.Input<string | undefined>;
}
