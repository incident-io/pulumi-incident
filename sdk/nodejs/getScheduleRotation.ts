// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Look up a rotation on a schedule by `id` or `name`. Exactly one lookup field should be set.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up a rotation by name. Names are unique within a schedule, so this is
 * // unambiguous.
 * const primary = incident.getScheduleRotation({
 *     scheduleId: platformIncidentSchedule.id,
 *     name: "Primary",
 * });
 * // ...or by ID.
 * const primaryById = incident.getScheduleRotation({
 *     scheduleId: platformIncidentSchedule.id,
 *     id: "01MNO456PQR789STU012VWX",
 * });
 * // Useful for pointing an escalation path at one rotation rather than the whole
 * // schedule, when the rotation itself isn't managed here.
 * const platform = new incident.EscalationPath("platform", {
 *     name: "Platform",
 *     path: [{
 *         type: "level",
 *         level: {
 *             targets: [{
 *                 type: "schedule",
 *                 id: platformIncidentSchedule.id,
 *                 urgency: "high",
 *                 scheduleMode: "all_users_for_rota",
 *                 selectedRotaId: primary.then(primary => primary.id),
 *             }],
 *         },
 *     }],
 * });
 * ```
 */
export function getScheduleRotation(args: GetScheduleRotationArgs, opts?: pulumi.InvokeOptions): Promise<GetScheduleRotationResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getScheduleRotation:getScheduleRotation", {
        "id": args.id,
        "name": args.name,
        "scheduleId": args.scheduleId,
    }, opts);
}

/**
 * A collection of arguments for invoking getScheduleRotation.
 */
export interface GetScheduleRotationArgs {
    /**
     * Look up the rotation by ID.
     */
    id?: string;
    /**
     * Look up the rotation by name, which is unique within the schedule.
     */
    name?: string;
    /**
     * ID of the schedule this rotation belongs to
     */
    scheduleId: string;
}

/**
 * A collection of values returned by getScheduleRotation.
 */
export interface GetScheduleRotationResult {
    /**
     * How many shifts run at the same time, which is how many people are on call at once
     */
    readonly concurrentShifts: number;
    /**
     * When this shape of the rotation took over. Unset when the rotation has only ever had one.
     */
    readonly effectiveFrom: string;
    /**
     * When the first handover interval starts being counted from, which fixes the time of day and day of week shifts change hands. The dashboard calls this the handover time.
     */
    readonly firstIntervalStartsAt: string;
    /**
     * The cadence shifts hand over on, applied in turn
     */
    readonly handovers: outputs.GetScheduleRotationHandover[];
    /**
     * Look up the rotation by ID.
     */
    readonly id: string;
    /**
     * Look up the rotation by name, which is unique within the schedule.
     */
    readonly name: string;
    /**
     * Where this rotation sits in the schedule's running order, lowest first. Unset when it has never been ordered.
     */
    readonly rank: number;
    /**
     * ID of the schedule this rotation belongs to
     */
    readonly scheduleId: string;
    /**
     * How users are allocated across shifts of differing length. Possible values are: `fair`, `sequential`.
     */
    readonly schedulingMode: string;
    /**
     * IDs of the people in the rotation, in the order they take shifts.
     */
    readonly users: string[];
    /**
     * Weekday intervals the rotation is on call for, if it's restricted to any.
     */
    readonly workingIntervals: outputs.GetScheduleRotationWorkingInterval[];
}
/**
 * Look up a rotation on a schedule by `id` or `name`. Exactly one lookup field should be set.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up a rotation by name. Names are unique within a schedule, so this is
 * // unambiguous.
 * const primary = incident.getScheduleRotation({
 *     scheduleId: platformIncidentSchedule.id,
 *     name: "Primary",
 * });
 * // ...or by ID.
 * const primaryById = incident.getScheduleRotation({
 *     scheduleId: platformIncidentSchedule.id,
 *     id: "01MNO456PQR789STU012VWX",
 * });
 * // Useful for pointing an escalation path at one rotation rather than the whole
 * // schedule, when the rotation itself isn't managed here.
 * const platform = new incident.EscalationPath("platform", {
 *     name: "Platform",
 *     path: [{
 *         type: "level",
 *         level: {
 *             targets: [{
 *                 type: "schedule",
 *                 id: platformIncidentSchedule.id,
 *                 urgency: "high",
 *                 scheduleMode: "all_users_for_rota",
 *                 selectedRotaId: primary.then(primary => primary.id),
 *             }],
 *         },
 *     }],
 * });
 * ```
 */
export function getScheduleRotationOutput(args: GetScheduleRotationOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetScheduleRotationResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getScheduleRotation:getScheduleRotation", {
        "id": args.id,
        "name": args.name,
        "scheduleId": args.scheduleId,
    }, opts);
}

/**
 * A collection of arguments for invoking getScheduleRotation.
 */
export interface GetScheduleRotationOutputArgs {
    /**
     * Look up the rotation by ID.
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * Look up the rotation by name, which is unique within the schedule.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * ID of the schedule this rotation belongs to
     */
    scheduleId: pulumi.Input<string>;
}
