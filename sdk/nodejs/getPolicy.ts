// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Use this data source to look up an existing policy, either by `id` or by `name`. It reports what the policy is, not how it is configured: use the `incident.Policy` resource for a policy Terraform owns.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up a policy by name, to reference one the dashboard owns rather than
 * // managing it here.
 * const postmortems = incident.getPolicy({
 *     name: "Post-mortems within 5 working days",
 * });
 * // Or by ID, for example one copied out of your organisation's settings.
 * const followups = incident.getPolicy({
 *     id: "01FCNDV6P870EA6S7TK1DSYD5H",
 * });
 * export const postmortemPolicyType = postmortems.then(postmortems => postmortems.policyType);
 * ```
 */
export function getPolicy(args?: GetPolicyArgs, opts?: pulumi.InvokeOptions): Promise<GetPolicyResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("incident:index/getPolicy:getPolicy", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getPolicy.
 */
export interface GetPolicyArgs {
    /**
     * Unique ID of the policy
     */
    id?: string;
    /**
     * Human readable name of the policy
     */
    name?: string;
}

/**
 * A collection of values returned by getPolicy.
 */
export interface GetPolicyResult {
    /**
     * Human readable description of the policy
     */
    readonly description: string;
    /**
     * Unique ID of the policy
     */
    readonly id: string;
    /**
     * Human readable name of the policy
     */
    readonly name: string;
    /**
     * Type of the policy, specifying what this applies to. Possible values are: `debrief`, `followUp`, `onCallReadiness`, `postMortem`, `schedule`, `vacationConflict`.
     */
    readonly policyType: string;
    /**
     * Disabled policies stop evaluating but keep their config. Possible values are: `enabled`, `disabled`.
     */
    readonly status: string;
}
/**
 * Use this data source to look up an existing policy, either by `id` or by `name`. It reports what the policy is, not how it is configured: use the `incident.Policy` resource for a policy Terraform owns.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as incident from "@incident-io/pulumi";
 *
 * // Look up a policy by name, to reference one the dashboard owns rather than
 * // managing it here.
 * const postmortems = incident.getPolicy({
 *     name: "Post-mortems within 5 working days",
 * });
 * // Or by ID, for example one copied out of your organisation's settings.
 * const followups = incident.getPolicy({
 *     id: "01FCNDV6P870EA6S7TK1DSYD5H",
 * });
 * export const postmortemPolicyType = postmortems.then(postmortems => postmortems.policyType);
 * ```
 */
export function getPolicyOutput(args?: GetPolicyOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetPolicyResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("incident:index/getPolicy:getPolicy", {
        "id": args.id,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getPolicy.
 */
export interface GetPolicyOutputArgs {
    /**
     * Unique ID of the policy
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * Human readable name of the policy
     */
    name?: pulumi.Input<string | undefined>;
}
